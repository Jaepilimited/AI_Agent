# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
*.egg

# Python virtual environments
venv/
.venv/
env/

# Environment
.env
.env.*
*.env

# Credentials & Keys
*.json_key
C:/json_key/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Claude Code local settings
.claude/

# Generated charts (runtime)
app/static/charts/*.png
app/static/charts/*.html

# GWS OAuth tokens (sensitive)
data/gws_tokens/
data/*.log

# Face-search 인덱스/메타 — 큰 binary + 매일 sync로 변경됨, git에 부담
data/face_clip_index.npy
data/face_clip_meta.json
data/face_index.npy
data/face_meta.json
data/face_drive_manifest.json
data/*.bak_*
data/clip_v1_backup/

# Coredumps (루트 레벨만, app/core/ 디렉토리는 제외)
/core
core.*

# Face-search 평가 thumb 캐시
/tmp/face_eval_cache/

# Dev SQLite database (local only)
data/dev.db

# Temp files (development/debugging)
temp_*
nul

# Test result files (generated output, not source)
test_*.txt
test_*.pdf
*_result.txt
*_result.json

# Root-level data dumps
all_products.txt
product_tab.txt
product_table_info.txt
sales_all_tab.txt
sales_all_tab_full.txt

# Generated PDF/HTML reports (keep .md source only)
docs/*.pdf
docs/*.html

# Root-level Office/PDF files (reports are in docs/)
/*.docx
/*.pdf
/*.xlsx
~$*

# Temp directories
temp_logos/
temp_pdf_preview/

# CSS dev files
custom_login.css

# OS files
Thumbs.db
Desktop.ini
.DS_Store

# Broken path files (Windows encoding artifacts)
C:*
CUsersDB_PC*

# Secrets
.webui_secret_key

# Logs
logs/*
*.log
# 예외: face-search 평가 리포트는 결과물로 보존
!logs/face_eval/
!logs/face_eval/*.json

# Backup directories
backup_before_custom_frontend/
_docker_recovery_temp/
open-webui-backup/
open-webui-backup-webui.db

# Temp analysis files (root level)
ANALYSIS_SUMMARY.txt
CONTEXT_RESULTS_ANALYSIS.txt
CONTEXT_RESULTS_STATS.json
WARN_*.txt
WARN_*.csv
warn_*.txt
warn_*.csv
chart18_test.txt

# Test output logs/results (root level)
qa_*.log
qa500_regression_log.txt
retest_*.log
retest_results.json
test_results_*.json
test_results_*.json.bak*

# Local scripts (not part of app)
start_open_webui.bat
analyze_warns.py

# Screenshots (root level)
ss_*.png
screenshot_*.png

# Temp/clone directories
craver_design_clone/
custom_frontend/

# Misc temp
logo_datauri.txt
관리자/
.gstack/
.worktrees/

# Knowledge Map build cache (regenerable)
knowledge_map/.cache/
!knowledge_map/.cache/.gitkeep

# Eval pipeline credentials
.env.eval

# Conversation-data exports / DB backups (sensitive, large, regenerable)
data/gcp_convo_export.json
data/gcp_feedback_export.json
data/gcp_misc_export.json
data/_local_convo_backup_*.json
data/_local_inventory.json

# Nightly debug/auto-fix system (runtime state + generated reports)
scripts/_nightly_state.json
logs/nightly_debug/

# 컷오버용 임시 DB 덤프 (scripts/cutover_db_sync.py 가 생성·삭제)
cutover.sql
skin1004_ai*.sql

# 보고서 산출물 — 거래처명·원가·마진이 들어간다. 코드로 언제든 재생성되므로 커밋하지 않는다
docs/reports/

# 채팅으로 생성된 보고서 본문 (원가·마진 포함, DB payload 로 재생성 가능)
data/reports/

# 배포용 격리 venv (paramiko 전용). 전역 파이썬을 건드리지 않으려고 따로 둔다
sshenv/
