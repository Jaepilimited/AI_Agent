# Cluster 35

> Auto-generated 2026-09-09T03:00:59.448358+09:00 · Files: 3

## Purpose
본 클러스터는 SKIN1004 AI Agent 프로젝트에서 노션(Notion) 플랫폼으로 데이터를 내보내는 공통 저장 엔진 및 연동 설계를 담당합니다. 매일 아침 제공되는 출근 브리핑 자동 배송 기능과 사용자의 채팅 답변 내역을 노션 워크스페이스에 안정적으로 기록하고 저장하는 단일 창구 역할을 합니다.

## Key Files
- `app/core/notion_export.py` — 노션 API를 호출하여 데이터를 실제로 기록하는 핵심 저장 엔진 모듈입니다.
- `docs/superpowers/specs/2026-09-07-notion-export-design.md` — 출근 브리핑 자동 배송 및 채팅 답변 저장을 위한 노션 연동 상세 설계 문서입니다.
- `docs/notion_unshared_pages.md` — 노션 인테그레이션 설정 시 발생할 수 있는 미공유 페이지 접근 권한 이슈와 실측 사례를 정리한 트러블슈팅 문서입니다.

## Key Concepts
- **노션 API 버전 관리 (API Versioning)**: `app/core/notion_export.py` 엔진은 노션 API 헤더 버전을 `2025-09-03`으로 고정하여 사용합니다. 이전 버전(`2022-06-28`)에서 발생하던 단일 소스 DB 행 생성 제한 문제를 해결하고, 사용자가 여러 데이터 소스를 추가하더라도 저장이 실패하지 않도록 보장합니다.
- **출근 브리핑 자동 배송**: 메가와리(Megawari) 등 SKIN1004 글로벌 이커머스 운영에 필요한 일일 지표와 브리핑 데이터를 노션 페이지로 자동 생성하여 전달하는 기능입니다.
- **채팅 답변 저장**: AI Agent가 사용자와 나눈 주요 채팅 답변 및 컨텍스트를 노션에 기록하여 지식 자산화합니다.
- **인테그레이션 권한 제어**: 노션 API 호출 시 특정 페이지나 데이터베이스에 접근하지 못해 발생하는 오류를 방지하기 위해, 워크스페이스 내 미공유 페이지 권한 설정을 관리합니다.

## How It Fits In
이 클러스터는 AI Agent의 출력(Output)을 외부 협업 도구인 노션과 연결하는 최종 관문 역할을 합니다. 에이전트가 분석한 비즈니스 인사이트나 채팅 결과물을 영속화하기 위해, 다른 비즈니스 로직 클러스터들이 이 공통 노션 저장 엔진(`notion_export.py`)을 호출하여 데이터를 전송합니다.

## Common Questions This Page Answers
- **노션에 데이터를 저장할 때 API 호출이 실패하는 이유는 무엇인가요?**
  - 노션 API 헤더 버전이 옛 버전(`2022-06-28`)으로 설정되어 있으면 단일 소스 DB 외에 추가 데이터 소스가 붙을 때 저장이 실패할 수 있습니다. 반드시 `2025-09-03` 버전을 사용해야 합니다.
- **특정 노션 페이지에 접근할 수 없다는 에러가 발생하면 어떻게 해결하나요?**
  - `docs/notion_unshared_pages.md` 문서를 참고하여, 노션 인테그레이션(봇)에 해당 페이지 또는 부모 페이지의 공유 권한이 올바르게 부여되었는지 확인하십시오.