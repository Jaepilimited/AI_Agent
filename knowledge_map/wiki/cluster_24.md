# Cluster 24

> Auto-generated 2026-08-19T03:00:36.499205+09:00 · Files: 5

## Purpose
이 클러스터는 SKIN1004 AI Agent 프로젝트에서 사용자의 질문 의도를 분석하여 최적의 보고서 계획을 수립하고, 이를 동적으로 조립 및 실행하는 **보고서 생성 엔진** 역할을 합니다. LLM은 보고서의 구조적 계획(조합 선택)만 담당하고, 실제 데이터 조회와 발견(finding) 문장 도출은 규칙 기반 시스템이 처리하여 보고서의 신뢰성과 다양성을 보장합니다.

## Key Files
- `app/reports/blocks.py` — 보고서를 구성하는 최소 단위인 분석 블록을 정의하며, LLM 대신 규칙 기반으로 정확한 발견(finding) 문장을 생성합니다.
- `app/reports/dynamic.py` — 고정 스펙(`specs/*.py`)과 병행하여, 질문에 맞춰 동적으로 블록을 조립하고 실행하는 엔진입니다.
- `app/reports/intent.py` — 사용자의 질문 유형을 판정하여 질문의 본질("왜 줄었나", "어디에 집중할까" 등)에 맞는 보고서의 뼈대를 결정합니다.
- `app/reports/planner.py` — 검증된 어휘(`semantic.vocabulary`)만을 사용하여 어떤 블록, 지표, 축, 순서로 보고서를 구성할지 계획을 수립합니다.
- `docs/update_log_2026-03-30.md` — 시스템 업데이트 로그를 기록한 문서입니다.

## Key Concepts
- **분석 블록 (Analysis Block)**: 조회, 표/차트, 그리고 규칙 기반으로 도출된 발견(finding) 문장으로 구성된 보고서의 최소 단위입니다. LLM의 환각(Hallucination)을 방지하기 위해 "1위가 전체의 X%"와 같은 문장은 기계적으로 생성됩니다.
- **동적 조립 (Dynamic Assembly)**: 고정된 템플릿 분석과 달리, 질문의 성격에 맞춰 필요한 블록들을 유연하게 선택하고 결합하여 넓은 범위의 분석을 수행하는 방식입니다.
- **의도 기반 뼈대 (Intent-based Skeleton)**: 모든 질문에 천편일률적인 순서(총량 → 추세 → 구성 등)로 답하던 한계를 극복하고, 질문의 의도에 따라 보고서의 구조와 흐름을 다르게 설계하는 개념입니다.

## How It Fits In
이 클러스터는 데이터 조회 엔진, 권한 제어, 캐시 레이어와 긴밀하게 결합되어 작동합니다. 
특히 `docs/update_log_2026-03-30.md` 파일에 기록된 바와 같이, 시스템의 안정성을 확보하기 위해 **`concept:circuit_breaker` (Cluster 29)** 메커니즘을 구현 및 적용하고 있습니다. 이를 통해 보고서 생성 과정에서 발생할 수 있는 연쇄적인 장애나 과도한 리소스 소모를 방지합니다.

## Common Questions This Page Answers
- **Q. 보고서의 finding 문장에 LLM이 거짓 정보를 지어내지 않도록 어떻게 방지하나요?**
  - A. `app/reports/blocks.py`에서 LLM을 배제하고, 사전에 정의된 규칙(Rule)에 따라 데이터로부터 기계적으로 finding 문장을 추출합니다.
- **Q. 사용자의 질문이 바뀔 때마다 보고서의 구성 흐름이 어떻게 유연하게 대처하나요?**
  - A. `app/reports/intent.py`가 질문의 유형을 판정하고, `app/reports/planner.py`가 검증된 어휘만을 조합하여 질문 의도에 최적화된 보고서 계획을 동적으로 수립합니다.