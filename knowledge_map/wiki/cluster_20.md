# Cluster 20

> Auto-generated 2026-08-19T03:00:36.499205+09:00 · Files: 1

## Purpose
SKIN1004 AI Agent 시스템의 건강 상태와 데이터 무결성을 매일 스스로 검증하고 장애를 사전에 감지하기 위한 자가 점검(Self-Check) 모듈입니다. 과거 AD 동기화 실패와 같이 크론 작업은 정상 실행되었으나 실제 내부 오류로 인해 작업이 누락되는 무음 실패(Silent Failure) 문제를 방지하고 시스템 신뢰성을 유지하는 역할을 합니다.

## Key Files
- `C:/Users/DB_PC/Desktop/python_bcj/AI_Agent/app/core/self_check.py` — 시스템 건강성 및 데이터 무결성 회귀를 매일 스스로 진단하고 점검하는 자가 점검 핵심 로직

## Key Concepts
- **자가 점검 (Self-Check)**: 외부 모니터링 도구에만 의존하지 않고, 애플리케이션이 스스로 DB 연결성, 환경 변수 설정, 주기적 배치 작업의 성공 여부 등 시스템 건강 상태를 매일 검증하는 메커니즘입니다.
- **무음 실패 방지 (Preventing Silent Failure)**: 프로세스나 크론(Cron) 자체는 정상 구동 중이나 실제 내부 로직(예: 잘못된 DB 비밀번호 설정으로 인한 AD 동기화 실패 등)이 실패하여 방치되는 현상을 방지합니다.

## How It Fits In
이 클러스터는 시스템의 안정적인 운영을 뒷받침하는 독립적인 코어 진단 레이어입니다. 데이터베이스 연결 상태 및 환경 변수(`.env`)의 정합성을 직접 검증함으로써, AI Agent가 참조하는 데이터 소스의 무결성을 보장하고 시스템 전반의 신뢰성을 높입니다.

## Common Questions This Page Answers
- **배치 작업이나 동기화가 실패했음에도 시스템이 정상 작동하는 것처럼 보이는 문제를 어떻게 해결하나요?**
  - `app/core/self_check.py`를 통해 매일 시스템 건강성과 데이터 무결성을 스스로 점검하여, 로그에만 묻히고 방치될 수 있는 내부 실패를 즉각 감지합니다.
- **과거 AD 동기화 누락과 같은 환경 변수 이관 오류를 어떻게 예방하나요?**
  - 자가 점검 로직이 구동되면서 DB 비밀번호 오류 등 설정 파일(`.env`)의 정합성을 자동으로 검증하여 잘못된 설정으로 인한 장기 장애를 예방합니다.