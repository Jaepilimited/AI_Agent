# Cluster 00

> Auto-generated 2026-08-19T03:00:36.499205+09:00 · Files: 5

## Purpose
이 클러스터는 SKIN1004 AI Agent가 사용자의 질문을 정확히 이해하고, 신뢰할 수 있는 답변을 생성하도록 돕는 핵심 텍스트 처리 및 검증 엔진입니다. 한국어 자연어 처리(NLP)의 특성(교착어, 띄어쓰기 모호성)과 사내 은어 및 오타를 보정하고, LLM이 생성한 답변 속 수치가 실제 데이터와 일치하는지 철저하게 검증합니다.

## Key Files
- `app/core/term_aliases.py` — "센앰", "프바시" 등 사용자들이 자주 쓰는 사내 축약어, 은어, 오타를 LLM 호출 전에 정식 명칭으로 보정하는 사전 필터링 모듈
- `app/core/query_keywords.py` — 질문에서 불용어를 제거하고 검색에 필요한 핵심 키워드만 단일화된 규칙으로 추출하는 모듈
- `app/core/textmatch.py` — 한국어 조사나 띄어쓰기로 인해 발생하는 오탐지(예: "외부 요인도"에서 "인도"를 국가명으로 인식)를 방지하는 낱말 경계 매칭 엔진
- `app/core/answer_check.py` — 채팅 경로에서 LLM이 생성한 답변 속 수치가 실제 조회 결과에 존재하는지 검증하여 환각(Hallucination)을 방지하는 방어선
- `app/reports/insight.py` — 보고서 생성 과정에서 유일하게 LLM을 사용하여 Key Insights 및 Action Items 문장을 작성하고, 문장 내 수치 정합성을 검증하는 모듈

## Key Concepts
- **사내 용어 보정 (Term Aliasing)**: "센앰"(센텔라 앰플), "포마"(포어마이징) 등 SKIN1004 임직원들이 사용하는 고유 축약어와 오타를 표준 제품명 및 용어로 변환하여 검색 정확도를 높입니다.
- **낱말 경계 매칭 (Word Boundary Matching)**: 한국어의 교착어 특성상 단순 포함 검사(`in`)를 사용할 때 발생하는 오탐지를 막기 위해, 낱말의 경계를 명확히 구분하여 매칭합니다.
- **수치 검증 방어선 (Numeric Verification)**: LLM이 그럴듯하지만 틀린 숫자를 지어내는 현상을 막기 위해, 생성된 문장 속 수치가 실제 DB 조회 결과에 존재하는지 대조하고 일치하지 않으면 해당 문장을 폐기합니다.

## How It Fits In
이 클러스터는 AI Agent의 입력(Input) 전처리 단계와 출력(Output) 검증 단계 모두에서 핵심적인 필터 역할을 수행합니다. 
특히 `app/reports/insight.py`는 보고서 생성 파이프라인의 핵심 개념인 **insight_generation (cluster_17)**을 구현하며, 규칙 기반 템플릿이 채울 수 없는 "Action Item"을 LLM으로 안전하게 생성하는 역할을 담당합니다.

## Common Questions This Page Answers
- 사용자가 "프바시"나 "힝라우선세럼" 같은 오타/축약어로 질문했을 때 시스템이 어떻게 올바른 제품을 찾아내나요?
- "외부 요인도 같이 봐줘"라는 문장에서 "인도"라는 국가명이 검색 키워드로 오탐지되는 문제를 어떻게 해결했나요?
- LLM이 답변이나 보고서에 잘못된 수치(환각)를 적어내는 것을 어떻게 방지하고 있나요?