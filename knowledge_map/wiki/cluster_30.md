# Cluster 30

> Auto-generated 2026-09-09T03:00:59.448358+09:00 · Files: 2

## Purpose
본 클러스터는 SKIN1004 AI Agent가 생성하는 답변 및 보고서 내의 수치 데이터 정합성을 검증하고 보장하는 역할을 합니다. LLM이 생성한 텍스트(채팅 답변 및 인사이트 보고서)에 포함된 숫자가 실제 데이터베이스 조회 결과와 일치하는지 확인하여, 환각(Hallucination)으로 인한 잘못된 수치 제공을 방지합니다.

## Key Files
- `app/core/answer_check.py` — 사용자가 가장 많이 사용하는 채팅 경로에서 생성된 답변 속 수치가 실제 조회 결과에 존재하는지 검증하고 방어하는 모듈입니다.
- `app/reports/README.md` — 보고서 생성 파이프라인의 구조와 수치 검증 방어선(`app/reports/insight.py` 등)이 어떻게 작동하는지 설명하는 문서입니다.

## Key Concepts
- **수치 검증 (Numerical Verification)**: LLM이 생성한 문장 속에 포함된 수치(예: 매출액, 메가와리 할인율 등)가 실제 SQL 조회 결과나 소스 데이터에 존재하는지 교차 검증하는 프로세스입니다.
- **채팅 경로 방어선**: 기존 보고서 생성 파이프라인(`app/reports/insight.py`)에만 존재하던 수치 검증 로직을 가장 빈번하게 사용되는 일반 채팅 답변 경로(`app/core/answer_check.py`)에 확장 적용하여 시스템의 안정성을 높였습니다.

## How It Fits In
이 클러스터는 AI Agent의 신뢰성을 담보하는 핵심 안전장치 역할을 합니다. 데이터베이스 조회 클러스터가 전달한 원본 데이터와 LLM이 최종 생성한 답변 사이에서 일종의 필터(Filter)로 작동하며, 검증을 통과하지 못한 잘못된 수치가 포함된 문장은 사용자에게 노출되지 않도록 차단하거나 폐기합니다.

## Common Questions This Page Answers
- **LLM이 답변에 그럴듯하지만 틀린 숫자를 지어내어 답변할 때 어떻게 방어하나요?**
  - `app/core/answer_check.py`를 통해 답변 속 수치가 실제 조회 결과에 존재하는지 검증하고, 일치하지 않는 경우 해당 오류를 걸러냅니다.
- **보고서 파이프라인과 채팅 파이프라인의 수치 검증은 각각 어떻게 관리되나요?**
  - 보고서 파이프라인의 검증 구조는 `app/reports/README.md`에 정의되어 있으며, 채팅 경로의 검증은 `app/core/answer_check.py`에서 독립적으로 수행됩니다.