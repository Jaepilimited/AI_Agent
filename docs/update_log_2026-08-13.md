# Update Log — 2026-08-06~13 (질문형 보고서 + 초상권 사진식별 + 골든셋 회귀 + Opus 5)

56 커밋 · 65개 파일 · +32,871 / −7,092 줄

## 변경 사항

### 1. [기능] 질문으로 받는 분석 보고서 (2026-08-12~13)
- "2026년 일본 매출 보고서 만들어줘" → 요약 4줄 + [보고서 열기] 링크. 절을 질문에 맞춰 조합한다 (고정 보고서가 아니다)
- 분석 블록 13종: 총량·추세·구성·전년대비·순위·교차·기여도·집중도·급변·믹스변화·계절성·비율·프로모션 대조
- **LLM 은 SQL 도 숫자도 쓰지 않는다.** 플래너는 "어떤 지표를 어떤 축으로"만 고르고, SQL 은 의미론 계층이 결정적으로 만든다. 서술 문장은 규칙이 숫자에서 뽑는다 — 서술에 숫자 리터럴이 있으면 렌더러가 거부한다
- 질문 속 국가·팀·브랜드·권역 필터를 결정적으로 뽑아 **모든 절에 강제**한다. 한 절이라도 전사를 집계하면 질문에 답하지 않은 문서가 된다
- 열람은 **본인이 만든 것만**. admin 도 남의 보고서를 열 수 없다 (원가·마진·거래처별 FOC율이 들어간다)
- 보고서는 **명시했을 때만** 만든다 (2026-08-13 규칙): `@@보고서` 지정 또는 질문에 "보고서"·"리포트". 조회 8~12회에 10~30초가 드는 특수 경로다

### 2. [기능] 모델 초상권 — 사진으로 "이 모델 써도 되나" 판정 (2026-08-07, 08-10)
- 초상권 DB 적재: 모델 33명 · 계약 281건. "이 사진 써도 돼?"에 사용 가능 여부·기한·에이전시로 답한다
- 사진을 첨부하면 **얼굴을 식별해** 해당 모델의 초상권을 찾는다 (일부 크롭된 사진도 처리). 등록 5명
- `@@초상권`(별칭 `@@bc`) 데이터소스 + System Status 상태 표시

### 3. [기능] 골든셋 회귀 시스템 (2026-08-06, 08-12)
- 매일 05:30 자동 실행, 일요일은 weekly 포함 전체. Admin > `골든셋` 탭에서 런 간 비교
- 35문항으로 시작해 **53문항**까지 확대 (집계 계약·초상권·@@ 소스 격리 회귀 포함)
- ⛔ 기대 키워드가 실패 답변에도 들어가면 그 문항은 무용지물이다 — 실제로 거짓 통과가 발생해 판정에서 후속 질문 블록을 분리했다
- ⛔ 문항 질문이 되묻기를 유발하면 아무것도 검증하지 못한다 — 기간·범위를 질문에 명시하도록 교정

### 4. [기능] 공식 한글 팀명 + 본부 체계 (2026-08-11)
- 공식 팀명 12개(한글)와 데이터의 코드값을 **양방향으로 결정적 변환**한다. 입력은 SQL 리터럴 교정, 출력은 `영업1팀(B2B1)` 치환 — 표·차트 라벨·인사이트가 한 번에 맞는다
- 본부(Division) 매핑 도입 — 본부별 질문을 조직도 기준으로 답한다
- 프롬프트에 손으로 적어 둔 DISTINCT 목록이 오답을 유도하던 것을 실측값으로 교정

### 5. [기능] 프로모션 캘린더 데이터소스 (2026-08-11, 08-13)
- 어느 팀이 어느 몰에서 언제 프로모션을 하는지 — 계획 데이터라 미래 날짜가 정상이다
- 이 테이블만 값 체계가 다르다(`team_id='west-mkt'`, `country_code='ID'`) — 결정적 교정을 처음부터 넣었다
- 보고서의 `프로모션 대조` 절이 **매출의 주기적 등락을 일정과 맞춰 본다.** 매출 톱니를 보고 "행사였겠지" 추정하지 않는다

### 6. [기능] LLM·BigQuery 토큰/비용 계측 (2026-08-06)
- "운영 비용이 얼마인가"에 숫자로 답한다 — `llm_usage` 테이블 + Admin API

### 7. [성능] 채팅 모델 Opus 5 상향 + 응답 지연 −37% (2026-08-13)
- 채팅 모델 `claude-opus-4-8` → `claude-opus-5` (요율 동일). Opus 5 는 thinking 이 기본 ON 이라 max_tokens 가 답변을 자르고 응답 파싱이 깨지던 것을 함께 막았다
- **인사말이 구글 검색을 돌고 있었다** — "오늘" 한 단어가 검색 키워드라 "안녕? 오늘 뭐 도와줄 수 있어?"가 웹 검색을 탔다
- 그 검색이 Gemini **Pro** 를 쓰고 있었다 (multi 경로는 이미 Flash 로 바꿨는데 direct 만 남아 있었다). Pro 7.8s vs Flash 2.0s
- 실측: 해당 질문 22.8초 → **12.2초** (−46%), 전체 런 13.3초 → **8.4초** (−37%)

### 8. [버그픽스] 보고서 차트가 거짓말하던 것 (2026-08-13)
- 추세 차트의 **바닥이 0이 아니라 최솟값**이었다. 최저 6.6억(최고의 12%)이 0으로 보였다
- 축이 하나도 없어 어느 봉우리가 언제인지 알 수 없었다 → 0 기준 막대 차트 + 월 라벨·금액 눈금·최고점 강조
- 2행짜리 절을 문장으로 바꾸면서 **막대 차트 3개가 조용히 사라져** 있던 것 복구

### 9. [버그픽스] 차트가 하루 38건 조용히 사라지던 원인 (2026-08-11)
- Gemini 가 JSON 모드에서 닫는 괄호를 빠뜨려 차트 생성이 실패하고 있었다. 정상 종료(finish_reason=STOP)한 응답이라 재시도로도 안 없어진다
- 문자열 밖 괄호를 세어 결정적으로 교정. 진단 정보는 WARNING 으로 남기도록 변경 (프로덕션은 앱 INFO 로그를 통째로 버린다)

### 10. [버그픽스] 보고서 라우팅 오분류 (2026-08-13)
- **"보고서 기능은 언제 쓰면 좋아?" 가 매번 전사 매출 보고서를 만들고 있었다** (10건 생성). 기능을 묻는 질문에 문서를 만들면서 질문에는 답하지 못했다
- 문구를 하나씩 막는 방식으로는 끝나지 않아, **대상이 시스템 자신이고 데이터 명사가 없으면 분석할 대상이 없는 질문**이라는 구조로 판정하도록 바꿨다

### 11. [버그픽스] 보고서 필터 결함 3건 (2026-08-13)
- **브랜드가 아예 안 잡히고 있었다** — "우마 매출 보고서"가 전사 보고서로 나갔다
- **팀명 안의 국가어를 국가로 잡았다** — "중국사업팀"이 `팀=CBT AND 국가=중국`이 돼 CBT 의 중국 외 실적이 통째로 빠졌다
- **세부 권역을 대륙으로 잡았다** — "동남아시아"가 `대륙=아시아`로 잡혀 동아시아·서남아시아까지 들어왔다

### 12. [버그픽스] 컨텍스트 무손실 아키텍처 (2026-08-10)
- 후속 질문이 주제를 이탈하던 것 — 직전 SQL 원문 보존 + 결정적 스키마 포함 + 계층 절단. 8종 시나리오 8/8 통과
- `@@` 소스 격리를 **서버가 강제**한다 (프론트 값을 믿지 않는다). 명시한 소스는 되묻지 않는다

### 13. [데이터] 원가·FOC·할인 집계 계약 확정 (2026-08-12)
- `SUM(Production_Cost2)` 는 FOC 원가를 **이미 포함**한다 — `+ SUM(FOC)` 하면 이중계상이다. `FOC_or_Not` 으로 분리 집계
- 직접비 = 유상원가 + FOC원가 + 할인, 매출총이익 = 매출 − 직접비. 수수료·고정비가 빠져 있어 **공헌이익도 영업이익도 아니다**
- ⚠️ `Service_Fee` 가 음수로 깨져 있다(19개월 −26.2억) → 이 데이터로 진짜 수익성은 낼 수 없다
- ⚠️ **원가 미적재 브랜드**: UM 99.9% · CBT 99.6% 가 원가 0원 → 원가·수익성 분석은 SK 한정
- ⚠️ **제품명 미적재**: UM·CBT 는 제품명이 100% 비어 있고 Product 테이블에 행이 없다 → 제품별 절은 나올 수 없다. 안 팔린 것이 아니라 이름이 없는 것이라 보고서가 그 사실을 공시한다

### 14. [문서] 사용 가이드·개발 규칙 갱신 (2026-08-12~13)
- 사내 사용 가이드(노션·PPT)에 **보고서로 받기** 섹션 추가 — 만드는 방법 두 가지와 검증된 예시 질문
- 개발 규칙(CLAUDE.md)과 자동 생성 사본(AGENTS.md) 동기화 스크립트 도입 — 손으로 관리하던 사본이 두 달 밀려 **이관 전 프로덕션 주소와 금지된 수량 컬럼 규칙이 그쪽에만 살아 있었다**

## 수정 파일 (주요 신규)

| 파일 | 내용 |
|------|------|
| app/reports/ (16개) | 질문형 보고서 파이프라인 — 의미론 계층·블록·플래너·렌더·권한 |
| app/core/golden_runner.py | 골든셋 회귀 실행기 |
| app/core/model_rights.py, face_embed.py | 초상권 판정 + 얼굴 식별 |
| app/core/usage_meter.py | LLM·BigQuery 토큰/비용 계측 |
| app/api/reports_api.py | 보고서 열람 API (본인만) |
