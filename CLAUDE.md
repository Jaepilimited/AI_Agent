# SKIN1004 AI Agent — 개발 규칙

> **이 문서가 규칙의 단일 소스다.** `AGENTS.md`(Codex 등이 읽는다)는 자동 생성 사본이므로
> 직접 고치지 말고, 여기를 고친 뒤 `python scripts/sync_agents_md.py` 를 돌린다.
> 손으로 관리하던 시절 사본이 두 달 밀려, **이관 전 프로덕션 주소와 지금은 금지된 수량 컬럼 규칙이
> 그쪽에만 살아 있었다** (2026-08-12 발견·정리).

## 🧠 Knowledge Map (먼저 읽기 — 필수)

**모든 작업 전에 다음 순서를 지켜라**:

1. **먼저** `knowledge_map/GRAPH_REPORT.md`를 읽는다. 한 페이지에 프로젝트 전체 구조·중심 노드·최근 변경이 요약돼 있다.
2. 필요하면 `knowledge_map/graph.json`을 읽어 관련 노드 2~3개만 골라낸다 (id, cluster, wiki_page 필드).
3. 골라낸 노드의 `wiki_page` 경로(`knowledge_map/wiki/**.md`)만 Read한다.
4. **그래도 부족할 때만** 원본 파일(`app/**`, `docs/**`)을 Read하거나 Grep한다.

**금지 행동**:
- GRAPH_REPORT.md를 건너뛰고 바로 Grep/Glob하지 마라. 토큰 낭비다.
- `knowledge_map/` 디렉토리를 무시하지 마라. 매일 03:00 자동 업데이트되는 신뢰 가능한 소스다.
- 지도가 낡았다고 판단되면 `python scripts/build_knowledge_graph.py --force` 실행을 제안하라.

**지도가 커버하지 못하는 영역**:
- `tests/`, `scripts/` 일회성 파일, `backup_*`, `logs/`, `temp_*`, `app/frontend/`, `app/static/` — 이들은 지도에 없다. 필요시 직접 탐색.

## Claude Code 작업 메모 — 반복해서 걸린 것들

- ⛔ **서버에서 스크립트를 돌릴 땐 앱과 같은 환경을 줘라.** 자격증명·프록시가 systemd 에만
  있어 그냥 돌리면 BigQuery `DefaultCredentialsError`·구글 `TransportError` 가 난다:
  `set -a; . /home/jeffrey/AI_Agent/.env; set +a; export HTTPS_PROXY=http://10.1.50.2:3128 HTTP_PROXY=$HTTPS_PROXY NO_PROXY=localhost,127.0.0.1,10.1.0.0/16`
- ⛔ **가드 테스트가 특정 낱말을 막는다** — 주석에 써도 걸린다. 우회하지 말고 표현을 바꿔라:
  `briefing.py` 에 `claude`·`llm`·`gemini` 금지(LLM 미사용 보증) · `personal-briefing.js` 에
  `localStorage` 금지(구글 데이터 영속 저장 금지)
- ⛔ **블록을 인덱스로 치환할 땐 끝 표식을 반드시 검증하라.** 시작만 맞추고 다음 블록을 끝으로
  삼아 `style.css` 에서 **2,600줄(70KB)을 지운 적 있다** — `assert` 로 길이·표식을 확인할 것
- ⚠️ **bash heredoc 에 이모지를 넣지 마라** — `unexpected EOF` 로 죽는다. 이모지가 든 파일은
  Write 도구로 쓰거나, 패치 스크립트를 파일로 쓴 뒤 실행할 것
- ⚠️ **콘솔이 cp949 다.** `print()` 에 한글·이모지·`—` 를 넣으면 `UnicodeEncodeError` 로
  스크립트가 **파일을 쓴 뒤 중단**된다. 비ASCII 를 찍으려면
  `sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8")`
- ⚠️ `/tmp` 는 Git Bash 전용이라 **윈도우 파이썬이 못 읽는다** — 임시 파일은 스크래치패드에
- ⚠️ **이 작업트리를 다른 세션이 함께 쓴다.** 편집 전 파일을 다시 읽고, 전체 테스트 실패가
  내 변경 탓인지 먼저 확인하라 (남의 미완성 파일이 섞여 들어온다)
- ⚠️ `git diff` 가 `style.css` 를 통째로 바꾼 것처럼 보이면 CRLF 잡음이다 —
  `git diff --ignore-all-space` 로 실제 변경만 본다
- ⚠️ 배포 스크립트가 `/health 000 · 기동 에러 N건` 을 찍어도 **오탐일 수 있다**
  (`sleep 12` 가 짧을 때). `systemctl is-active` 와 `/health` 를 직접 확인할 것
- 💡 프론트 검증: `node --check <js>` · 브라우저 테스트는 Playwright (`tests/frontend/`).
  ⚠️ `pytest --timeout` 은 없다 (플러그인 미설치)

## 배포 규칙 (최우선) — 2026-07-30 신규 서버 이관 완료

### ⚠️ 프로덕션은 더 이상 172.16.1.250 이 아니다

- **프로덕션 = `http://10.1.100.5` (사내 신규 서버, IT팀 관리 VM)**
  - Web `10.1.100.5` (nginx) → WAS `10.1.150.5` (앱, systemd `ai-craver`)
  - APP `10.1.150.105` (배치 크론) / DB `10.1.200.5` (MariaDB 10.11, DB명 `ai`)
- **172.16.1.250:3000 은 리다이렉트 껍데기다.** 앱은 떠 있지만 모든 요청을 307로
  신규 서버에 넘기고 배치 스케줄러도 꺼져 있다. **여기에만 배포하면 아무것도 반영되지 않는다.**

### 배포 흐름 (두 곳 모두 반영 — 컷오버 후 병행 기간)

```
python scripts/deploy_new_server.py was    # ★ 실제 서비스. 코드 전송 + systemd 재기동 + 헬스체크
pm2 restart skin1004-prod                  # 리다이렉트 껍데기(172.16.1.250). 리다이렉트 로직 바꿀 때만
```
- `CRAVER_SSH_PW` 환경변수 필요 (계정 `jeffrey`, 비밀번호는 노션 "AI Craver" 페이지)
- **paramiko 는 `sshenv/` 격리 venv 에 있다** — 전역 파이썬에 깔면 별도 실행 중인
  프로세스와 충돌한다. 없으면 만들 것:
  `python -m venv sshenv && ./sshenv/Scripts/python -m pip install paramiko`
  그리고 배포는 `./sshenv/Scripts/python scripts/deploy_new_server.py was`
- ⚠️ **새 패키지를 추가했으면 배포 목록에 드는지 먼저 확인**하라 (`collect()` 를 직접 호출).
  `EXCLUDE_DIRS` 가 디렉토리 **이름**으로 걸러 `app/knowledge_map/` 이 통째로 빠진 적이 있다
- 신규 서버는 git 저장소가 아니라 **SFTP 전송본**이다. git pull 로 갱신되지 않는다.
- **패키지(requirements) 변경 시**엔 휠을 다시 받아 올려야 한다 (런북 참조)
- 배치 크론을 바꿨으면 `deploy_new_server.py app` 도 실행

### ⛔ git 은 배포 관문이 아니다 — 스크립트가 대신 말한다 (2026-09-08 실측)

배포는 git 이 아니라 **작업트리 통째 SFTP 전송**이다. 그래서 *"커밋 안 한 코드가
프로덕션에 있다"* 가 사고가 아니라 **구조적으로 정상 경로**다.

그날 프로덕션은 HEAD 가 아니라 작업트리를 돌고 있었고, 그중 넷은
`git log --all` 이 **한 번도 본 적 없는 파일**이었다:

| | 줄 | |
|---|---|---|
| `app/core/user_directory.py` | 330 | 로그인 경로 |
| `app/core/user_directory_migration.py` | 393 | 로그인 경로 |
| `app/core/calendar_stats.py` | 350 | |
| `scripts/sync_entra_users.py` | 30 | **APP 크론 22:00 진입점** |

⛔ **크론 체인의 세 고리가 전부 git 밖이었다** — 진입점은 30줄 껍데기이고 실제
일은 위 두 모듈이 한다. 옛 진입점 `scripts/sync_ad_users.py` 는 추적되지만
LDAP 직결 옛 코드라 대체재가 못 된다. **AD 동기화 실패는 조용하다**
(2026-08 에 6일간 아무도 몰랐던 그 자리다).

⚠️ **에러도 경고도 없었다.** 사람이 트리·HEAD·프로덕션을 해시로 대조해 보고서야
드러났다. 회귀 2,797줄도 함께 git 밖이라, 코드만 살려도 검증 수단이 함께 사라진다.

**그래서 배포 스크립트가 스스로 말한다** (`app/core/deploy_preflight.untracked_in_payload`):

```
[보존] !! git 이 모르는 소스 1개(330줄)가 이번 전송에 포함됩니다
         app/core/user_directory.py  330줄
       작업트리가 사라지면 이 코드는 프로덕션에만 남습니다. 커밋을 권합니다
       (막지 않습니다 - 전송은 그대로 진행됩니다)
```

- ⛔ **막지 않는다. 적기만 한다.** 더티 트리를 막는 관문을 만들면 그날로 꺼진다 —
  이 트리는 세션 서넛이 상시 공유해 깨끗한 순간이 사실상 없고, `M` 이 100개를
  넘는다. 매번 걸리는 관문은 곧 우회된다 (훅에 대해 이미 적어 둔 그대로)
- ⛔ **전송 목록에서 판정한다.** 경로 규칙을 경고 쪽에 따로 적으면
  `EXCLUDE_PATHS` 가 바뀔 때 **경고만 조용히 낡는다** — `knowledge_map` 이 이름으로
  걸려 `app/knowledge_map/` 이 통째로 빠졌던 그 자리다. 호출부가 `collect()` 결과를
  그대로 넘기고 판정은 그 안에서만 한다
- ⚠️ **조용한 것이 정상이다.** 기준선을 0 으로 만들어 두었다 — 상시 몇 개가 떠 있으면
  이 경고는 태어나자마자 죽는다. `scripts/_*`(일회성 관례)와 `.gitignore` 된 것은
  뺀다. **무시는 결정이지 실수가 아니다**
- ⚠️ **`M` 은 알리지 않지만 "잃을 수 없다" 는 뜻이 아니다.** 잃을 수 없는 것은
  *파일*이고 **미커밋 변경 자체는 잃는다** (HEAD 상태로 돌아간다). 추적 안 된 것과의
  차이는 *"어디에도 없다"* 와 *"직전 상태로 돌아간다"* 이고, **심각도 차이지 무손실이 아니다**
- ⛔ **자가 점검(매일)으로 만들 수 없다** — 신규 서버는 git 저장소가 아니라 SFTP
  전송본이라 서버에서 `git status` 가 성립하지 않는다. 이 경고는 git 이 있는 쪽,
  즉 **배포하는 사람 화면**에서만 뜻이 있다
- ⚠️ 막는 관문(`preflight`)과 섞지 마라. 섞이면 "적기만 한다" 던 것이 언젠가 막는
  쪽으로 흘러가고, 그러면 매번 걸려 우회된다. 회귀가 둘의 분리를 검사한다
- 회귀: `tests/test_deploy_untracked_notice.py` (41문항 — 울릴 때·조용할 때 양방향)

### ⚠️ 배포 전에 무엇이 실려 가는지 확인하라

전송이 트리 통째라 **누가 눌러도 다른 세션 것이 다 함께 나간다.** 2026-09-08 에
한 세션의 배포가 다른 세션의 진행 중이던 인증 이관을 프로덕션에 올렸다 —
결과가 괜찮았던 것이지 확인하고 올린 것이 아니었다.

```
git log --oneline -10        무엇이 담겼나 (남의 커밋도 함께 나간다)
git status --porcelain       미커밋 몇 개가 실려 가나
python -m pytest tests/ --ignore=tests/frontend -q
python scripts/deploy_new_server.py was --dry
```

- ⛔ **`git add <경로>` 로 지목해도 `git commit` 은 인덱스 전체를 담는다.**
  남이 스테이징해 둔 파일이 그대로 딸려 간다 (2026-09-09 실제 발생 — 한 세션의
  구현 2파일이 다른 세션의 커밋에 실려 갔다). **`git diff --cached --stat` 을
  먼저 보거나** `git commit -- <경로>` 로 커밋 자체를 지목할 것
- ⛔ **파일을 새로 만들기 전에 있는지 확인하라.** `cat > tests/...` 로 "새 파일" 을
  썼는데 이미 있던 269줄이 149줄이 되며 **회귀 22문항이 사라졌다** (같은 날).
  남은 8개는 전부 통과했고 사라진 것들은 아무 소리도 내지 않았다
- 💡 **착수 전에 다른 세션에 한 줄 알려라.** 같은 기능을 두 세션이 나란히 구현하는
  일이 실제로 있었다. `ListAgents` 로 상대를 찾고 `SendMessage` 로 보내면 된다

### 상태 확인·롤백

- 전체 점검: `python scripts/verify_migration.py` (SSH·프록시·DB·서비스 한 번에)
- 신규 서버 로그: `journalctl -u ai-craver -n 50 --no-pager` (SSH 접속 후)
- **롤백**: 172.16.1.250 의 `.env` 에서 `MIGRATED_REDIRECT_URL` 줄 삭제 → `pm2 restart skin1004-prod`
  → 기존 서버가 즉시 원래대로 서비스하고 스케줄러도 자동 재개
- 172.16.1.250 은 롤백 대비로 유지 중. **kill / stop / delete 절대 금지**
- `git push jaepilimited master` 는 코드 백업용으로 유지

### 이관 상세

`docs/MIGRATION_AI_CRAVER.md` 가 단일 소스 — 검증 결과·오프라인 구축 절차·컷오버·롤백 전부 여기 있다.

### 아직 172.16.1.250 에 남아 있는 것

- **CRM 서비스** (`:3100`) — 전용 서버로 별도 이관 예정. 이것 때문에 DB_PC 를 끌 수 없다.
- `SKIN1004-Watchdog`, `SKIN1004-PM2-AutoStart`, `SKIN1004-Git-Push-Daily` (예약 작업)
- AD 동기화·지식맵 예약 작업은 APP 서버 크론으로 이관했고 **DB_PC 쪽은 비활성화**했다
  (중복 실행 방지). 롤백 시 `schtasks /change /tn SKIN1004-AD-Sync-Daily /enable` 로 되살릴 것.
- **GCP(34.64.99.179)** 는 2026-06-12부로 AI Agent 서비스 중지 — CRM 만 운영 중

## 서버 관리

- **신규 프로덕션 (실제 서비스)**
  - SSH: `jeffrey@10.1.100.5` / `10.1.150.5` / `10.1.150.105` (172.16.1.250 에서만 접속 허용)
  - 앱 재기동: `sudo systemctl restart ai-craver` (WAS)
  - 로그: `journalctl -u ai-craver -n 50 --no-pager`
  - nginx: `sudo nginx -t && sudo systemctl reload nginx` (Web)
  - DB 접속: WAS 경유 터널링만 가능 (DB 직접 접근 차단)
- **기존 서버 172.16.1.250 (리다이렉트 껍데기 + CRM)**
  - 반영: `pm2 restart skin1004-prod` / 로그: `pm2 logs skin1004-prod --lines 30 --nostream`
  - ⚠️ Windows fork 모드에서 `pm2 reload` 는 고아 프로세스를 만든다(2026-07-06 장애) — 반드시 `restart`
  - `pm2 status` 의 ↺ 가 수십 회 이상이면 포트 점유 고아 의심 (delete → 포트 킬 → start)
  - 서버 켜기(재부팅 후): `pm2 start ecosystem.windows.config.js`
- **로컬 개발** (포트 3001): `pm2 restart skin1004-dev`
- GCP SSH (CRM 확인용): `ssh -i C:/Users/DB_PC/.ssh/gcp_skin1004 skin1004@34.64.99.179`

## BigQuery 데이터 규칙 (SQL 로직 기준)

- **매출** → `SALES_ALL_Backup.Sales1_R` (원화 환산, 항상 이 컬럼)
- **판매수량** → **무조건 `Product.Total_Qty`** (2026-07-15 확정 — SALES_ALL_Backup.Total_Qty는 세트를 1개로 세어 부정확, 수량 답변에 사용 절대 금지. 실제 오답 사고로 규칙 강화)
- **매출+수량 동시 질문** → 매출은 SALES_ALL_Backup, 수량은 Product 서브쿼리로 각각 조회
- `Product` 테이블은 `SALES_ALL_Backup`의 세트 제품(SET에 `+` 연결)을 개별 SKU로 분해한 테이블 (필터 컬럼은 SALES_ALL과 동일: Country, Date, Brand, Mall_Classification 등)
- **신제품 정의 (2026-07-27 확정)** → **첫 판매일(데이터 최초 등장일)로부터 6개월 이하**인 제품
  - SQL 구현: 제품별 `MIN(Date)`로 첫 판매일을 구하고, 기준 시점(보통 오늘)으로부터 6개월 이내인 제품만 필터 — 예: `HAVING MIN(Date) >= DATE_SUB(CURRENT_DATE(), INTERVAL 6 MONTH)`
  - 첫 판매일 판정은 세트 분해된 `Product` 테이블 기준 (SKU 단위 정확성)
- **브랜드 = 스킨천사 · 우마(UM) · 좀비뷰티 · 커먼랩스** (2026-08-05 확정)
  - **주력은 스킨천사·UM**, 좀비뷰티·커먼랩스는 아주 소량

    | 브랜드 | 조회 조건 | 규모(2025~) |
    |---|---|---|
    | 스킨천사 | `Brand IN ('SK','CBT') AND (Line!='ZB' OR Line IS NULL)` | 약 11,112억 |
    | 우마 | `Brand='UM'` | 3,252억 |
    | 좀비뷰티 | `Brand='SK' AND Line='ZB'` | 18.7억 |
    | 커먼랩스 | `Brand='CL'` | 2.9억 |

  - ⛔ **`Brand='CBT'`(659.0억)는 스킨천사 매출이다** (2026-08-05 확정). `Brand` 컬럼에 팀 값이
    잘못 들어간 것 — `Team_NEW` 의 CBT 와 건수·금액이 완전히 일치한다 (1,919,601건).
    브랜드별 집계 시 **SK 와 합산**할 것. 별도 행으로 내면 팀이 브랜드 표에 섞인다
  - **좀비뷰티는 `Brand` 에 없다** — `Brand='SK'` 안에 `Line='ZB'` 로 들어 있어, 브랜드별로 나눌 때
    스킨천사에서 빼지 않으면 이중 계상된다
  - **CBT·JBT·KBT·EAST·WEST·DT·B2B 는 팀** → `Team_NEW` 로 답할 것. 브랜드로 나열 금지
- **팀 = 12개, 공식 팀명은 한글이고 데이터는 코드다** (2026-08-11 확정)

    | 본부 | 소속 팀 (코드) |
    |---|---|
    | 글로벌마케팅본부 | 중국사업팀 `CBT` · 일본사업팀 `JBT` · 한국사업팀 `KBT` · 동남아시아1팀 `EAST1` · 동남아시아2팀 `EAST2` · 서구권마케팅팀 `WEST_MKT` · 서구권이커머스팀 `WEST_Ecomm` |
    | 영업1본부 | 영업1팀 `B2B1` · 영업2팀 `B2B2` |
    | 유통1본부 | 유통1팀 `DT1` |
    | 유통2본부 | 유통2팀 `DT2` |
    | 상품본부 | 브랜드커뮤니케이션팀 `BCM` (플래그십 파트) |

  - 조직도에는 유통 쪽에 리테일_UMMA·리테일1~3 / 뉴비즈1·뉴비즈2·코스트코 같은 이름이
    더 있지만 `Team_NEW` 에는 없다 — 그 단위로 물으면 나눌 수 없다고 안내할 것
    (코스트코·ULTA 거래는 전부 `DT2`)
  - 조직도 팀명의 `GM ` 접두(`GM CBT` 등)는 데이터에 없다 — 떼고 조회
  - **검증된 팀 담당 국가 질문은 검색 문서보다 조직 원본을 우선한다** (2026-08-26 #152)

    | 팀 | 담당 국가 |
    |---|---|
    | 동남아시아1팀 `EAST1` | 인도네시아 · 필리핀 |
    | 동남아시아2팀 `EAST2` | 말레이시아 · 싱가포르 |
    | 서구권마케팅팀 `WEST_MKT` | 미국 |
    | 서구권이커머스팀 `WEST_Ecomm` | 미국 · 호주 |

    - 단일 소스는 `app/core/org_structure.py`. 일반·스트리밍 채팅 모두 라우터 전에
      `answer_team_country_scope()`로 답한다. 이 관문이 없으면 무관한 Notion 문서가 검색돼
      EAST2에 인도네시아를 포함한 실제 오답이 재발한다
    - ⛔ **서구권은 광고가 아니라 매출로 확정했다** (2026-09-02). 광고 테이블로 세면
      노출 지역까지 잡혀 `WEST_Ecomm` 201개국 · `WEST_MKT` 83개국이 나오는데, 그건
      담당 범위가 아니라 **노출 범위**다. 실제로 판 곳은 2～3개국뿐이다
      (2026년 실측: `WEST_Ecomm` 미국 91.2%·호주 8.7%, `WEST_MKT` 미국).
      ⚠️ 이 방법은 **대조군이 보증한다** — EAST1·EAST2 를 매출로 세면 등록값과 정확히 같다
    - ⛔ **한 나라를 두 팀이 맡을 수 있다.** 미국은 `WEST_MKT`·`WEST_Ecomm` 둘 다다 —
      서구권은 동남아처럼 국가로 나뉜 게 아니라 **기능(마케팅·이커머스)으로** 나뉜다.
      국가→팀 역방향 답변은 **둘 다 적고 왜 둘인지 밝힌다** (하나만 고르면 나머지 팀이
      조용히 사라지고, 이유가 없으면 데이터 오류로 읽힌다)
    - ⚠️ `WEST_MKT` 는 매출 대부분이 국가가 안 붙는 **글로벌 B2B 플랫폼**(`Country='글로벌_플랫폼'`,
      2026년 77.7%)이라 국가로 나누면 미국만 남는다. 답변이 그 사실을 함께 적는다 —
      ⛔ **거기에 비율을 쓰지 마라**, 해마다 변하고 변하면 조용히 틀린 문장이 된다
    - ⛔ 등록되지 않은 팀의 담당 국가는 추정하지 않는다. 매출·광고·일정 같은 데이터
      질문은 이 정적 관문이 가로채지 않고 원래 데이터 경로로 보낸다
    - ⛔ **권역명(`서구권`·`동남아`)으로 물으면 되묻는다** (2026-09-02 사용자 지시:
      *"서구권, 동남아 이렇게 애매하게 말하면 되물어야지"*). 팀 이름이 아니라서
      관문을 못 넘는데, **그냥 통과시키면 검색 경로로 새서 무관한 Notion 문서가 나온다** —
      이 관문이 애초에 막으려던 바로 그 실패다. 모르면 지어내지도, 흘리지도 말고 되묻는다
      - ⚠️ 되묻되 **후보를 담당 국가까지 붙여서** 준다. "어느 팀인가요?" 만 던지면
        사용자가 팀 이름을 몰라 한 턴을 더 버린다
      - ⚠️ 긴 낱말부터 판정한다 — `동남아` 가 `동남아시아` 안에 들어 있어, 짧은 쪽이
        먼저 맞으면 **사용자가 쓰지도 않은 낱말로** 되묻는다
    - ⚠️ 조사는 받침으로 고른다 (`_josa`·`_join_ko`) — `미국와 호주`·`미국은(는)` 이
      나가면 그 한 줄로 답 전체가 기계 티가 난다. ⚠️ `**팀이름(CODE)**` 처럼 장식된
      문자열은 마지막 글자가 한글이 아니라 받침 판정이 빗나간다 — 맨 이름을 따로 넘길 것

  - **양방향 혼용을 코드로 보장한다** — 프롬프트 지시만으로는 확률적이라 국가 리터럴과 같은 방식을 썼다
    - 입력: `_localize_team_literals()` 가 SQL 의 `Team_NEW='영업1팀'` → `'B2B1'` 로 교정 (생성·재생성 4경로 전부)
    - 출력: `_relabel_team_values()` 가 결과 행의 코드를 `영업1팀(B2B1)` 로 치환 → 표·차트 라벨·인사이트가 한 번에 맞는다
    - 새 팀이 생기면 `TEAM_CODE2KR` 한 곳만 고치면 프롬프트 3곳(`TEAM_DISPLAY_RULE`)까지 따라온다
  - ⛔ **`기타`·`OP` 는 정식 팀이 아니다** — 팀별 집계에서 제외한다 (합계 0 수준 잔여값)
  - `integrated_ad.team` 은 **매출 테이블과 같은 코드 8종**이다 (2026-08-13 실측 — 예전에
    "`WEST` 로 통합돼 있다"고 적혀 있었으나 사실이 아니다. `WEST_MKT` 15,970건 ·
    `WEST_Ecomm` 15,324건으로 나뉘어 있다). 없는 팀은 B2B1·B2B2·DT1·DT2·BCM — 광고를 안 돌린다
    - ⚠️ `기타` 가 11,776건으로 작지 않다. 매출 쪽 `기타`(잔여값)와 달리 실제 집행분이므로
      **팀별로 나눌 때 빼먹은 만큼을 밝힐 것**
    - ⚠️ `CBT` 는 96건뿐이다 (2025-01~). 중국 광고를 이 테이블로 논하지 말 것
  - ⚠️ `Product` 테이블에만 있는 `DD`(356건)는 정체 미확인
- **원가·FOC·할인 집계 계약** (2026-08-12 실측 검증 — 어기면 이중계상)
  - `FOC_or_Not='O'`(무상 출고) → `Sales1_R=0`, `FOC = Production_Cost2`
  - `FOC_or_Not='X'`(유상 판매) → `FOC=0`
  - ⛔ **`SUM(Production_Cost2)` 는 FOC 원가를 이미 포함한다.** `+ SUM(FOC)` 하면 FOC 를 두 번 센다.
    `CASE WHEN FOC_or_Not='X'/'O'` 로 **분리 집계**할 것. "원가"만 물으면 유상원가로 답한다
  - **직접비 = 유상원가 + FOC원가 + `Discount_Coupon`**, **매출총이익 = 매출 − 직접비**.
    수수료·고정비가 빠져 있으니 **공헌이익도 영업이익도 아니다** — 그렇게 부르지 말 것
  - `Discount_Coupon` 은 **B2C 전용** (B2B 는 전 구간 0원)
  - ⚠️ **`Service_Fee` 는 음수로 깨져 있다** (SK 19개월 합계 −26.2억, 음수 188만 행).
    수수료·물류비 전체를 비용 분석에서 제외한다 → **이 데이터로 진짜 수익성은 낼 수 없다**
  - ⚠️ **Tiktok 4개 채널은 `Discount_Coupon` 이 전 구간 0원** (12~19개월, 할인 행 0건).
    채널 할인율 비교에서 제외하고 빠진 매출 규모를 밝힐 것
  - ⚠️ **원가 미적재 브랜드**: `UM` 99.9% · `CBT` 99.6% 가 원가 0원이다 (SK 는 0.7%).
    **원가·수익성 분석은 `Brand='SK'` 한정**. README 에는 CBT 만 적혀 있었는데 UM 이 더 크다
- ⛔ **"적재 중" 은 실제로 쓰는 중일 때만 답변에 적는다** (2026-08-31 · 2026-09-02)
  - 8/30 KBT 광고비를 물었더니 **NaverGFA·NaverSearch 가 통째로 빠지고** Google 이
    447,791원으로 나갔다. 하루 뒤 실측: Google **3,863,561원(8.6배)** · NaverGFA 722,011원 ·
    NaverSearch 665,961원. **에러도 경고도 없었다** — 그 시각엔 아직 채워지는 중이었다
  - 같은 사용자가 30분 사이 같은 질문에 `8/26` → `8/30` 으로 갈린 답을 받았다
  - ⛔ **점검 감지는 이걸 못 잡는다.** `maintenance_auto_detect_loop` 은 행이 **줄어드는 것**
    (truncate)만 보고 append 는 "조회해도 안전" 으로 넘긴다. 전체 추이라면 맞지만
    **날짜를 지목한 질문에는 틀리다**
  - **공시는 두 겹이고 한 번에 하나만 붙는다** (`safety.data_update_notice_for_sql`):

    | 우선 | 무엇을 보나 | 문구 |
    |---|---|---|
    | 1 | 점검 감지(truncate) | 데이터 업데이트 중 |
    | 2 | **수정 시각이 폴링 사이에 앞으로 갔다** (지금 쓰는 중) | 지금 적재 중입니다 |

    - ⛔ **날짜만 보고 하는 공시는 없앴다** (2026-09-02 사용자 지시: *"이거는
      안내하지말고 데이터가 업데이트 중일때만 안내해."*). 3번으로 "질문이 최근 2일을
      포함하면" 무조건 붙이던 줄(`loading_edge_notice_for_sql`)이 있었는데, 그건 적재
      상태를 **본 것이 아니라 날짜로 추측한 것**이라 적재가 진작 끝난 뒤에도 어제·오늘을
      물을 때마다 붙었다. 매번 뜨는 경고는 곧 아무도 안 읽고, 그러면 **진짜 적재 중**일
      때의 1·2번까지 함께 무시당한다. 함수·문구를 통째로 걷어냈고 회귀가 부활을 막는다

    - 2번이 사용자 지시의 핵심이다 (2026-08-31): *"적재중이면 답변에 적재중이라고
      표기해야함. 그래야 사람들이 이게 오답이 아니구나라고 판단함."* 점검 감지만으로는
      거의 못 말한다 — 그건 행이 **줄어드는 것**만 보는데 실제 사고는 append 중이었다
    - ⚠️ 조회를 더 하지 않는다 — 감시 루프가 60초마다 읽어 둔 값과 SQL 의 날짜 리터럴만 본다
    - ⛔ **경과 초를 그대로 저장하지 마라.** 루프가 멈추면 값이 얼어붙어 **영원히
      "방금 적재됨"** 이 된다. `modified_ts`(절대 시각)로 두면 경과가 자연히 커져
      공시가 스스로 꺼진다 (안전한 쪽으로 실패한다)
    - ⛔ **기록은 판정보다 먼저, 조건 없이 한다.** 점검 판정 안에 두면 append 때는
      기록이 안 남아 공시도 못 한다 (회귀가 소스 순서를 검사한다)
    - ⛔ **"최근에 수정됨" 은 "지금 적재 중" 이 아니다** (2026-08-31 사용자 제보:
      *"지금 적재중입니다는 맞는 표현이 아님. 이미 업데이트가 되어있음"*). 처음엔
      수정 시각이 15분 안이면 무조건 붙였는데, 그 조건은 적재가 **끝난 뒤에도**
      15분 내내 참이라 **11분 전에 끝난 적재**에 공시가 붙었다. 쓰는 중이면
      폴링(60초)마다 수정 시각이 **앞으로 간다** — `moved_ts` 로 그 움직임을 보고
      3분(폴링 3회) 안에 움직였을 때만 말한다. 끝나면 값이 굳어 스스로 꺼진다
    - ⚠️ 잡음 흡수(`_LOAD_MOVE_EPSILON_SECONDS` 10초)를 0으로 두지 마라 — 경과 초는
      정수로 오고 왕복 지연이 매번 달라, 가만히 있는 테이블도 1~2초씩 흔들린다
    - ⚠️ 임계 15분(`_ACTIVE_LOAD_SECONDS`)은 이제 바깥 울타리다. 넓히지 마라 —
      적재가 끝난 뒤에도 계속 붙으면 매번 뜨는 경고가 된다. 실측: 이 테이블의
      `last_modified` 경과는 단조 증가한다(1376초 → 1418초).
      *"상시 적재라 늘 0초 전"* 이라던 옛 주석은 사실이 아니다
  - ⛔ **System Status 의 `업데이트 중` 은 이걸 못 잡는다** — 대량 truncate 전용이다.
    실측: 광고 테이블 142,901행에서 KakaoMoments 816행(0.57%)이 빠졌는데 임계는
    **7,145행(5%)** 이다. 화면이 `정상` 인 것을 "데이터가 온전하다" 로 읽으면 안 된다
  - 0행일 때는 `zero_row.diagnose()` 가 그 조건 조합의 **실제 적재 마지막 날짜**를 재서
    "집행이 없었던 것이 아니라 아직 안 들어온 것" 이라고 구분해 준다
  - ⛔ **매체가 통째로 사라질 수 있다** — `KakaoMoments` 가 오전엔 있었고(8/27 최신,
    2026-08 594,843원) 오후엔 매체 19종 어디에도 없었다. 셀라는 두 번 다 **그 시점의
    사실**을 답했고, 사람이 두 답을 나란히 놓기 전까지 아무도 몰랐다 (에러가 없다).
    - **원인 확정 (2026-08-31 데이터팀)**: 이 테이블은 **하루 두 번** 적재되는데
      **오후 적재가 오류로 실패**해 빠졌다. 재실행으로 복구됨
      (KakaoMoments 816행 · 2023-08-12~2026-08-27 · 5,201만원, 매체 19→20종)
    - 감시는 `app/core/ad_media_watch.py` 가 한다: **하루 두 번(04:20·18:20)** 매체 목록
      스냅샷(`ad_media_snapshot_daily`) → 자가 점검 `ad_media_missing` 이 실패로 올린다.
      한 번만 뜨면 오후 적재 실패를 **다음 날 아침에야** 안다 — 실제 사고가 그것이었다
    - ⛔ **한 번 안 보인다고 유실이 아니다.** 적재가 하루 두 번이라 스냅샷이 적재 도중에
      찍힐 수 있다 — **두 번 연속** 없을 때만 확정한다 (한 번은 `suspected`, 실패 아님)
    - ⚠️ 확정 판정은 **`older` 에 있었고 `prev`·`cur` 둘 다에 없는 것**이다.
      `prev` 만 순회하면 두 번째 스냅샷에서 그 매체가 `prev` 에 없어 **루프가 닿지 않고
      확정이 영영 안 된다** — 처음에 그렇게 짜서 "두 번 연속 없음" 이 정상으로 통과했다
    - ⚠️ `schema_watch` 는 테이블·컬럼을, 점검 감지는 **전체 행 수**를 본다.
      작은 매체가 빠지는 것은 **둘 다 못 잡는다** (594,843원짜리 소액 집행이었다)
    - ⛔ **"최신일이 안 움직였다" 는 감시하지 않는다.** 집행이 없던 날은 정상이라
      매일 대부분이 걸린다 — 처음에 넣어 봤더니 19종 중 19종이 떴다
    - ⚠️ 스냅샷은 **적재가 끝난 뒤**에 떠야 한다. 적재 중에 뜨면 정상 재적재를 유실로 읽는다
- **광고 데이터는 매출과 나란히 놓을 수 있다 — 축 4개뿐이다** (2026-08-13 실측)
  - 테이블: `marketing_analysis.integrated_ad` — 광고비·노출·클릭·전환·전환매출
  - ✅ **국가는 매출과 같은 한글 국가명**이다 (148개 일치). 컬럼명만 `country` 로 다르다.
    팀도 같은 코드 8종 → **국가·팀·월·분기 네 축에서만 매출과 조인된다** (ROAS 가 이렇게 나온다)
  - ⛔ **채널·브랜드·제품 컬럼이 없다.** "채널별 ROAS"·"제품별 광고효율"은 이 데이터로 답할 수 없다.
    LLM 이 있는 줄 알고 만들어내지 않도록 지표 설명에 못 박아 뒀다
  - ⛔ **`conversion_value_krw`(전환매출)는 광고 플랫폼이 스스로 집계한 값**이다. `Sales1_R` 과
    다르고, 플랫폼마다 추적 방식이 달라 **플랫폼끼리 견주는 것도 정확하지 않다**
  - ⛔ **ROAS 0 을 "최악"이라고 쓰지 마라.** Meta·Tiktok 은 전환 추적이 없으면 0 으로 들어온다 —
    매출이 없다는 뜻이 아니다. 원가 미적재(UM·CBT 99%)와 같은 종류의 함정이다.
    보고서 `ratio` 블록은 분자 0 인 행을 **순위에서 빼고 그 사실을 본문에 적는다**
  - ⚠️ **2026-09 ~ 12 에 각 4행씩 미래 데이터가 있다.** 기간을 안 막고 전체 집계하면 섞인다
- **프로모션 캘린더는 실적이 아니라 실행 일정이다** (2026-08-11 통합)
  - 테이블: `promotion_calendar.promotion` — 어느 팀이 어느 몰에서 언제 프로모션을 하는지
  - **계획 데이터라 미래 날짜가 정상**이다. 매출 테이블과 달리 "아직 안 일어난 행"이 다수
  - ⛔ **이 테이블만 값 체계가 다르다.** 다른 테이블 습관대로 쓰면 조용히 0건이 난다

    | | 프로모션 | 다른 테이블 |
    |---|---|---|
    | 팀 | `team_id = 'west-mkt'` (소문자·하이픈) | `Team_NEW = 'WEST_MKT'` |
    | 국가 | `country_code = 'ID'` (2글자 ISO) | `Country = '인도네시아'` |

    - 입력은 `_localize_promotion_literals()` 가 한글 팀명·국가명을 이 체계로 교정하고,
      출력은 `_relabel_team_values()` 가 `동남아시아1팀(east1)` 로 되돌린다 (팀 규칙과 같은 계열)
  - **`WHERE NOT is_deleted` 를 항상 붙일 것** — 약 19% 가 삭제분이다
  - 기간은 **겹침**으로 판정한다 (`start_date <= 말일 AND end_date >= 1일`).
    시작일만 보면 달을 걸쳐 진행 중인 행사를 통째로 놓친다
  - `channel` 표기가 혼재한다 (아마존/Amazon, 올영/올리브영/Oliveyoung) — `LIKE` 로 변형을 함께 잡을 것
  - ⚠️ 원본은 구글 시트이고 `synced_at` 컬럼이 있는데 **동기화 주체가 확인되지 않았다.**
    자동 적재가 아니면 데이터가 조용히 낡는다 — 확인되면 `EXPECTED_JOBS` 에 신선도 감시를 걸 것
  - ⛔ **보유 구간이 좁다 — "기록 없음"을 "행사 없음"으로 읽으면 안 된다** (2026-08-13 실측)

    | 구간 | 상태 |
    |---|---|
    | 2025 전체 | **0건** |
    | 2026-02 · 03 | 각 1건 (사실상 비어 있음) |
    | 2026-04 ~ 08 | 31~60건 (여기부터 촘촘) |

    - 총 261건 전부 2026년이고 `status` 는 대부분 `schedule` 이다 — **앞으로 할 일을 적는
      시트**라 지난 행사는 소급 입력되지 않는다
    - 실제로 일본 매출 최대 달인 **2026-03(55.1억)의 메가와리가 캘린더에 없다**.
      이걸 "행사 없음"으로 세면 성분에서 '미상'을 '미포함'으로 썼던 오답이 그대로 재현된다
    - 그래서 `promotion` 블록은 **기록 없는 달을 분모에서 빼지 않고**, 비교 기준을
      "행사 없는 달"이 아니라 **구간 전체 평균**으로 둔다. 보유 구간도 본문에 공시한다
- **제품 전성분은 사내 스프레드시트에 있다** (BigQuery 에는 없다 — 2026-08-06 구축)
  - 출처: 시트 `11gX_Gg7...` → 탭 `01. 제품정보_내수통합용(품목기준)`, 7행부터.
    **AG=전성분(KR, 2025-07 이후 최신) / AH=전성분(EN)**, G열이 BigQuery 매칭 키.
    AC~AF 는 구버전이니 쓰지 말 것
  - 적재: `app/core/ingredients.py` → `product_ingredients` / `product_ingredient_map`.
    매일 04:00 `ingredient_sync_daily` 자동 갱신 (자가 점검 EXPECTED_JOBS 등록됨).
    수동은 `python scripts/sync_product_ingredients.py [--dry-run]`
  - 커버리지: BigQuery 243종 중 **115종 매칭**(종수 47%) — **판매수량 기준 89.7%**.
    미매칭은 대부분 Sachet·기획세트
  - ⛔ **"성분 미상"과 "성분 미포함"을 절대 섞지 마라.** 시트에 없는 제품은 성분을 *모르는* 것이지
    *안 들어간* 것이 아니다. 뭉개면 원래 오답(제품명 `LIKE '%RETINOL%'` 매칭으로 나이아신아마이드가
    든 제품이 "미포함 1위")이 그대로 재현된다. 미매칭은 결과에서 빼고 커버리지를 답변에 명시한다
  - ⛔ **LLM 에 성분 SQL 을 맡기지 마라** — 다시 제품명 문자열 매칭으로 흘러간다.
    `orchestrator._handle_ingredient_query()` 가 제품 목록을 먼저 확정하고 그 목록으로만 집계한다
  - 검증법: 같은 성분의 포함/미포함 목록 **교집합이 0** 이어야 한다
  - 개별 제품 전성분 설명은 `@@BP`(제품 Q&A) 경로가 계속 담당한다
- **대륙 = `Continent1` 기본** (2026-08-04 오답 사고로 규칙화)
  - `Continent2` 에는 **'유럽'·'아시아'·'동유럽' 값이 아예 없다.** 광역 대륙을 거기서 찾으면 0건이 난다
  - 광역 → `Continent1` / 세부 권역 → `Continent2`
  - 예외는 "동남아"뿐 — `Continent1` 에 없어서 `Continent2 = '동남아시아'` 를 쓴다
  - ⛔ **여기에 값을 나열하지 마라. 프롬프트에도 마찬가지다.** 실제 목록은
    `{{VALUES:Continent1}}`·`{{VALUES:Continent2}}` 로 매일 실측 주입된다
    (`app/core/value_lists.py`). 손으로 적으면 반드시 낡고, 낡으면 **에러가 아니라 0건**이다
  - ⛔ **`남미`·`중미` 는 `Continent1` 에 없다** — `중남미` 하나로 통합됐다 (2026-08-18).
    잘게 보려면 `Continent2` 의 `남아메리카`·`중앙아메리카` 다. `북아프리카` 도 없다
    (`Continent2` 는 `아프리카` 하나다)
  - 2026-08-24 실제 오답: 프롬프트에 `{{VALUES:Continent1}}`(중남미)와 **손으로 적은 옛 목록
    (남미·중미)이 함께** 있었다. LLM 이 손 목록을 믿고 0건을 낸 뒤 **그 목록을 근거로 인용하며**
    "값은 정상 존재하므로 데이터가 없는 것" 이라고 단정했다 — 조회도 설명도 틀렸다.
    재발 방지: 정적 검사 `static_value_list_dupes` + 후처리 `_localize_continent_literals()`
    (⚠️ 후처리는 컬럼을 통째로 바꾸지 않는다 — `IN ('남미','유럽')` 에서 컬럼을 바꾸면
    `유럽` 이 `Continent2` 에 없어 그 절반이 조용히 0건이 된다)
- **기간은 결정적이어야 한다 — 달을 지목하면 그 달 전체다** (2026-08-25, 붐따 #143·#144)
  - 같은 질문을 3분 간격으로 두 번 했더니 총량이 **1,805 / 11,865** 로 갈렸다. SQL 두 개의
    차이는 딱 하나, 기간의 끝이었다 (`08-24` vs `08-31`). 제품 필터는 오히려 앞쪽이 더
    넓었는데 결과는 1/6 이었다. **자른 사실은 답변 어디에도 없었다**
  - `_normalize_named_period()` 가 BETWEEN 상한을 되돌린다. 조건은 좁다: 질문이 달을
    지목했고(`8월`) 범위를 직접 말하지 않았을 것(부터·까지·~), 하한이 그 달 1일이고
    상한이 같은 달이며 말일보다 앞일 것
  - ⚠️ "8월 1일부터 8월 24일까지" 는 **사용자가 스스로 자른 것**이라 건드리지 않는다.
    늘리면 묻지 않은 것을 답하게 된다. "최근 3개월" 도 달을 지목한 것이 아니다
- ⛔ **오늘 이후 행을 잘라내지 마라 — 미래 날짜는 포함한다** (2026-08-25 사용자 확정)
  - 발주·예정 물량은 **미래 날짜가 정상 데이터**이고 그것이 사용자 대시보드(product
    metrics)의 기준이다. 예전 규칙("`Date <= CURRENT_DATETIME()` 을 붙여 미래를 제외")은
    "잘못 적재된 행" 을 막으려던 것인데, 그 행들은 잘못 적재된 게 아니었다
  - ⚠️ **말없이 포함하는 것은 말없이 자르던 것과 같은 종류의 조용한 오답이다.** 매출
    추이에 아직 일어나지 않은 달이 섞여도 아무도 모른다. `_future_period_note()` 가
    조회 상한이 오늘 이후면 답변 끝에 한 줄을 붙인다 — **공시는 코드가 한다**
  - ⚠️ 이 변경은 매출·광고 질문에도 퍼진다. 골든셋은 답변 문자열만 보므로 값이 커진 것을
    못 잡는다 — 수치가 튀면 그 테이블에 미래 행이 있다는 뜻이다
  - ⛔ **규칙을 뒤집었으면 그것을 강제하던 코드도 같이 걷어내라** (2026-08-27 실측).
    위 결정은 내려졌는데 **강제 관문(`_requires_current_date_cap`)과 그것을 지키던
    회귀 테스트가 그대로 남아** 이틀간 서로 반대되는 규칙 두 벌이 돌았다. 게다가 그
    관문은 판정부터 깨져 있어서 `Date <= '2026-08-25 23:59:59'`(시간 붙음)과
    `date <= DATE('2026-08-27')`(DATE() 로 감쌈)를 **상한이 있는데도 없다고** 읽었다 —
    프로덕션 14일 발동 10회 중 **9회가 오탐**, 요청 **5건이 사망**("SQL이 생성되지
    않았습니다"). 한 사용자는 같은 질문을 5분간 5번 던져 5번 다 같은 에러를 받았다.
    지금은 관문을 걷어내고 `_future_period_note()` 가 **막지 않고 공시**한다
    (공시 정규식은 위 세 가지 표기를 전부 읽는다 — 하나라도 못 읽으면 조용한 오답이다)
  - ⛔ **관문이 "요청 사망" 으로 끝나면 트리거보다 그 결말을 먼저 의심하라.**
    같은 모양이 하나 더 있었다 — 기간별 순위 관문(`_requires_partitioned_period_ranking`)이
    14일간 요청 3건을 죽였는데 죽은 3건이 **전부 잘릴 수가 없는 SQL** 이었다
    (`GROUP BY` 없는 행 목록 / 축이 기간 하나뿐인 월별 추이). 트리거가 대화 맥락에서
    `주요`·`상위` 를 물려받는 것은 **의도된 설계**라 좁히면 원래 사고가 되살아난다.
    그래서 `_period_rank_truncation_possible()` 이 **실제로 잘릴 때만** 막는다:
    전역 `LIMIT` + `GROUP BY` 축이 둘 이상(기간 × 항목)일 때. 모르면(`GROUP BY ALL`)
    막는 쪽으로 기운다 — 조용히 잘린 표가 답을 못 주는 것보다 나쁘다
  - ⚠️ **관문의 효과를 로그 건수로 논하지 마라.** 성공 로그는 INFO 라 프로덕션에서
    통째로 버려진다 (14일간 앱 INFO 0건). "성공 0회" 는 없었다는 뜻이 아니라
    **안 보인다**는 뜻이다 — 거부당한 SQL 원문을 직접 읽어야 오탐률이 나온다
- **대륙 값이 반대 컬럼에 걸리면 코드가 바로잡는다** (2026-08-25, 붐따 #120)
  - "유럽" 이 `Continent2` 에 걸려 0건이 났고, **답변은 "대륙 필드가 세분화돼 있어서" 라고
    설명했다 — 사실이 아니다.** `Continent1` 에는 '유럽' 이 있다
  - `_fix_continent_column()` 이 **어느 한쪽에만 있는 값**일 때만 컬럼을 옮긴다. 값은
    `value_lists.values()` 실측 목록으로 판정한다 (손으로 적지 않는다)
  - ⚠️ 양쪽에 다 있는 값(북미·중동·CIS)이나 섞인 조건은 건드리지 않는다 — 통째로 옮기면
    절반이 조용히 0건이 되는 그 함정이다
  - 💡 같은 규칙이 반대 방향도 고친다: `Continent1='동남아시아'` → `Continent2`
- ⛔ **내부 테이블 경로를 답변 본문에 쓰지 마라** (2026-08-25, 붐따 #147 — 보안)
  - "프로모션 캘린더 경로 알려줘" 에 `프로젝트.데이터셋.테이블` 을 표로 내놨다.
    프롬프트에 이미 "테이블명·프로젝트 ID 노출 금지" 가 있었는데도 샜다 —
    **프롬프트는 확률이고 보증은 코드다** (FI 방어선과 같은 사상)
  - `_mask_internal_paths()` 가 본문을 가린다. 비스트리밍 1 + 스트리밍 2 경로 전부
  - ⚠️ `<details>실행된 쿼리</details>` 와 코드 펜스는 **건드리지 않는다** — 근거다
  - ⚠️ 스트리밍은 경로가 청크 경계에서 쪼개진다. 줄 단위로 버퍼링해 판정한다
  - ⚠️ 마스킹 대상은 **실재하는 데이터셋·프로젝트 이름**으로 판정한다. 생김새만으로 자르면
    `3.14.15`·`v1.2.3`·`2026.08.25` 같은 멀쩡한 문장이 뭉개진다
- **`GROUP BY` 없는 집계(`SUM`/`COUNT`)는 0건이어도 NULL 한 행을 돌려준다.** 빈 결과 판정을
  `if not results:` 로만 하면 이 케이스를 놓쳐 LLM이 원인을 지어낸다. 전 컬럼 NULL 단일 행은
  빈 결과로 정규화할 것 (`format_answer` 에 구현됨)

### `@@` 데이터소스 — 서버가 단일 소스다 (2026-08-13)

- **프론트는 `/api/datasources` 로 목록을 받는다.** `chat.js` 에 키를 하드코딩하지 마라.
  - ⛔ 예전엔 같은 목록을 프론트가 따로 갖고 있었다. 서버만 고치면 조용히 어긋났고,
    실제로 두 가지가 새 있었다: `@@Google Workspace` 가 질문에 "Workspace" 를 남겼고,
    `초상권` 은 프론트 라우트 맵에 없어 `bigquery` 로 잘못 떨어졌다(서버는 `model_rights`)
  - `chat.js` 에 남는 것은 **표현**뿐이다 — 그룹 순서·이모지·링크(`GROUP_META`/`GROUP_BY_NAME`)
  - ⚠️ **서버에 새 그룹을 만들면 `GROUP_BY_NAME` 에도 넣어야 한다.** 안 넣으면 그 그룹의
    소스가 **화면에서 통째로 사라진다** (에러 없이) — `static_at_sources` 가 이걸 잡는다
  - 목록을 못 받으면 마지막 성공분(localStorage)을 쓴다. **하드코딩 폴백은 두지 않는다** —
    그게 바로 없애려던 두 번째 사본이다

## 새 데이터소스(테이블)를 붙일 때 건드릴 7곳 (+2)

하나라도 빠지면 **에러가 아니라 반쪽만 동작**해서 발견이 늦다 (2026-08-11 프로모션 통합에서 정리).

1. `config.allowed_tables` — 전역 화이트리스트.
   ⚠️ **빠지면 `@@` 로는 되는데 일반 질문만 "허용되지 않은 테이블" 로 막힌다.** `@@` 로만 테스트하면 못 잡는다
2. `sql_agent._source_table_map` — `@@` 소스 → 테이블 매핑
3. `sql_agent.MARKETING_TABLES` — 키워드 기반 lazy 스키마 로드
4. `prompts/sql_generator.txt` — 테이블 섹션.
   ⚠️ **FI 섹션 앞에 배치할 것.** 뒤에 두면 FI 마스킹 정규식에 같이 잘려 나간다
5. `safety._mkt_tables` + `_MONITORED_TABLES` — System Status 노출 + 신선도 감시
6. `orchestrator` 의 `@@` 엔트리 (별칭·라우트·그룹)
7. `chat.js` — `@@` 목록·라우트 맵·칩 아이콘 (⛔ 캐시 번호는 손대지 마라 — 자동이다)
   ⚠️ **서버에 새 그룹을 만들었으면 `GROUP_BY_NAME` 에도 넣어야 한다** — 안 넣으면
   그 그룹 소스가 **화면에서 통째로 사라진다**(에러 없이). `static_at_sources` 가 잡는다
8. `schema_watch.WATCHED_DATASETS` — 새 **데이터셋**이면 컬럼·테이블 변경 감시 밖이다
   (2026-09-03 수출 물류 연동에서 추가. 데이터셋이 이미 있는 테이블은 해당 없음)
9. `value_lists.REGISTRY` — 값 체계가 다른 컬럼은 목록을 **손으로 적지 말고** 실측 주입한다.
   ⚠️ 고카디널리티(포워더 390종·거래처 536곳)는 넣지 마라 — 상한을 넘으면 목록이 통째로 빠진다.
   ⚠️ 배포 직후에는 캐시가 비어 있다 (`value_lists_daily` 03:50) — 즉시 채우려면 `refresh()`

- 값 체계가 다른 컬럼이 있으면 **처음부터 `_localize_*_literals()` 를 만들어라.** 프롬프트 지시는 확률을 높일 뿐이고,
  보증은 후처리가 한다 (국가 → 팀 → 프로모션 모두 같은 방식)
- 검증은 **`@@` 지정과 일반 질문 양쪽으로** 한다. 라우팅이 갈리는 지점이 서로 다르다
- ⚠️ **어휘 충돌을 확인할 것** — '일정·스케줄·캘린더' 는 Google Workspace 확신 키워드라
  프로모션 질문이 개인 캘린더로 샜다. 대응은 키워드 추가가 아니라 **확신 플래그 조건을 좁히고
  라우터 프롬프트에 구분 기준을 넣는 것**. 반대 방향("내 이번주 일정" → GWS)도 골든셋으로 함께 감시

## 채팅 답변 수치 검증 — 2026-08-13

- **파일**: `app/core/answer_check.py` · 훅: `sql_agent._number_check_notice()` —
  **스트리밍(`run_sql_agent_stream`)·비스트리밍(`format_answer`) 양쪽에서 부른다**
- **왜**: 보고서는 "문장 속 수치가 조회 결과에 없으면 버린다"를 이미 했는데 **채팅에는
  없었다.** 가장 위험한 실패(그럴듯한데 틀린 숫자)가 가장 넓은 경로에서 무방비였다.
  실제로 같은 질문이 경로에 따라 245.5억 / 256.5억으로 갈렸다 (LLM 이 브랜드 필터를
  스스로 붙였다)
- ⛔ **만들어 놓고 실사용 경로에 배선하지 않았다** (2026-08-31 발견). 호출부가
  `format_answer` **하나뿐**이었는데 채팅은 스트리밍으로 나간다(`chat.js` 가
  `stream: true`) — 그래서 **계측조차 실트래픽에서 한 번도 돌지 않았고**,
  "미검증률 0%" 는 아무도 안 쓰는 경로에서 잰 값이었다. 그 사이 실제 사고가 났다:
  8,287만원짜리 3개월 매출을 표에 **정확히 1,000배**로 적고 요약에 "약 828.7억원"
  이라고 썼다 (사용자: *"억대가 아닌데 억대로 나옴"*). 같은 답변 안의 코드 계산
  합계표는 8,403만원이라고 맞게 적혀 있었다. 돌려 보면 미검증률 **91.7%** 로 걸린다
- ⛔ **채팅에서도 문장을 버리지는 않는다.** 채팅 수치는 상당수가 파생값(비중·증감률·
  평균·차이)이라 보고서식 폐기는 과하다. 대신 **조회 원본 표를 답변 끝에 붙여**
  대조하게 한다 ("본문과 다르면 아래 값이 맞습니다"). 로그는 그대로 남는다
- ⚠️ **보여주는 것은 `significant` 뿐이다** — 금액 단위를 달았거나(`828.7억원`)
  1만 이상인 미검증 수치. 반올림된 퍼센트까지 들고 나오면 매번 뜨는 경고가 되고,
  그러면 아무도 안 읽는다
- ⛔ **LLM 에 넘기는 수치는 화면에 찍힐 정밀도로 미리 줄인다** (`_display_round`,
  `_fast_fmt_cell` 과 같은 규칙). `33399393.5798` 을 그대로 주면 소수점 네 자리가
  자릿수 구분의 미끼가 된다 — 위 1,000배 사고가 그것이다. 원 단위 소수점은 뜻이 없다
- ⛔ **억·만 환산도 LLM 에게 시키지 마라 — 산술이고, LLM 의 산술은 확률이다**
  (2026-08-31 같은 사고 2회차). 위를 고친 뒤 표는 맞게 나왔는데 **요약 문장만**
  `82,871,719원` 을 "약 828.7억원"(10만으로 나눔)이라고 썼다. 1억 미만이라 애초에
  '억'을 쓰면 안 되는 값이었다. 사용자: *"표와 시각화는 값이 맞는데 이부분이 안맞음"*
  - 프롬프트에 **코드가 환산한 문자열**을 미리 준다 (`_amount_note`) — 할 일이 복사가 된다
  - 보증은 `answer_check.repair_money_scale()` 이 한다. **배율만 틀린 표기를 되돌린다**:
    ① 쓰인 그대로는 조회 결과에 없고 ② 다른 배율로 읽으면 있고 ③ 그 값이 하나로
    확정될 때만. 교정 허용오차는 검증(2%)보다 엄격한 **0.5%** 이고, 2등 후보가 3배
    이상 멀어야 한다 — **고쳐 쓰는 것은 되돌릴 수 없기 때문**이다
    - ⚠️ "하나만 걸릴 것"으로 두면 안 고쳐진다. 828.7 은 허용오차 안에서
      82,871,719(0.002%)와 84,034,602(1.4%) **둘 다**에 걸렸다 — 가장 가까운 것을 고른다
    - ⚠️ `'원'` 없는 `만` 은 건드리지 않는다 (`3만 개` 는 수량이다)
    - ⛔ **교정 후보를 조회 결과 전체에서 뽑지 마라 — 맞는 금액이 파괴된다**
      (2026-09-04 프로덕션 실측). `104,694,886 KRW` 에 LLM 이 **맞게** 붙인
      "약 1.0억원" 이 **"약 1원"** 으로 나갔다 (`fixes=['1.0억원 -> 1원']`).
      같은 행의 **선적 건수 `1`** 이 후보에 있었기 때문이다. 검증(`verify`)은
      넉넉해야 정상 답변을 미검증으로 잡지 않지만, **교정은 되돌릴 수 없어서
      좁아야 한다** — 둘은 다른 함수를 쓴다 (`repairable_amounts` ↔ `grounded_amounts`)
      - 후보는 **금액 컬럼**에서만 뽑고(`answer_check.is_money_column` — ⛔ 목록은
        여기 한 곳뿐이다. `sql_agent._is_money_column` 이 이 함수를 부른다),
        **1만원 미만은 후보에서 뺀다**(`_REPAIR_FLOOR`). `_MONEY_UNIT` 은 만·억·조만
        매치하므로 교정 결과가 1만원 미만일 수가 없다
      - ⚠️ 금액처럼 보이는 열이 하나도 없으면 예전처럼 전체를 쓴다 — 그때도 하한이 남는다
  - 스트리밍은 `_mask_stream(transform=…)` 의 **줄 버퍼**에 얹는다. `828.` + `7억원`
    으로 쪼개져 오므로 청크 단위로 판정하면 절반만 듣는다
  - ⚠️ 두 경로(`run_sql_agent_stream`·`format_answer`)에 **함께** 걸어야 한다.
    한쪽만 고치면 경로에 따라 답이 갈린다 (이미 겪은 사고다)
- **설명 가능한 값으로 인정하는 것**: 행 값(단위 변환 포함) · 질문 속 값 · 열 합계 ·
  부분합/잔여합 · 평균(행수·3·4·6·12로 나눈 것) · 비중 · 증감률 · 배수 · **차이** ·
  연도 · 작은 정수
- ⚠️ **오탐을 줄이지 않으면 경보가 소음이 된다** ("매일 같은 알림은 곧 무시당한다"와
  같은 이유). 실측으로 네 가지를 걷어냈다 — 처음 4~18% 였던 미검증률이 0%가 됐다:

    | 오탐 원인 | 예 |
    |---|---|
    | 단위 변환 | 행은 원, 답변은 "1,153.6억" |
    | 날짜 조각 | "2026-06-30" 의 06·30 |
    | 부분합·평균 | "기타 27개 합계", "월평균 208.3억" |
    | 소수 비율 반올림 | 0.1% (상대오차로 보면 늘 실패) |

- ⚠️ 반대로 **너무 넓히면 검출력이 죽는다** — 합계끼리의 비율까지 허용했더니 조회에
  없는 "베트남 88.3억"이 우연히 설명됐다. 파생값 공간을 넓힐 때마다 **오답 케이스로
  역검증**할 것 (`tests/test_no_silent_failures.py` 에 양방향으로 있다)

## 보고서 파이프라인 — 2026-08-12

- **위치**: `app/reports/` (스펙·엔진·렌더·템플릿·API) / **상세 규칙은 `app/reports/README.md`**
- **사용자는 채팅에서 질문으로 받는다. 생성 전에는 반드시 최소 2개 확인 질문을 거친다**
  (2026-08-24). 첫 요청에서는 조회하지 않고 ① 분석 대상·기간과 핵심 판단, ② 드릴다운
  축과 비교 기준을 한 번에 묻는다. 바로 다음 답변을 원 요청에 합쳐 기간·필터 추출기와
  플래너에 넘긴 뒤 요약 4줄 + 링크를 만든다. 대기 상태는 `app/reports/clarify.py` 가 직전
  assistant 메시지의 숨은 표식으로만 이어받는다 — 오래된 요청이 일반 대화를 가로채면 안 된다.
  스트리밍·비스트리밍 양쪽 모두 `messages` 를 `_handle_report()` 에 전달할 것.
  CLI 는 대화형 확인 없이 `python scripts/build_report.py <스펙>` 으로 직접 실행한다.
- ⛔ **보고서는 명시했을 때만 만든다** (2026-08-13 규칙 결정). 조회 8~12회에 10~30초가
  드는 특수 경로다. 두 가지 중 하나여야 만든다:
  1. **`@@보고서`** 로 데이터소스를 지정 — 이 경우 문구를 보지 않는다
     (`@@보고서 2026년 일본 매출` 처럼 '보고서'라는 말이 본문에 없어도 만든다)
  2. 질문에 **"보고서"·"리포트"·"report"** 라고 적음
  - 예전 신호어 **"분석해줘·정리해줘·종합·진단·심층·브리핑·딥다이브" 는 뺐다.**
    평범한 조회에도 흔한 말이라 의도치 않은 보고서가 계속 생겼다
  - `@@보고서` 는 `_DB_REGISTRY` 의 `route: "report"` 엔트리다. 테이블이 아니라
    **산출물**이라 그룹도 따로 둔다 (`chat.js` 의 `SOURCE_GROUPS`/`SOURCE_ROUTE_MAP`/
    `SERVICE_ICONS`/`SLASH_PRESETS` 4곳)
  - `explicit` 플래그는 `_handle_report → service.run → registry.route` 로 관통시킨다.
    ⚠️ 한 곳만 고치면 `@@보고서` 인데 `route()` 가 None 을 내 조용히 일반 답변이 나간다
  - System Status(`safety.get_safety_status`)에 **생성 조건을 함께 노출**한다 —
    "왜 보고서가 안 만들어지지"를 화면에서 답할 수 있어야 한다
  - **사용자 가이드**(노션 "AI Chat 가이드" `3262b428…`)에도 같은 예시가 들어 있다.
    ⛔ `scripts/notion_user_guide.py` 는 **돌리지 마라 — 페이지를 통째로 지운다.**
    그 스크립트는 clear 후 재생성인데 실제 페이지는 손으로 많이 편집됐다(스크린샷·
    DB 정보·Tester 섹션…). 게다가 `upload_to_notion` 이 블록 헬퍼를 더 이상
    내보내지 않아 import 부터 깨져 있다. 가이드는 **바꿀 구간만** 고친다
    (Notion MCP `update-page` 의 `update_content` old_str/new_str)
  - **작동이 검증된 예시 질문**은 `app/reports/README.md` 의 표에 있다. 웰컴 칩
    (`chat.html`)과 후속 제안(`chat.js` `FOLLOWUP_POOLS.report`)도 같은 문장을 쓴다 —
    ⚠️ 제안 문구에 "보고서"가 빠지면 눌러도 일반 답변이 나간다
- **질문 속 필터는 결정적으로 뽑는다** (`registry.extract_filters`). 여기서 놓치면
  "우마 매출 보고서"가 전사 보고서로 나가 **질문에 답하지 않은 문서**가 된다.
  2026-08-13 에 세 가지를 실측으로 잡았다:
  - **브랜드가 아예 안 잡히고 있었다** → `_BRAND_FILTERS` 추가. 값은 브랜드 정의를 따른다
    (스킨천사=SK+CBT, 좀비뷰티=SK+`라인`ZB). ⚠️ `sk`·`um` 같은 짧은 코드는 넣지 말 것 —
    다른 단어에 부분 일치한다
  - **팀명 안의 국가어를 국가로 잡았다** — "중국사업팀"이 `팀=CBT AND 국가=중국` 이 돼
    CBT 의 중국 외 실적이 통째로 빠졌다. 팀을 먼저 잡고 그 이름에 든 국가어를 제외한다
  - **세부 권역을 대륙으로 잡았다** — "동남아시아"가 `대륙=아시아`(Continent1)로 잡혀
    동아시아·서남아시아까지 들어왔다. 권역(`Continent2`)을 먼저 본다
- ⛔ **`UM`·`CBT` 는 제품명(`SET`)이 100% 비어 있고 `Product` 테이블에 행이 아예 없다**
  (2026-08-13 실측: UM 86,894행·1,249.6억, CBT 841,214행·278.8억 전부 빈 값).
  제품별·수량 절은 조회가 0건이라 실릴 수 없다 — **안 팔린 게 아니라 이름이 없는 것**이다.
  그냥 절을 빼면 "결과 없음"으로만 보이므로 `_quality_notes` 가 이유를 본문에 공시한다.
  원가 미적재(UM 99.9%·CBT 99.6%)와 같은 계열의 사실이다
- ⛔ **기능을 묻는 질문에 보고서를 만들지 마라** (2026-08-13 실측). `wants_report()` 가
  신호어만 보던 탓에 **"보고서 기능은 어떤 때 쓰면 좋아?" 가 매번 전사 매출 보고서를
  만들었다 — 10건이 그렇게 쌓였다.** 질문에 답하지도 못하면서 플래너 LLM 1회 +
  BigQuery 8~12회를 태우고 12초를 쓴다 (수정 후 7.6초, 제대로 된 설명 답변)
  - 대응은 신호어를 빼는 게 아니다 — "매출 보고서 만들어줘"는 계속 만들어야 한다.
    `_REPORT_META` 로 **요청형과 설명 요구를 결정적으로 가른다** (보고서 기능/란/사용법/
    어떻게 만들어…). 회귀 21문항으로 양방향을 함께 지킨다
  - ⛔ **`보고서`가 분석 산출물이 아니라 업무 문서 이름이면 만들지 않는다** (2026-08-19 실측).
    `"출장 보고서와 정산서 작성 시 B2B2팀의 내부 역할 분담 규정은?"`이 `보고서` 한 단어
    때문에 기본 2026 상반기 B2B 매출 보고서 7개 절을 만들었다. `_OPERATIONAL_REPORT`와
    `_REPORT_DOCUMENT_QUESTION`이 출장·정산 보고서의 양식/작성/규정/절차 질문을 생성 경로에서
    제외한다. 일반 라우터는 같은 질문을 **notion 확신 경로**로 보낸다. 반대 방향인
    `"2026년 B2B2 매출 보고서 만들어줘"`는 계속 보고서여야 한다
  - ⛔ **문구를 하나씩 막는 방식으로는 끝이 없다.** 위 정규식을 넣고도
    `"이 시스템으로 뭘 할 수 있는지 짧게 정리해줘"` 가 보고서를 만들었다 —
    '보고서'라는 말이 없어도 `정리해줘` 가 신호어이기 때문이다(2건 추가 생성).
    신호어도 대상어도 여러 개라, **대상이 시스템 자신이고(`_SELF_SUBJECT`)
    데이터 명사가 하나도 없으면(`_DATA_NOUN`) 분석할 대상이 없는 질문**이라는
    구조로 판정한다. "이 시스템으로 우리 팀 매출 정리해줘"는 그대로 보고서가 된다
  - 💡 이 부류의 오분류는 **응답이 느린 것으로 먼저 드러난다.** 답이 그럴듯해 보여도
    경로가 틀리면 시간이 튄다 — 느린 질문을 볼 때 라우팅을 함께 의심할 것
- ⛔ **열람은 만든 사람과, 만든 사람이 지목한 사람만.** admin 도 예외가 아니다 (2026-08-12
  결정 — 원가·마진·거래처별 FOC율이 들어간다). 판정은 `store.get_for_user()` **한 곳에서만**
  한다 — 공유를 붙이면서도 이 함수 안에서 소유·공유를 함께 보게 해 호출부를 그대로 뒀다
- **공유는 사람을 지목하는 방식이다** (2026-08-13 추가). 링크를 아는 사람이 다 열 수 있는
  방식은 만들지 않았다 — 잔디·메일로 한 번 새면 회수할 수단이 없다. 대신 **URL 은 그대로
  붙여도 된다**: 지목된 사람만 열리고 나머지는 404 를 본다
  - 공유를 **거는 쪽**(`store.share_add`)도 소유자 확인을 **SQL 안에서** 한다
    (`INSERT ... SELECT ... WHERE r.user_id = %s`). 파이썬에서 먼저 확인하고 나중에
    INSERT 하면 확인을 빠뜨린 호출부가 언젠가 생긴다 — FI 방어선과 같은 사상이다
  - 대상은 **가입한 사용자만**이다. FI 는 미가입자에게 미리 부여해두지만, 공유는 지금
    보여주려고 누르는 동작이라 열 수 없는 사람을 목록에 올리면 공유한 줄 알게 된다
  - ⚠️ 공유 막대(`share_ui.py`)는 **저장된 HTML 에 굽지 않고 응답마다 끼운다.** 파일은
    한 번 저장되고 여러 사람이 열기 때문에, 구워 넣으면 **보는 사람마다 달라야 할 것이
    만든 사람 기준으로 굳는다**. 인쇄·PDF 에서는 빠진다(`@media print`)
  - ⚠️ `GET /api/reports/share-targets` 는 **`/{report_id}` 보다 위에** 둬야 한다.
    아래 두면 report_id 로 먹혀 422 가 난다
- **질문 유형이 보고서의 모양을 정한다** (`app/reports/intent.py`, 2026-08-14).
  ⛔ 예전엔 플래너 프롬프트에 *"보통 좋은 순서: 총량→추세→구성→전년비→순위"* 가 박혀 있어
  **무엇을 물어도 비슷하게 생긴 보고서**가 나왔다 (사용자 지적)
  - 유형 판정은 **규칙**이 한다 (LLM 아님): size · change · efficiency · compare_target ·
    concentration · timing · ranking. 여러 개 걸리면 더 구체적인 것이 이긴다
  - 유형별 권장 절 순서가 곧 논지다 — "왜 늘었나"는 `versus → compare → contribution`,
    "효율"은 `ratio → correlation`, "언제"는 `seasonality → promotion`
  - 뼈대는 프롬프트에 **우선순위로** 넣는다(강제 아님). LLM 이 죽어도 같은 유형의
    기본 계획이 나온다 — 예전 기본 계획은 유형과 무관하게 규모형 하나였다
- **모델을 바꿔 견주려면** `python scripts/compare_report_models.py "질문" --models gemini,claude`
  - 이 파이프라인에서 모델이 바꾸는 것은 **계획과 해석뿐**이다. 숫자·SQL 은 결정적이라
    모델을 바꿔도 표는 같다 — 비교도 그 둘만 본다
  - ⛔ **자동 점수를 매기지 않는다.** LLM 이 LLM 을 채점하면 근거 없는 숫자가 하나 더 생긴다
- **두 갈래다** — 손으로 검증한 고정 스펙(`specs/*.py`)과, 질문에 맞춰 블록을 조합하는
  동적 보고서(`planner`+`blocks`+`semantic`). 고정 스펙이 있으면 **그쪽이 이긴다**
- **맨 앞 절이 논지를 말한다** (2026-08-13). 절을 나열만 하면 표 모음이지 분석이 아니다.
  `요약과 해석` 한 절에 **사실(규칙) → 해석(LLM) → 다음 할 일(LLM)** 을 담는다
  - ⚠️ 요약은 "무엇이 얼마인가"에서 멈춘다. 추세·기여도까지 담았더니 바로 아래 해석과
    **같은 얘기를 두 번** 했다 — 겹침은 프롬프트가 아니라 후처리로 막는다
    (이미 적힌 요약을 넘기고, 새 수치가 없는 문장은 버린다)
- ⛔ **`app/reports/insight.py` 가 이 파이프라인에서 유일하게 LLM 이 문장을 쓰는 곳이다.**
  규칙으로는 "그래서 무엇을 해야 하나"를 못 써서 연 예외다. **대신 숫자는 만들지 못한다** —
  문장 속 수치를 조회 결과와 대조해 하나라도 없으면 그 문장을 통째로 버린다.
  검증은 프롬프트가 아니라 후처리다. 버린 것은 WARNING 에 남긴다
- **비교는 `versus` 절이 한다.** 필터가 모든 절에 AND 로 걸려 비교 대상이 존재할 수 없었다 —
  이 절만 주 필터 하나를 빼고 재조회해 `나머지 = 전체 − 대상` 을 만든다. 남은 필터는
  양쪽에 똑같이 걸어 '일본 B2C' 를 '일본 외 B2C' 와 견준다
- **동적이어도 LLM 은 SQL 도 숫자도 쓰지 않는다.** 플래너는 "어떤 지표를 어떤 축으로"만 고르고,
  SQL 은 `semantic.py` 가 결정적으로 만든다. 서술(finding)은 `blocks.py` 의 규칙이 숫자에서 뽑는다.
  어휘 밖의 계획은 버리고 로그를 남긴다
- **질문 속 국가·팀 필터는 결정적으로 뽑아 모든 절에 강제**한다 (`registry.extract_filters`).
  "일본 매출 보고서"인데 한 절이라도 전사를 집계하면 질문에 답하지 않은 문서가 된다
- **숫자는 코드가, 문장은 템플릿이 만든다.** 서술에 숫자 리터럴이 있으면 렌더러가 거부한다.
  LLM 이 수치를 쓰는 경로를 아예 두지 않는 것이 설계의 전부다
- **품질 게이트가 서술보다 먼저 돈다.** 깨진 컬럼을 포함한 채 그럴듯한 비율을 내는 것이
  가장 위험한 실패다 — 조용하기 때문이다. 제외했으면 제외 사실과 영향을 본문에 공시한다
- ⚠️ **반올림한 값으로 `HAVING` 을 걸지 마라** — 억 단위 반올림 후 `> 0` 으로 걸러
  거래처가 420곳에서 122곳으로 줄었다. 필터·비율은 원값, 반올림은 표시할 때만
- ⛔ **주기적 등락의 원인을 매출 데이터에서 추정하지 마라** (2026-08-13 지시).
  일본 매출이 3·6·9월에 솟는 것은 큐텐 메가와리인데, 그건 실적 테이블이 아니라
  **프로모션 캘린더가 아는 사실**이다. `promotion` 블록이 일정과 월별 매출을 맞춘다.
  `seasonality` 는 "몇 월이 평균보다 높다"까지만 말하고 원인은 그쪽으로 넘긴다
- **판정 계층 — 표를 '읽어야 하는 것'에서 '판단이 끝난 것'으로** (`app/reports/judge.py`,
  2026-08-13). 데이터분석파트의 `Paid_Profitability_Report` 를 보고 들여왔다. 그 문서가
  좋았던 건 차트가 아니라 **장마다 판정이 끝나 있다는 점**이고, 셋 다 숫자에서 기계적으로 나온다
  - `결론` 한 줄 — 절의 발견 중 앞머리를 따로 세운다. ⛔ **방법론 문장은 결론이 아니다**
    ("전체의 0.5% 미만은 뺐다"가 결론 자리에 올라온 적 있다). `_METHOD` 로 거른다
  - `판정` 열 — 성장은 급증/증가/보합/감소/급감, 비율은 전체 평균 대비 높음/평균/낮음.
    ⛔ **기준선에서 이상값과 0을 뺀다** — 있을 수 없는 120% 하나가 평균을 끌어올려
    멀쩡한 행이 죄다 '낮음'이 됐다. 판정 기준은 화면에 함께 적는다
  - `다음에 볼 곳` 절 — 키울 곳 / 막을 곳 / 확인할 곳. ⛔ **규모 하한을 반드시 걸어라**:
    `share` 가 없는 절(성장 비교)에서 필터가 통과돼 러시아 +183%·불가리아 −100% 가
    실행안 1·4위로 올라왔다. 없으면 그 절 안에서 직접 구한다.
    데이터 구멍(품질 노트·0건 절)을 **버킷 앞쪽에** 넣는다 — 뒤에 두면 상한에 잘린다
  - 분자가 분모의 일부인 비율이 **100%를 넘으면 데이터 문제**로 짚는다 (할인>매출은 없다)
  - ⛔ **그 문서의 수수료·배송비·인건비 P&L 은 가져오지 않았다.** 우리 데이터로 만들 수 없고
    (`Service_Fee` 가 음수), 그 문서도 수수료를 "보수적 임계치 30%" 가정으로 채웠다.
    가정으로 채운 손익은 통계처럼 생겨서 가장 잘 믿긴다 — 외부 맥락 절과 같은 이유로 뺐다
- ⛔ **템플릿 자바스크립트 문법이 깨지면 에러 없이 백지가 나간다** (2026-08-13 실측:
  `rest` 를 두 번 선언해 스크립트가 통째로 죽었다). 서버는 200 을 주고 HTML 도 멀쩡히
  저장되므로 **사람이 열기 전까지 아무도 모른다.** `render.lint_script()` 가 `node --check`
  로 막는다 (node 가 없으면 건너뛴다). 표시가 코드에 있는 구조라 표시도 검사해야 한다
- **상관은 계산해서 말하고, 외부 요인은 계산하지 않고 말한다** (2026-08-13)
  - `correlation` 절 — 두 지표를 월별로 맞춰 **코드가 피어슨 상관을 낸다**. 1개월 지연도 함께
    본다(광고→매출처럼 늦게 듣는 관계). 점이 6개 미만이면 만들지 않는다.
    ⛔ **"상관은 인과가 아니다"를 발견 문장에 항상 넣는다** — 계수만 남으면 인과로 읽힌다
  - `external` 절 — "날씨·외부 이슈도 봐줘" 같은 질문에만 붙는다. 검색으로 **그 달에 무슨 일이
    있었는지 문장만** 모으고 매출 옆에 나란히 놓는다. 판단은 사람이 한다
    - ⛔ **여기서 상관계수를 내지 않는다.** 검색은 문장을 줄 뿐 시계열을 주지 않는다.
      LLM 이 "기온과 매출 상관 0.72"를 쓰면 계산이 아니라 창작이고, **통계처럼 생겨서
      가장 잘 믿긴다.** 진짜 날씨 상관이 필요하면 기온 시계열을 적재해야 하고,
      그때는 `correlation` 이 그대로 계산한다
    - 코드가 세 가지를 버린다: **숫자**(연월만 허용) · **인과 표현**(때문·덕분·영향으로) ·
      **우리 실적 언급**(조회하지 않은 값이다). 절 제목과 note 에 검증 안 됐다고 항상 적는다
    - `insight` 의 사실 묶음에서 **제외**한다. 검증 안 된 문장이 해석의 근거가 되면
      나머지 방어가 다 무의미해진다
- ⚠️ **필터 낱말은 앞 글자를 봐야 한다** — "외부 요**인도** 같이 봐줘"의 `인도` 를 국가로 잡아
  일본 보고서가 '일본 · 인도' 보고서가 됐다 (2026-08-13). 앞이 한글이면 더 긴 낱말의 일부로 본다.
  **뒤는 보지 않는다** — 조사(베트남**과**)가 붙는 것이 정상이다
- **차트는 축이 있어야 하고 바닥은 0이어야 한다** (2026-08-13 사용자 지적).
  면적 그래프를 최솟값 기준으로 그려 **최저 6.6억(최고의 12%)이 0으로 보였다.**
  축이 없어 어느 봉우리가 언제인지도 알 수 없었다 — 표의 장식이지 차트가 아니었다
  - **눈금 숫자만으로는 부족하다.** 축선과 **축 제목**이 있어야 무슨 축인지 안다.
    제목은 payload 의 컬럼 라벨에서 가져와 차트와 표가 같은 말을 쓰게 한다
  - ⚠️ y축 제목은 **맨 왼쪽 고정** — `padL` 기준으로 잡으면 눈금·범주 라벨과 겹친다
    (라벨 길이가 데이터마다 달라 여백을 계산으로 맞출 수 없다)
  - ⚠️ **그리는 값 기준으로 정렬해 자를 것.** 표 순서(매출순)대로 자르면 **가장 크게
    줄어든 항목이 잘려 나간다**
  - ⚠️ 절마다 `chart_key` 로 무엇을 그릴지 정한다. compare 를 value 로 그렸더니 바로 앞
    구성 절과 **똑같은 그림이 두 번** 나왔다 — 그 절은 `delta` 를 그려야 한다
- **한국어 조사는 `blocks._josa()` 로 붙인다.** "일본**는** +35.4%", "55.1억**가** 최고"
  처럼 틀리면 자동 생성 티가 난다. 한글 받침·숫자 읽기(0·1·3·6·7·8이 받침)·영문 어미 처리
- **표시 규칙을 바꿀 땐 모든 렌더 경로를 확인하라.** 2행짜리 절을 문장으로 바꾸면서
  막대가 표 경로에만 남아 세 절의 차트가 조용히 사라졌다. 에러가 없어 늦게 발견된다
- 발견(finding) 문장은 **평문으로 렌더**된다(HTML 주입 방지). 마크다운을 쓰면
  `**강조**` 가 별표째 보인다 — 강조는 문장 구조로 할 것
- 지표·축 어휘로 표현할 수 없는 조회(프로모션 캘린더 등)는 블록의 `queries()` 가
  **완성된 SQL 문자열**을 돌려줄 수 있다. 그 SQL 도 `semantic.py` 안에서 만들고
  `validate_sql()` 을 똑같이 통과한다 — 블록이 직접 SQL 을 짜지는 않는다

## 출근 브리핑 (첫 화면 + 잔디) — 2026-08-25

- **파일**: `app/core/workday.py`(근무일 창) · `app/core/work_briefing.py`(문서) ·
  `app/core/jandi_briefing.py` + `app/api/jandi_briefing_api.py`(전달) ·
  `scripts/jandi_briefing_relay.py`(DB_PC 릴레이) · `app/frontend/personal-briefing.js`(렌더)
- **잡**: `personal_briefing_daily` 매일 **07:30** (08:30 → 09:00 → 07:30, 전부 사용자 요청).
  ⚠️ **잔디 발송(08:00)보다 먼저 끝나야 한다** — 릴레이는 만들어 둔 것을 가져갈 뿐이라
  생성이 늦으면 그날 몫이 통째로 다음 회차로 밀린다. 시각을 바꿀 땐 둘을 함께 옮길 것.
  문서를 만들고, 잔디를 등록한 사람 몫은 발송 대기열(`briefing_jandi_outbox`)에 넣는다
- **메일 구간은 "마지막 근무일 18:00 ~ 지금"** 이다 (평일=어제, 월요일=금요일, 연휴 다음날=연휴 전).
  ⛔ **공휴일 표를 손으로 적지 마라** — 구글 '대한민국 공휴일' 캘린더를 읽는다.
  못 읽으면 주말만 보고 **"공휴일 확인 실패"를 브리핑 본문에 적는다.** 조용히 틀린 창이 되지 않게
  - ⚠️ 창이 하루를 넘으면 Gmail 수집 상한을 20 → 40 으로 올린다. 20 으로 두면 월요일 아침에
    **앞부분(금요일 저녁분)이 통째로 잘리는데 화면엔 아무 말도 안 나온다**
- ⛔ **LLM 은 문장만 고른다. 사실은 코드가 판정한다** (`work_briefing.compose`) —
  보고서 `insight.py` 와 같은 사상이다. 넷을 코드가 막는다:
  1. 모든 항목이 실재하는 `event_id`/`message_id` 에 묶여야 한다
  2. **문장 속 숫자가 원문(제목·미리보기·시간·장소)에 없으면 그 항목을 버린다.**
     날짜·개수(≤31)·연도만 예외다 — 넓히면 금액·비율이 새어 검출력이 죽는다
  3. 🔴 는 규칙이 정한다: 3시간 내 시작 · 오늘/내일 마감. LLM 판단을 덮어쓴다.
     그리고 **최대 5개** — 흔해지면 아무 뜻도 없어진다
  4. 마감일은 파싱되고 어제~45일 안일 때만 남는다
  - 버린 문장은 `work_briefing_items_dropped` WARNING + 화면·본문에 건수로 공시한다
- ⛔ **요약을 두 벌 만들지 마라.** 카드(`mail.summary`)는 문서에서 **파생**시킨다
  (`_apply_document`). 각자 만들면 같은 메일을 두고 한 화면이 서로 다른 말을 한다 —
  direct 프롬프트가 두 벌이라 경로마다 답이 갈렸던 것과 같은 부류다
- ⚠️ 일정 `description` 과 메일 `snippet` 은 **문서를 만드는 동안만** 살아 있다.
  저장 직전에 `_strip_event_notes`/`_strip_mail_snippets` 가 벗긴다
- ⚠️ `personal-briefing.js` 는 **브라우저 저장소를 쓰지 않는다** (구글 데이터를 다루는 파일이라
  영속 경로를 아예 두지 않는 것이 규칙이고, 프론트 테스트가 이를 막는다). 접힘 상태는 메모리에만

### 첫 화면 배치 — 문서 = 오늘, 카드 = 오늘 바깥 (2026-08-26)

⛔ **같은 사실을 문서와 카드가 둘 다 그리지 마라.** 처음엔 기존 4카드를 그대로 두고 문서를
얹었는데, `오늘 우선 확인` 카드가 **밑의 세 카드에서 뽑아온 요약**이라 한 화면에 요약이
세 겹이 됐다 — 문서 → 우선 확인 → 각 카드. 메일 요약 문장은 **글자 그대로 두 번** 나왔고,
`일본 B2C 매출 18%` 는 카드 두 곳에 동시에 떴다. 사본이 갈리면 언젠가 서로 다른 말을 한다.

| 자리 | 맡는 것 |
|---|---|
| 브리핑 문서 | **오늘** — 일정·메일 요약·Action Item·마감·업무 지표 |
| `내일부터` 카드 | 오늘 **이후** 일정만 (`isSameKstDay` 로 오늘 제외) |
| `그 밖의 메일` 카드 | 문서가 **고르지 않은** 메일만 (`document.mail` 의 id 제외) |

- 정리 결과 첫 화면 높이 1403 → 1154px. 업무 지표가 문서로 올라가서 **잔디 본문에도 실린다**
- ⚠️ **빈 카드를 남기지 마라** — "없습니다" 만 적힌 카드는 화면만 길게 만든다. 0장이면
  그리드를 숨기고, 1장이면 `.single` 로 전체 폭을 쓴다 (2열에 한 장만 있으면 잘려 보인다)
  - 단 **구글 미연결 상태에서는 빈 카드를 남긴다** — 다시 연결할 버튼 자리가 필요하다
- ⚠️ **접었을 때 급한 것을 숫자로만 남기지 마라.** `급한 것 3` 만으로는 무엇이 급한지 알 수 없다.
  `briefing-doc-urgent` 가 접힘 상태에서만 상위 2건 제목을 보여준다 (펼치면 본문에 다 있다)

### 화면의 신호 방식 — 왼쪽 시간축 (2026-08-26)

⛔ **이모지로 위계를 만들지 마라.** 처음엔 섹션마다 이모지(📅✉️✅⏰📊), 줄마다 신호등(🔴🟡),
항목마다 둥근 회색 박스였다. 사용자 지적: *"AI 느낌을 좀 지우고 싶어"*. 원인은 내용이 아니라
**신호 방식**이었다 — 정보 하나하나를 카드로 감싸는 건 생성형 UI 의 습관이고 밀도를 떨어뜨린다.

- **시그니처는 왼쪽 고정폭 시간축이다.** 이 화면의 모든 항목이 '언제'를 갖는다 —
  일정은 시작·종료, 메일은 수신 시각, 기한은 날짜, 지표는 기준일. 시각을 왼쪽에 세우면
  **아이콘 없이도 종류가 읽히고** 하루가 위에서 아래로 흐른다
  - ⚠️ `font-variant-numeric: tabular-nums` 가 없으면 숫자 폭이 달라 축이 성립하지 않는다
  - ⚠️ **할 일에는 그 자체의 시각이 없다.** 근거가 온 시각을 쓰되 그냥 두면 *"그 시각에 하는 일"*
    로 읽힌다 — `origin`(근거 메일 제목)을 함께 실어 무엇에서 나왔는지 밝힌다
- **긴급은 색 원이 아니라 타이포다** — 시각이 진해지고 굵어지며, 행 왼쪽에 2px `--accent` 바.
  빨강을 쓰지 않아 화면이 알록달록해지지 않는다
- **항목에 배경 박스를 두지 않는다** — 행 + 얇은 구분선. 섹션 제목은 `[이름] … [개수]`
- ⛔ **상태 칩은 문제일 때만 보인다** (`ready`·`empty` 는 `display:none`). 늘 "최신" 이 떠 있으면
  읽히지 않는 소음이다
- **첫 화면 폭은 640 → 1040px** (2026-08-26). 세로로만 쌓여 스크롤이 길다는 지적에 넓혔다.
  ⛔ **브리핑만 넓히지 마라** — `--welcome-width` 는 인사말·추천 칩·브리핑이 **함께** 쓰는
  단 하나의 폭이다. 갈라 두면 왼쪽 끝이 어긋나 "가운데 정렬이 안 된 것 같다" 가 재발한다
  - 문서 본문은 1080px 이상에서 **2열**이다: 왼쪽 = 오늘 일어나는 것(일정·메일),
    오른쪽 = 해야 할 것과 참고(할 일·기한·지표). 927px 로 줄었다 (2열 전 약 1,600px)
  - ⚠️ **참고 섹션을 오른쪽 열로 올리지 마라 — 실측하니 오히려 늘었다** (963 → 1046px).
    오른쪽이 과적재돼 그쪽이 높이를 결정한다. 오른쪽 여백은 낭비가 아니라 숨 쉴 자리다
  - ⚠️ 폭을 넓히면 **추천 칩이 같이 늘어난다** — 3열 그리드였던 것을 flex 로 바꿔
    글자 폭에 맞는 pill 로 되돌렸다 (`test_welcome_chips_are_pills_not_a_stretched_grid`)
  - **추천 칩 7개는 인사말과 `Today` 사이**에 둔다 (2026-08-26 사용자 요청 — 맨 아래에서 올렸다).
    마크업 순서가 곧 화면 순서라 `test_welcome_chips_sit_between_the_greeting_and_today` 가 지킨다
  - ⛔ **칩 문구를 바꿀 땐 그 경로의 의도 판정 함수로 실제 확인할 것.** `재고` 칩은
    `inventory_intent("센텔라 앰플 재고 알려줘")` 가 검색어를 `센텔라 앰플` 로 깨끗하게 뽑는
    문장이다. 낱말이 남으면 에러가 아니라 **0건**이 난다 (`얼마나` 가 남던 문구를 실측으로 걸렀다)
- 카드도 같은 어법을 쓴다 — 날짜는 `8/27(목)` 로 문서의 기한 표기와 맞춘다.
  ⚠️ 한 화면에 두 어법을 두지 마라
- 💡 **잔디 본문(markdown)은 이모지를 그대로 쓴다.** 평문 매체라 위계를 줄 수단이 그것뿐이다 —
  사실은 같은 소스(`document`)이고 표현만 매체에 맞춘 것이다
- 💡 서버의 `priorities` 필드는 남아 있지만 **화면에서는 그리지 않는다.** 문서의 🔴 +
  Action Item 절이 같은 일을 근거까지 붙여서 한다

### 지표는 합계가 아니라 **가장 눈에 띄는 변화**다 (2026-08-26)

사용자: *"전사적으로 하지말고 가장 눈에띄는 변화를 포착해서 인사이트를 만들어줘.
데이터리터러시도 중요한 부분이야."* — 합계만 적으면 "무슨 일이 있었나" 를 말하지 못한다.

- **금액으로 고른다.** ⛔ 변화율로 고르면 작은 축의 배수가 이긴다 — `_top_change` 주석에
  그 실제 사고가 있다 (영국 +733%(+10.8억)가 뽑히고 정작 아랍에미리트 −12.9억을 놓쳤다).
  관문 셋: 양쪽 다 1억 이상 · 축 합계의 5% 이상 움직임 · 그 축 기준 10% 이상
- **얼마나 큰 이야기인지 함께 말한다** (`_contribution`) — `감소분의 85%`.
  ⛔ 방향이 반대면 몫을 적지 마라: 전사가 늘었는데 이 나라만 줄었으면 "감소분의 32%" 는
  거짓이다 → `전체와 반대 방향`. 축 하나가 전체 증감분보다 크면 `증감분보다 큼 · 다른 곳은 반대로`
- **없으면 없다고 한다** — `눈에 띄게 움직인 국가 없음 (1억·10%·축의 5% 기준)`.
  기준을 함께 적어 "왜 없나" 에 답한다
- ⛔ **축을 벗어나지 마라 — 2026-08-26 프로덕션에서 두 가지가 조용히 틀렸다**
  (숫자는 진짜라 화면만 보고는 알 수 없었다):
  1. 광고 스냅샷의 `by_country` 는 **전사 집계**라 동남아시아1팀·서구권마케팅팀·
     서구권이커머스팀이 **모두 같은** `미국 광고비 2.9→0.9억` 을 받았다.
     → `pairs`(team×country 원본)를 스냅샷에 남기고 `_ad_by_country()` 로 축을 좁힌다
  2. 국가 축(`말레이시아`)인데 `호주 13.8→2.8억` 이 떴다. `compose()` 는 이미
     "국가 축이면 그 나라 자체가 주제다" 라고 지키고 있었다 — 지표도 같은 규칙을 따른다

### 지표 = 매출 · 마케팅 · ROAS 세 줄, 최신 집계에서 (2026-08-26)

⛔ **저장된 알림 행(`daily_briefings`)에서 지표를 만들지 마라.** 그건 *알릴 만한 변화*만
담는 자리라 조용한 날엔 행이 없고, 그러면 며칠 전 수치가 화면에 붙는다. 마케팅 스냅샷이
이미 같은 이유로 따로 있었다 — **매출도 `briefing_sales_snapshots` 를 두고**, 지표는
매번 그 둘에서 새로 만든다 (`sales_item` / `marketing_item` / `roas_item`).

- 저장 행에서 가져오는 것은 **관심 축(scope)** 뿐이다. 그건 사람마다 고정된 값이다
- ⛔ **ROAS 는 실매출이 아니다.** `conversion_value_krw` 는 **광고 플랫폼이 스스로 집계한
  전환매출**이고 `Sales1_R` 과 다르다 — 그래서 이름을 `광고 전환 ROAS` 로 붙이고
  본문에 `플랫폼 자체 집계값` 이라고 적는다
- ⛔ **전환매출 0 을 `ROAS 0.00` 으로 쓰지 마라.** Meta·Tiktok 은 전환 추적이 없으면
  0 으로 들어온다 — 매출이 없다는 뜻이 아니다. 배수를 만들지 않고 `산출 불가 · 전환 추적 없음`
  이라고 적는다 (원가 미적재·`Service_Fee` 음수와 같은 계열의 함정이다)
- ⚠️ **기준일이 셋 다 다를 수 있다** — 매출 8/24, 광고 8/25 (광고 적재가 하루 빠르다).
  각 줄이 자기 날짜를 시간축에 세운다. 억지로 맞추지 마라
- ⚠️ `briefing.py` 에는 **`claude` 라는 낱말이 들어가면 안 된다** (LLM 미사용 가드가
  파일 전체를 훑는다). 규칙 문서를 인용할 땐 다른 이름으로 부를 것

### 지표는 매일 저장하고, 알림만 쉰다 (2026-08-26)

⛔ **`is_repeat()` 의 7일 쿨다운으로 저장까지 건너뛰지 마라.** 그 장치는 **알림**용이다
(매일 같은 알림은 곧 무시당한다). 그런데 첫 화면 지표는 알림이 아니라 **대시보드**라
같은 이야기가 이어져도 수치는 매일 새로워야 한다.

- 사용자 제보: *"지표 업데이트가 매일 되야하는데 안되네"* — **잡은 6일 연속 정상이었다.**
  건너뛴 저장이 원인이었고, 소유자 계정엔 **8/23 것이 8/26 까지** 걸려 있었다
- `daily_briefings.notified` 로 가른다: `1` = 알림함·메일, `0` = 화면 지표로만
  - `save(..., notified=False)` 는 **저장은 하고 알리지 않는다**
  - ⚠️ upsert 에서 `notified=GREATEST(...)` — 같은 날 두 번 돌 때 조용한 갱신이
    이미 알린 것을 0 으로 내리면 알림함에서 사라진다
- ⛔ **화면 지표는 `latest_for_user()` 로 읽는다.** `for_user()` 는 알림함용이라
  `notified=1` 만 본다 — 그걸 쓰면 며칠 전 지표가 다시 붙는다
- 실측: 고치기 전 13건 저장 → 고친 뒤 **59명 전원**. 알림은 그대로 13건
- ⚠️ 기준일은 `stable_date()` 가 정하며 **적재 지연 때문에 보통 D-2** 다. 이건 방어이지
  결함이 아니다 (적재 중인 날을 "매출 급감" 으로 알리지 않는다). 화면이 기준일을 밝힌다

### 오늘의 환율 — 서버는 못 가져온다, DB_PC 가 밀어 넣는다

**2026-08-26 실측** — 잔디와 같은 부류이고 방향만 반대다:

| 경로 | 결과 |
|---|---|
| WAS → `open.er-api.com` · `frankfurter` · `koreaexim` · `naver` | ❌ 000 (2ms 즉시 실패) |
| WAS → `generativelanguage.googleapis.com` | 404 ← **프록시는 살아 있다** |
| DB_PC → `open.er-api.com` · `api.frankfurter.app` | ✅ 0.17s |

- 파일: `app/core/fx_rates.py` · `POST /api/internal/fx`(잔디와 같은 방어선 3겹) ·
  `scripts/fx_relay.py` · 예약작업 `SKIN1004-FX-Relay`
- ⛔ **LLM 에게 환율을 물어보지 마라.** 검색 그라운딩으로 가져오면 숫자를 지어낼 수 있고,
  환율은 그럴듯하게 틀리면 알아채기 어렵다 (`insight.py` 가 숫자를 못 쓰게 한 것과 같은 이유)
- ⛔ **`date.today()` 로 고시일을 찍지 마라.** `open.er-api` 는 **09:02 KST 에 갱신**된다 —
  07:30 브리핑 시점의 최신값은 **전일 고시**다. API 가 준 날짜를 그대로 싣고
  화면이 `N일 전 고시` 라고 밝힌다 (2026-08-31 사용자 확인: 그렇게 표기하면 된다).
- ⛔ **잔디 본문의 환율은 08:00 에 굳는다. 첫 화면은 굳지 않는다.**
  `get_cached_for_user()` 가 조회할 때마다 `_safe_fx(오늘)` 을 다시 읽으므로,
  오늘 고시가 저장되는 즉시 첫 화면에 반영된다 — 문서(document)만 07:30 산출물이다.
  ⚠️ 그래도 **09:02 이전에는 오늘 환율을 만들 수 없다.** 소스가 그때 갱신한다
     (2026-08-31 08:57 실측: 여전히 전일치를 준다). 이건 결함이 아니라 상한이다.
  ⚠️ 릴레이는 **07:10부터 30분 간격 4시간**(11:10까지). 09:02 이후 기회가 한 번뿐이면
     그 한 번이 실패할 때 오늘 고시가 영영 안 들어온다 — 재시도가 남아 있어야 한다
- ⚠️ **값이 없으면 줄 자체를 넣지 않는다** — 빈 칸은 0원처럼 읽힌다.
  전일 대비가 없을 때 **0% 라고 쓰지 마라**: 안 움직인 것과 모르는 것은 다르다
- JPY 는 **100엔** 단위다 (한국 고시 관행). 통화는 USD·JPY·CNY·EUR —
  ⚠️ 늘리면 한 줄이 길어져 읽히지 않는다. 늘릴 땐 화면부터 확인할 것

### 잔디로 보내는 것 = 출근 브리핑 **+ 셀라 알림** (2026-08-26)

**왜 늘렸나 — 실측이 정했다** (2026-08-26):

| | |
|---|---|
| AD 전체 → 가입 | 500명 → **62명 (12%)** |
| 최근 7일 질문한 사람 | **17명** (전사의 3.4%) |
| 브리핑 발송 → 열람 | 334건 → **27건 (8.1%)** |
| **열어본 날 질문 전환** | **23.7%** |

⟶ **콘텐츠가 아니라 도달이 병목이다.** 열면 4명 중 1명이 질문으로 이어지는데 8%만 연다.
앱 안에만 두지 말고 이미 보고 있는 곳(잔디)으로 밀고, **돌아올 문(링크)을 매번 준다.**

- ⛔ **잔디로는 받는 사람이 특정되는 것만 보낸다** (2026-08-26 사용자 지시: *"꼭 필요한
  내용만 전달해줘. 너무 많으면 스팸 같아"*). 기준은 **중요도가 아니라 수신자 특정 여부**다

    | | |
    |---|---|
    | 보낸다 | 출근 브리핑(본인이 등록) · 보고서 공유(누가 **나를** 지목) · 의견(붐따) 회신(**내가** 물었다) |
    | 안 보낸다 | **공지** — 전원 방송이다. 알림함 배지가 맡는다 |

  - **공지가 특히 위험했다.** 알림함을 한 번도 안 연 사람은 `announce_seen_at` 이 NULL 이라
    **최근 공지 10건이 전부 '안 읽음'** 이다 → 아무에게도 앞으로 오지 않은 방송이 한 사람에게
    최대 5통씩 꽂힌다. 실측(2026-08-26 프로덕션): 대기 중이던 **2통이 전부 공지**였고,
    개인 앞으로 온 것은 0건이었다. 고친 뒤 2 → 0
  - ⛔ 개수를 줄이는 것으로 고치려 하지 마라 — **종류가 틀린 것**이다.
    새 종류를 넣기 전에 물어라: *받는 사람이 특정되는가?* 아니면 알림함이 맡는다
    (`jandi_notify._PERSONAL_SOURCES`, 회귀 테스트가 목록을 고정한다)
  - 한 번에 최대 **3건**, **14일 이내**것만 (`MAX_PER_USER`·`PUSH_WINDOW_DAYS`).
    ⚠️ **알림함에는 이 창을 걸지 마라** — 회신은 나이와 무관하게 닿아야 한다.
    잔디는 "지금 봐 달라"고 두드리는 채널이라 석 달 된 회신은 알림이 아니라 소음이다
  - ⚠️ 넘치면 **새것부터**. 오래된 쪽을 먼저 울리면 급한 것이 뒤로 밀린다.
    시각을 모르면 **보낸다** — 모른다고 버리면 새 알림이 조용히 사라진다
- ⛔ **모든 메시지에 셀라 링크를 붙인다.** 잔디에서 읽고 끝나면 셀라에 오지 않는다.
  ⚠️ `public_base_url` 이 비어 있으면 **링크를 지어내지 마라** — 눌러도 안 열리면 신뢰를 잃는다
- ⛔ **같은 알림을 두 번 보내지 마라** — 그 다음부터 아무도 안 읽는다.
  대기열 유니크 키가 `(user_id, for_date)` 에서 **`(user_id, dedup_key)`** 로 바뀌었다.
  브리핑의 키는 날짜(`briefing:2026-08-26`), 알림의 키는 그 항목 자신(`report_share:7`)
  - ⚠️ 옛 유니크 키를 **반드시 지워야** 한다 (`_OUTBOX_MIGRATIONS`). 남기면 하루 한 건
    제약이 그대로 살아 알림이 **에러 없이** 안 나간다
- ⛔ **안 읽은 것만** 보낸다. 그리고 **처리된 의견만** — "접수됨" 을 잔디로 또 알리면 소음이다.
  한 번에 최대 5건 (밀린 알림이 쏟아지는 것도 소음이다)
- ⚠️ 종류 표(`KIND_META`)가 **서버와 릴레이에 각각 있다** — 릴레이는 DB_PC 단독 실행이라
  앱 모듈을 import 하지 못한다. 한쪽만 고치면 색·제목이 어긋난다 (테스트가 대조한다)
- 잡: `jandi_notify_hourly` 평일 08~18시 매시 :25 / 릴레이는 **매일 08:00~18:30 30분 간격**
  (⚠️ 여기 오래 "평일" 이라고 적혀 있었으나 예약작업 실측은 매일이다 — 2026-09-01
  `schtasks /query /tn SKIN1004-Jandi-Briefing /xml`: `DaysInterval 1` · `PT30M` · `PT10H35M`.
  회차 목록 `jandi_briefing.SEND_TIME_CHOICES` 가 이 창을 따른다)
  (2026-08-31 사용자 요청으로 발송을 8시로 앞당겼다. ⚠️ 브리핑 생성이 **07:30** 으로
  함께 당겨졌다 — 릴레이는 만들어 둔 것을 가져갈 뿐이라 생성이 늦으면 그날 몫이
  통째로 다음 회차로 밀린다. 한쪽만 옮기지 말 것)
  (하루 한 번이면 알림이 다음 날 도착한다 — 그건 알림이 아니다)

### 브리핑 도착 시각은 사람마다 다르다 (2026-09-01)

첫 화면 `잔디로 받기` 에서 고른다. 07:30 생성은 전사 공통이라 못 옮기고, 대기열에서
**언제부터 꺼내갈 수 있는지**(`briefing_jandi_outbox.send_after`)만 사람마다 둔다.

- **선택지는 릴레이 회차와 같아야 한다** — `08:00~18:30` 30분 단위
  (`jandi_briefing.SEND_TIME_CHOICES`). ⛔ 여기 없는 시각을 저장하면 그 사람의
  브리핑은 **영영 오지 않는다** (에러도 없다). API·`set_webhook` 두 곳에서 막는다
- ⛔ **프론트에 목록을 하드코딩하지 마라.** 서버가 `send_time_choices` 로 내려준다 —
  `@@` 데이터소스 목록이 두 벌이라 갈렸던 그 사고와 같은 종류다 (회귀가 지킨다)
- ⛔ **`send_after <= NOW()` 로 비교하지 마라.** 실측(2026-09-01) DB 시계는 KST 와
  같지만 `time_zone` 이 `SYSTEM` 이다 — 호스트 TZ 를 따라가므로 DB VM 이 UTC 로
  재구축되면 9시간 어긋난다. `jandi_briefing.now_kst()` 를 **파라미터로** 넘긴다
- ⚠️ **`send_after IS NULL` 을 빼지 마라** — 셀라 알림(보고서 공유·의견 회신)이
  통째로 멈춘다. 알림은 시각을 갖지 않는다 (미루면 뜻이 없어지는 성격이다)
- ⚠️ **자가 점검이 기다리는 중인 것을 밀린 것으로 세면 안 된다** — 18:30 을 고르면
  07:30 에 만든 브리핑이 11시간 대기한다. `jandi_relay` 는 도착 시각이 지난 것만
  센다 (매일 뜨는 경고는 곧 아무도 안 읽는다)
- ⚠️ 주소를 **비워서 PUT 하면 저장된 것을 쓴다** — 서버는 주소를 가려서 내려주므로
  시각만 바꾸려는 사람에게 되붙일 방법이 없다
- ⚠️ `지금 대기열에 넣기` 는 시각을 붙이지 않는다 — 지금 되는지 보려고 누른 것이다
- ⚠️ PyMySQL 은 `TIME` 컬럼을 **`timedelta`** 로 돌려준다. `normalize_send_at()` 이
  받는다 (str() 이 우연히 통과하지만 우연에 기대지 않는다)

### 무엇을 받을지는 사람마다 다르다 (2026-09-01)

**파일**: `jandi_briefing.SECTIONS`(단일 소스) · 저장은 `user_jandi_webhooks.muted_sections` ·
적용은 `personal_briefing._enqueue_jandi`(브리핑)와 `jandi_notify.enqueue_for_user`(알림).
화면은 `personal-briefing.js` 의 `잔디로 받기` 다이얼로그.

- ⛔ **끈 것을 저장한다 (켠 것이 아니라).** 켠 목록으로 두면 나중에 절이 하나 늘었을 때
  **이미 설정을 저장해 둔 사람에게는 영영 안 보인다** — 에러도 흔적도 없다.
  끈 목록이면 새 절은 모두에게 기본으로 켜진다
- ⛔ **끈 것은 "잔디로 안 받는다" 이지 "안 본다" 가 아니다.** 첫 화면 문서와
  알림함은 그대로 둔다 — 양쪽에서 지우면 그 사람은 어디서도 못 본다.
  `render_markdown(muted=...)` 은 잔디 본문에서만 부른다
- ⛔ **절 키는 문서 키와 같아야 한다.** 다르면 체크는 꺼지는데 본문은 그대로 나가고,
  사용자는 "설정이 안 먹는다" 고만 느낀다. 실제로 환율을 `rates` 라고 적었다가 걸렸다
  (진짜 키는 `items`) — 회귀가 절마다 껐다 켜며 본문이 실제로 줄어드는지 본다
- ⛔ **실릴 것을 전부 끈 사람에게 머리말만 보내지 마라.** 매일 오는 빈 알림은 곧
  무시당하고, 그러면 안 끈 사람의 브리핑까지 함께 안 읽힌다 (`content_sections()`).
  ⚠️ 원래도 **빈 날**에는 머리말만 나간다 — 그건 그대로 둔다. 막는 것은
  "실을 것이 있었는데 사용자가 다 껐다" 는 경우뿐이다
- ⚠️ **`muted` 를 안 보내는 것과 빈 목록은 뜻이 다르다** — 없으면 "안 바꿈",
  `[]` 는 "전부 받기". 시각만 저장하는 요청이 항목 설정을 지우면 안 된다
- ⚠️ `set_webhook` 은 **읽고-고쳐-쓰지 않는다** (`muted_sections=IF(%s,...)`).
  저장에 조회가 딸려 들어가면 두 요청이 겹쳤을 때 한쪽 설정이 조용히 사라진다
- ⚠️ 항목 목록도 **서버가 단일 소스**다 (`GET /api/personal-briefing/jandi` 의
  `sections`). 프론트에 사본을 두면 절이 늘 때 화면에서 통째로 사라진다

### 참조(CC)로만 온 메일은 할 일이 아니다 (2026-09-01 사용자 지시)

받는 사람이 따로 있고 나는 알고만 있으라고 걸린 메일이다. 할 일에 섞이면
**진짜 내 일이 뒤로 밀린다.**

- 판정은 `google_workspace.list_gmail_digest` 가 `cc_only` 로 싣고,
  `work_briefing._action_rows` 가 그 항목을 버린다(`action:cc_only` 로 남긴다)
- ⛔ **참조 칸에 있고 받는 칸에 없을 때만** 참조다. 어느 칸에도 없으면
  (메일링 그룹으로 온 메일) 참조가 아니다 — 모르는 것을 참조로 찍으면
  **진짜 할 일이 조용히 사라진다.** 사서함 주소를 못 구하면 아무것도 안 버린다
- ⚠️ **메일 목록에서는 빼지 않는다.** 뺀 것은 할 일뿐이다 — 안 읽은 메일이
  브리핑에서 사라지면 "왜 이 메일이 없지" 가 된다
- ⚠️ Gmail 은 `metadataHeaders` 에 `To`·`Cc` 를 더해서 받는다. **본문·첨부는
  여전히 받지 않는다** (`format="metadata"`) — 그 전제를 지키는 테스트가 있다
- ⚠️ 프롬프트에도 `cc_only` 를 알려 주지만 **보증은 `_action_rows`** 다.
  프롬프트는 버려질 문장을 줄일 뿐이다

### 저장한 보고는 **전문**으로, 차트는 **글자 막대**로 (2026-09-07)

⛔ **요약만 실어 표가 통째로 사라졌다** (제보: *"잔디에서 내가 저장한 보고가 전체를
보고싶은데 잘려서 나와"*). 실측 1,091자 → 123자. `summarize_answer(…, 300)` 이 앞
문장만 남겼다 — 그 규칙 자체는 옳았지만(잘린 마크다운 표는 파이프 더미다) 그건
**자를 때** 이야기지 전문을 싣지 말라는 뜻이 아니었다.

- **화면 카드는 요약, 잔디는 전문**으로 갈랐다 (`_saved_rows` 가 `answer` 와
  `answer_full` 을 함께 낸다 · 렌더는 `full_answer_for_jandi`). 첫 화면까지 전문으로
  바꾸면 스크롤 지옥이 된다
- ⛔ **`<details>실행된 쿼리</details>` 는 반드시 뺀다** — 앱에서는 접힌 근거지만
  잔디에서는 **평문으로 펼쳐져 내부 테이블 경로가 채널에 노출된다**
- ⛔ 코드펜스(차트 설정 JSON)와 후속 제안 칩도 뺀다 — 잔디에서는 그냥 글자 더미다.
  ⚠️ 차트를 뺐으면 홀로 남는 `시각화` 제목도 함께 걷는다
- ⚠️ 그래도 길면 **자른 사실을 적는다** (`_JANDI_BODY_LIMIT` 9,000) — 조용히
  자르는 것이 애초의 불만이었다

#### ⛔ 차트는 그림으로 못 보낸다 — 글자로 그린다

사용자 질문: *"차트 같은경우는 캡쳐해서 보내주면 안되냥?"* 막히는 것은 렌더가 아니라
**그림을 올릴 자리**다:

| | |
|---|---|
| 잔디가 이미지를 받는 법 | `connectInfo[].imageUrl` — **URL 을 주면 잔디가 가서 받아온다** |
| 실측: 공개 이미지 | ✅ **그린다** (2026-09-07 실제 토픽으로 확인) |
| 실측: 사내 이미지 `10.1.100.5` | ❌ **못 닿는다** — 그림이 아니라 링크 글자로만 남는다 |
| 우리 주소 | `public_base_url = http://ai.cravercorp.internal` · `10.1.100.5` — **내부 전용** |
| 프로덕션 WAS 렌더 수단 | playwright ✗ · matplotlib ✗ (오프라인 SFTP 박스) |

⟶ 그림으로 가려면 **매출 차트를 공개 URL 에 올려야 한다.** 링크 하나가 새면 회수할
수단이 없다 — 보고서 공유를 "링크 아는 사람 다 열람" 으로 만들지 않은 그 이유이고,
내부 경로 마스킹 규칙과도 정면으로 부딪힌다. **그래서 글자로 그린다** (표를 줄로 펴는
것과 같은 계열이다).

- ⛔ **막대는 줄 맨 앞에 세우고, 빈 칸까지 문자로 채운다** (`█` + `░`, 폭 20 고정).
  처음엔 찬 칸만 그리고 줄 맨 앞에 세우면 시작점이 맞을 줄 알았는데, **실제 잔디 화면에서
  막대 왼쪽 끝이 줄마다 어긋났다** — 잔디는 앞쪽 공백을 그대로 그리지 않는다.
  같은 유니코드 블록 문자(U+2588·U+2591)는 폭이 서로 같으므로, 빈 칸까지 채우면
  **글꼴이 무엇이든·정렬이 어떻든** 경계가 같은 자리에서 비교되고 라벨도 한 선에 선다
  (2026-09-07 잔디 실측으로 확인 — `░` 는 두부로 깨지지 않는다)
- ⛔ **본문을 `.strip()` 으로 마무리하지 마라 — 첫 줄의 들여쓰기까지 걷는다.**
  표가 답변 맨 앞에 오면 **첫 막대만 왼쪽으로 튀어나간다.** 화면에서는 "정렬이 좀
  이상하네" 로만 보이는 조용한 실패다 (회귀가 잡았다). `strip("
")` 으로 빈 줄만 걷는다
- ⛔ **자릿수 단위를 반드시 환산한다** (`_UNIT_SCALE`) — `212.4억` 과 `4,860만` 을
  적힌 그대로 세면 만 쪽이 더 긴 막대가 된다. **표의 숫자는 맞는데 그림만 뒤집히고,
  그림이 숫자보다 먼저 읽힌다** (통화 혼재 합계 사고와 같은 계열)
- ⛔ **합계 행은 축척에서 뺀다** — 합계가 최댓값이 되면 나머지가 전부 한 칸이 된다
- ⛔ **안 그리는 쪽으로 기운다** (`_barrable_column`). 넷 중 하나라도 걸리면 그리지
  않는다: **통화 혼재**(USD·KRW 를 한 축에) · **음수**(길이로는 방향을 못 말한다) ·
  **숫자 아닌 셀**(빈 칸이 0으로 보인다) · **본문 3행 미만**(그림이 아니다).
  ⚠️ 퍼센트 열은 고르지 않는다 — 비중과 증감률이 표에서 같은 모양이다
- ⚠️ **눈금을 로그로 바꾸지 마라.** 하위 팀이 전부 한 칸으로 보이는 것은 결함이 아니라
  사실이다 (실측: 0.9%·0.4%·0.1%). 작은 것을 크게 보이게 하면 그림이 거짓말을 한다.
  다만 **0칸으로 사라지지는 않게** 최소 한 칸은 남긴다
- 회귀: `tests/test_jandi_saved_report.py` (24문항 — 막대 11문항 포함)

### 잔디 전달 — 서버는 못 보낸다, DB_PC 가 보낸다

**2026-08-25 실측 (문서를 믿지 말고 찔러 본 결과)**:

| 경로 | 결과 |
|---|---|
| DB_PC → `wh.jandi.com:443` | ✅ 0.18s |
| DB_PC → `10.1.100.5:22` · `10.1.150.5:22` | ✅ |
| DB_PC → `10.1.100.5:80` · `10.1.150.5:3000` | ❌ timeout |
| WAS·APP → `wh.jandi.com` | ❌ 403 (2026-08-18) |

- **잔디에 붙는 쪽과 브리핑을 가진 쪽이 서로 다르다.** 그래서 서버는 만들어 두기만 하고,
  DB_PC 예약 작업(`SKIN1004-Jandi-Briefing`, 매일 09:05)이 **SSH 터널로 꺼내 가** 보낸다
  (`direct-tcpip` 채널 위에서 HTTP — 로컬 포트를 점유하지 않는다)
- ⛔ **잔디 인커밍 웹훅은 DM 이 아니라 토픽 주소다.** 개인 브리핑이라 **받는 사람이 각자
  자기 토픽 웹훅을 등록**한다 (첫 화면 브리핑 > `잔디로 받기`). 공용 URL 하나로 돌리면
  남의 메일 제목이 단체 토픽에 뿌려진다
- **내부 엔드포인트 `/api/internal/briefing-outbox` 방어선 3겹** (하나가 뚫려도 나머지가 막는다):
  1. `BRIEFING_RELAY_TOKEN` 이 비어 있으면 **404** — 기본값으로 열려 있지 않다
  2. 토큰 상수시간 비교
  3. 호출자가 루프백이어야 한다 — nginx 를 거친 실사용자 요청은 닿지 못한다
- ⛔ **저장 URL 은 `wh.jandi.com/connect-api/webhook/…` 만 받는다.** 임의 URL 을 받으면
  서버가 만든 남의 메일 요약을 아무 데나 보내주는 발송기가 된다. 화면에 되돌릴 때도 가린다
- ⚠️ **`.env` 는 배포에서 제외된다.** `BRIEFING_RELAY_TOKEN` 은 WAS 에서 직접 넣고 재기동해야
  한다 — 코드만 올리면 릴레이가 404 를 본다 (모델명이 `.env` 때문에 안 바뀌던 것과 같은 함정)
- ⚠️ **DB_PC 를 끄면 잔디 알림이 멈춘다.** `jandi_relay` 자가 점검이 6시간 넘은 대기 건으로 잡는다
- 💡 근본 해결은 IT 가 프록시 화이트리스트에 `wh.jandi.com` 을 넣는 것이다. 그때는 릴레이를
  걷어내고 서버가 직접 보내면 된다 (대기열 구조는 그대로 쓸 수 있다)

## 수출 물류 — `Export_control.export_logistics` (2026-09-03 연동)

- **소스**: `@@물류` (별칭 `LOG`·`선적`·`출고`·`수출물류`·`물류관리`·`logistics`·`export`) ·
  그룹 **물류 데이터**
  - ⚠️ 처음엔 키가 `LOG` 였다 (2026-09-03 사용자 요청으로 `물류` 로 변경). `LOG` 는 별칭으로 남겼다
  - ⚠️ Qdrant `TEAM_MAP` 에 이미 `물류 → LOG`(물류팀 노션 문서)가 있다. 그 팀은 아직
    `_DB_REGISTRY` 에 없어 지금은 충돌하지 않지만, **노출한다면 이름을 갈라야 한다**
- **테이블**: `skin1004-319714.Export_control.export_logistics` — 발주 한 건이
  나가서 현지 창고에 들어갈 때까지의 **수출 관리대장**. 한 행 = 수출 건 1건
- **원본 스키마 문서**: 구글 시트 `1iwFzA_ks…` 탭 `물류관리_스키마`
  (⚠️ **시트는 실제 테이블보다 좁다** — 시트에 없는 컬럼이 70개 더 있다. 시트를
  스키마의 단일 소스로 믿지 마라. `bq.get_table()` 로 실측할 것)
- 실측(2026-09-03) 2,527행 · `order_date` **2025-12 ~ 현재**
  (2025-06/10/11 에 5건이 흩어져 있으나 사실상 2025-12부터)
- ⛔ **보유 구간 밖을 "수출이 없었다" 로 읽지 마라** — 기록이 없는 것이다
  (프로모션 캘린더와 같은 함정)

### OP 재고와의 관계 — **적재된 데이터는 안 겹친다, 시트 전체는 앞뒤로 맞물린다** (2026-09-03 실측)

사용자 질문("op 시트와 내용이 겹치는 것 같다")을 받고 OP 시트 **40탭 전체**와
테이블 컬럼을 대조했다. **앱이 적재하는 것끼리는 중복이 아니다** (조인할 키조차 없다):

| | 수출 물류 (`export_logistics`) | OP 재고 (`@@OP` 시트) |
|---|---|---|
| 세는 것 | **흐름** — 나간 건 (수출 주문 단위) | **잔량** — 지금 창고에 있는 수량 |
| 키 | 주문번호 · ERP번호(SB) | **SKU** |
| 제품 축 | **없다 (SKU 컬럼 0개)** | SKU 가 키다 |
| 물류 축 | ETD·ETA·B/L·포워더·인코텀즈·통관 | **40탭 어디에도 없다** |

- ⛔ **`발주` 라는 낱말이 양쪽에 있지만 방향이 반대다.** OP 시트의
  `발주현황/입고예정` 탭은 **제조업체 → 자사 창고(inbound)** 발주이고,
  `export_logistics.order_date` 는 **수출 주문(outbound)** 발주다.
  게다가 그 탭은 숨김 + 2022년 데이터로 죽어 있고 앱이 읽지도 않는다
  (앱이 읽는 탭은 `SK/HQ_NEW`·`ERP 코드 통합 재고현황`·`유통기한` 셋뿐)
- ⚠️ **그렇다고 "무관" 은 아니다 — 시트 전체로 보면 같은 일의 앞뒤다.**
  앱이 읽지 않는 탭까지 열어 보면 셋이 맞물린다:
  1. **수출 발주를 준비하는 계산지** — 사람 탭(`나영(FBI)`·`민재(US)`·`훈`·`Yoona`·`다운`)과
     `제품정보` 탭이 **SKU별 발주수량 → 박스수량·예상무게** 를 계산한다.
     `제품정보` 헤더: `ERP코드 | 영문명 | 바코드 | HSCODE | 내입수량 | 아웃박스 무게 |
     발주수량 | 박스수량 | 예상무게`. 이건 `export_logistics` 의
     `quantity_ea`·`carton_count`·`weight_kg` 와 **같은 일의 앞단**이다
     (OP 는 *무엇을 얼마나 실을지 계산*, LOG 는 *실제로 나간 기록*)
  2. **같은 사람** — LOG `op_manager` 7명 중 5명(나영·다운·민재·배훈(훈)·윤아(Yoona))이
     OP 시트에 자기 탭을 갖고 있다. 같은 팀이 두 일을 한다
  3. **같은 현지창고** — LOG `local_warehouse_received_date` 가 찍힌 국가 상위가
     미국(36)·인도네시아(24)인데, OP 시트 창고 컬럼에
     `[현장] CG ETC(미국)`·`[현장] SK_FAST BEAUTY(인도네시아)` 가 있다.
     **LOG 는 "언제 도착했나", OP 는 "지금 얼마 있나"** — 같은 창고의 다른 국면이다
- ⛔ **개인 작업지는 학습하지 않는다** (2026-09-03 사용자 지시: *"개인 작업지는
  학습하지마"*). 위 1번의 계산지들이 그것이다 — 탭마다 형식이 다르고
  (`나영(FBI)` 는 바코드+`_Z` 접미, `민재(US)` 는 `_R`/`_ZR`/`_MC` 열),
  `제품정보` 탭에는 *"사용 후 꼭 지워주세요!!"* 라고 적혀 있다. **누가 값을 넣었다
  지웠다 하는 임시 칸**이라 적재하면 조용히 낡거나 조용히 비는 데이터가 된다 —
  에러가 아니라 오답이 된다
  - 보증은 코드가 한다: `inventory.ALLOWED_TABS` **세 탭**(`SK/HQ_NEW` ·
    `ERP 코드 통합 재고현황` · `유통기한`)만 읽고, `_fetch()` 가 그 밖의 탭이면
    **왜 막혔는지 적어서** 거절한다 (`_RANGES[tab]` 의 KeyError 에 기대지 않는다)
  - ⚠️ **탭 이름을 코드에 두지도 마라** — 있으면 다음 사람이 "읽어도 되나 보다" 한다.
    회귀가 15개 탭 이름이 소스에 없는지까지 검사한다
  - 필요해지면 **HSCODE 같은 안정된 축만** 별도 마스터로 받는다 (개인 탭 말고)
- ⟶ **그래서 진짜 위험은 중복 집계가 아니라 질문이 엇갈리는 것이다.**
  두 도메인이 `출고`·`물류`·`입고`·`발주` 를 함께 쓴다:

    | 질문 | 가야 할 곳 |
    |---|---|
    | "인도네시아 **창고에** 재고 얼마나 있어" | `@@OP` (`inventory` 경로) |
    | "인도네시아**로 나간** 수출 건수" | `@@물류` (BigQuery) |
    | "유통기한 임박 품목" | `@@OP` (`expiry_intent` 가 **재고보다 먼저** 판정) |

  회귀가 **양방향**으로 지킨다 (`tests/test_logistics_source.py` 6절)
- ⛔ **제품별 수출 물량은 답할 수 없다** — SKU 컬럼이 없다. 프롬프트가 못 박고 있다.
  ⚠️ `memo` 에서 제품명을 뽑아 만들어 내지 마라 — 거기 든 것은 업체번호·PO번호다
  (실측 표본: `업체넘버_REF 3008` · `PO-033897` · `출고 요청하기`)
- ⛔ **`amount` 를 매출로 쓰지 마라** — 수출신고/인보이스 금액이고 통화가 섞여 있다.
  매출은 `SALES_ALL_Backup.Sales1_R` 이다

### ⛔ `order_team` 은 **세 번째 값 체계**다 — 조용한 오답의 본체

`Team_NEW` 코드도, promotion 의 소문자·하이픈도 아니다. 사람이 손으로 적어 넣은
값이 그대로 쌓여 **한 팀이 최대 4가지 표기**로 흩어져 있다 (실측 30종):

| | |
|---|---|
| 영업1팀 | `영업1`(934) · `영업1팀`(328) · `B2B1`(2) |
| 서구권마케팅팀 | `GMWM`(65) · `GM WEST MKT`(5) · `서구권마케팅팀`(11) |
| 브랜드커뮤니케이션팀 | `BCM_플래그십 파트` · `BCM_플래그십` · `BCM_플` · `브랜드커뮤니케이션팀_플래그십` |

- ⛔ **`order_team = 'B2B1'` 은 2건을 준다. 실제 영업1팀은 1,264건이다.**
  **0건이 아니라 그럴듯한 소수**라 에러도 안 나고 티도 안 난다 — 국가·팀·프로모션
  리터럴과 같은 방식으로 **코드가 보증한다**
  - 입력: `sql_agent._localize_logistics_literals()` 가 `order_team='영업1팀'` →
    `order_team IN ('영업1','영업1팀','B2B1')` 로 편다 (생성·재생성 4경로 전부)
  - 프롬프트: `build_logistics_team_section()` 이 표와 `CASE` 를 **코드 상수에서
    생성**한다 (`{{LOG_TEAM_SECTION}}`). ⛔ 프롬프트에 같은 표를 또 적지 마라
  - ⚠️ **`CASE` 의 `ELSE '미분류'` 를 지우지 마라** — 새 표기와 두 팀이 함께 적힌
    값(`'서구권이커머스팀, 서구권마케팅팀'`)이 **조용히 사라지는** 대신 보여야 한다
  - ⚠️ 새 표기 감시는 `{{VALUES:LogTeam}}`(매일 실측 주입)이 한다. 위 표에 없는
    값이 거기 보이면 그것이 새 표기다
- ✅ **국가(`country`)는 매출과 같은 한글 국가명**이다 (실측 124개) — 손대지 않는다

### 숫자는 겹치는가 — **두 군데서 겹친다** (2026-09-03 실측, 사용자 질문)

| | 값 (2026, 실측) |
|---|---|
| 물류 총 수출수량 (이상치 제외) | **58,038,175 ea** |
| OP 총 창고재고 | **57,347,838 ea** |

⚠️ **크기가 비슷해서 같은 숫자로 착각하기 쉽다.** 하나는 *1년치 흐름*, 하나는
*지금 이 순간의 잔량*이다. 우연히 비슷할 뿐이다.

**1) 같은 물건을 두 번 센다 (흐름 vs 잔량)** — 해외 현지창고에서만 겹친다:

| | 물류(2026 수출) | OP(현재 재고) |
|---|---|---|
| 인도네시아 | 6,001,001 | `SK_FAST BEAUTY(인도네시아)` **1,674,503** |
| 미국 | 7,262,493 | `CG ETC(미국)` **494,905** |

- 인도네시아로 600만 개를 실어 보냈고 그중 167만 개가 아직 현지창고에 있다 —
  **그 167만 개는 양쪽에 다 있다.** 더하면 이중계상이다
- ⚠️ 반대로 **OP 재고의 94%(`SF_B2B` 53.3M 등)는 국내 창고**라 물류와 전혀 안 겹친다.
  "OP 전체 재고" 와 "물류 수출량" 을 견주는 것은 그래서 의미가 없다
- 💡 구조적으로는 안전하다 — 저장소(BigQuery ↔ MariaDB)도 경로(`bigquery` ↔
  `inventory`)도 달라 **한 SQL 로 섞일 수 없다.** 위험은 답변 서술에만 있다

**2) `quantity_ea` 를 판매수량으로 읽으면 답이 갈린다** — 이게 더 위험하다:

| 2026 인도네시아 | 값 |
|---|---|
| 물류 `quantity_ea` (선적 수량) | 6,001,001 |
| `Product.Total_Qty` (판매수량) | 6,545,865 |

- ⛔ **수량 = 무조건 `Product.Total_Qty`** 라는 대원칙이 물류 때문에 흔들리면 안 된다.
  프롬프트가 못 박고, 답변은 **"수출 선적 수량"** 이라고 밝히게 했다
- 실측 라우팅은 정상이다: "판매수량" → `Product` / "수출한 수량" → 물류.
  **각각 맞는 테이블이지만 숫자가 다르므로, 어느 쪽인지 말하지 않으면 혼란이 된다**

### ⛔ 수량 칸에 주문번호가 들어간 행이 있다 — 총계가 3,492배 틀렸다 (2026-09-03)

프로덕션 채팅이 *"2026년 코스타리카 수출 물류 수량은 총 **202,602,265,193개**"* 라고
아무 경고 없이 답했다. 원인은 **한 행**이다:

```
2026-01-14 · 코스타리카 · order_number=202601120126 · quantity_ea=202602190028 · 1,265.75kg
```

- `quantity_ea` 에 **주문번호 형식(`YYYYMMDDnnnn`)의 값**이 들어갔다.
  2,026억 개가 1.2톤일 수 없다 (**160,064,934 ea/kg**, 정상 중앙값 6.7)
- 그 한 행이 2026 총수량을 **58,038,175 → 202,660,228,203 (3,492배)** 로 만든다
- ⛔ **우리가 고칠 것이 아니다 — 원본(물류관리 시스템)의 셀**이다.
  자동 수정은 하지 않는다 (데이터 값 교정은 되돌릴 수 없다). 대신 **두 겹으로 보이게** 한다:
  - 자가 점검 `logistics_quantity_outlier` — Admin 화면에서 매일
  - **답변에 공시** (`app/core/logistics_quality.py` · `notice_for_sql`) — 값은
    그대로 두고 사실만 적는다
- ⛔ **프롬프트로는 못 막았다.** *"억 단위가 나오면 이상치라고 답변에 적어라"* 를
  넣고 배포한 **뒤에도** 프로덕션이 *"총 202,602,265,193개 · 압도적인 수출 규모"* 라고
  답했다 (2026-09-03 실측). **프롬프트는 확률이고 보증은 코드다** — FI 마스킹·
  내부 경로 마스킹과 같은 사상이다
- ⛔ **표보다 먼저 말한다.** 처음엔 답변 맨 뒤에 붙였는데 후속 질문 제안·
  `<details>실행된 쿼리</details>` 뒤로 밀려 **정작 숫자를 읽는 사람 눈에 닿지 않았다.**
  지금은 답변 **맨 앞**이다 (OP 재고 신선도 공시와 같은 규칙: *사람은 표를 보지
  각주를 안 본다*). ⚠️ 스트리밍·비스트리밍 **양쪽에** 걸어야 한다
- ⚠️ **스스로 꺼진다** — 원본을 고치면 조회가 비고 공시도 사라진다. 영원히 뜨는
  경고가 아니다. ⚠️ 조회 실패 시에는 **공시하지 않는다** (안전한 쪽으로 실패:
  조회가 죽었다고 멀쩡한 답변에 경고를 붙이지 않는다)
- ⚠️ 임계는 `logistics_quality` **한 곳**에만 둔다 — 자가 점검도 같은 함수를 부른다.
  두 번 구현하면 화면과 답변이 언젠가 어긋난다
- 임계는 **실측 최대에서 크게 떨어뜨렸다** (매일 뜨는 경보는 곧 아무도 안 읽는다):
  한 선적 1,000만 개 초과(정상 최대 1,332,166 의 **7.5배**) 또는
  kg 당 10,000개 초과(정상 최대 392 의 **25배**). 오늘 걸리는 것은 이 1건뿐이다

### ⛔ `amount` 는 통화가 섞여 있다 — 그냥 더하면 뜻이 없다

`unit` 이 통화 코드다: `USD` 1,668 · `EUR` 97 · `KRW` 76 · `JPY` 18 · `CNY` 9 —
**그리고 통화가 비어 있는 행이 384건(15%)** 이다. 빈 행도 한 통화가 아니다
(콜롬비아 17,434 ≈ USD · 일본 23,964,000 ≈ JPY 가 같이 들어 있다).

- 금액을 물으면 **`GROUP BY unit` 으로 나누고 통화 미상 건수를 답변에 적는다**
- ⛔ **`cost_total_krw` 에 부분비용을 더하지 마라** — 부분합(`forwarder_cost_krw` +
  `duty_vat_krw` + …)과 합계가 맞는 행은 1,660건 중 696건뿐이다
  (`Production_Cost2` 가 FOC 를 이미 포함하던 것과 같은 함정)

### 한화 환산 = 사내 **월말환율** · 그 전에 물류비를 금액이라 부른 사고 (붐따 #162)

붐따 #162 (정다운 제보): *"수출 자료 다운로드시, 한화로 변환이 안되는것같습니다!"*
확인해 보니 **변환이 안 된 것이 아니라 엉뚱한 값이 한화 금액 행세를 한 것**이었다.

    질문: "금액은 한화로 다 바꿔줘"
    SQL : SUM(cost_total_krw) AS total_export_amount_krw,  'KRW' AS currency
    제목: 「담당자별 한화(KRW) 기준 수출 물류 실적」

`cost_total_krw` 는 **물류비**(운임·관세·통관 수수료)이지 수출 금액이 아니다.
게다가 거의 비어 있다 — 실측(2026-08, 삭제 제외 385건): 금액(유상+무상) **302건** ·
`cost_total_krw` **14건(3.6%)**. 그래서 미국 65건·캐나다 44건이 전부 `null` 로 나갔고,
인사이트는 그 null 을 **"원화 정산 처리가 완료되지 않았을 가능성"** 이라고 지어냈다.
CSV 도 같은 값이었다. **에러는 없었다.**

- ⛔ **프롬프트에는 이미 "환율 환산도 하지 마라" 가 있었다.** LLM 은 그 말을 지키면서
  **다른 컬럼으로 우회**했다 — 금지어를 늘리는 방식으로는 끝나지 않는다.
  보증은 `app/core/logistics_amount.py` 의 `krw_notice()` 가 한다 (스트리밍·비스트리밍
  **양쪽**에 건다). 두 방아쇠는 서로 다른 것을 잡는다:
  1. `wants_krw_conversion(질문)` — 환산을 요구받았다 → 「환산해 드리지 못했습니다」+이유
  2. `cost_labelled_as_amount(SQL)` — **물류비에 금액이라고 주장하는 별칭**이 붙었다
     → 「이건 수출 금액이 아니라 물류비」. ⚠️ 별칭이 `cost`·`물류비` 면 정상이라 안 뜬다
- ⚠️ **통화어만으로 켜지 마라** — "통화가 KRW 인 건만" 은 환산 요청이 아니다.
  바꿔 달라는 **동사**(환산·변환·바꿔·통일·맞춰)가 함께 있을 때만 참이다.
  매번 뜨는 경고는 곧 아무도 안 읽는다

#### 환산은 `Sales_Integration.Exchange_Rate` 로 한다 (2026-09-07 연동)

2026-09-06 사내 환율표가 올라와 **실제 환산이 된다** (사용자: *"환율은 여기서
가져와 근데 이건 월말환율이야"*). ⚠️ 그전에 여기 적혀 있던 *"환율 데이터가 없어
환산할 수 없다"* 는 **더 이상 사실이 아니다** — 되살리지 마라.

실측(2026-09-07): 13통화 × 82개월(2020-01~2026-10) = 1,066행 **결측 없음** ·
물류의 모든 통화를 100% 덮는다 (USD 1,693 · EUR 96 · JPY 19 · CNY 9).

- 조인식·환산식은 **`app/core/logistics_fx.py` 한 곳**에서 만든다
  (`{{LOG_FX_SECTION}}` 로 프롬프트에 주입). ⛔ 프롬프트에 같은 식을 또 적지 마라
- ⛔ **`fx_rates`(출근 브리핑)와 섞지 마라 — JPY 단위가 다르다.** 저쪽은 **100엔**,
  이 표는 **1엔**(8.5927)이다. 섞으면 **에러 없이 100배** 틀린다
- ⛔ **`LEFT JOIN` 이다.** `JOIN` 이면 통화가 비어 있는 건(2025-12 이후 626건 =
  **24.9%**)이 **행째로** 사라져 금액뿐 아니라 **건수까지** 조용히 줄어든다.
  환산 못 한 건수를 함께 세어 답변에 적는다 (`UNCONVERTED_EXPR`)
- ⛔ **환율표에 `KRW` 행이 없다** — 원화 건은 `IF(unit='KRW', 1, fx.rate)` 로 둔다.
  안 그러면 원화 매출이 통째로 빠진다
- ⛔ **이번 달과 그 이후는 확정 환율이 아니다.** 월말환율은 그 달이 끝나야 정해진다 —
  실측: 82개월 중 직전 달과 값이 **완전히 같은** 달은 2026-09·10 둘뿐이고 8월 값이다
  (안 끝난 달을 직전 확정치로 채워 둔 것). 하필 제보자가 물은 것이 **9월 ETD 건**이었다.
  ⚠️ 판정은 **날짜로** 한다(`confirmed_through`) — 표를 조회해 "값이 같으면 미확정"
  으로 보면 환율이 진짜 안 변한 달을 오판하고, 조회가 죽으면 공시도 죽는다
- **기준월 = 발주일(`order_date`)의 달** (`BASIS_COLUMN`). ⚠️ 조회가 기간을 잡는
  날짜와 **같은 날짜**여야 한다 — 발주일로 걸러 놓고 출항일 환율로 환산하면 표 안에서
  숫자가 어긋나고 그 어긋남은 티가 안 난다. 회계 기준이 수출신고일이면 이 상수만 바꾼다
- **환산은 요청받았을 때만** 한다. 기본은 통화별 원값이다 (기본을 바꾸면 기존 답변이 전부 바뀐다)
- 배선: `config.allowed_tables` · `_source_table_map["물류"]`(⚠️ 빠지면 "허용되지 않은
  테이블" 인데 **사용자 눈에는 조회 실패로 보인다**) · `safety._MONITORED_TABLES["환율"]`
- 회귀: `tests/test_logistics_fx.py` 15문항
- ⛔ **표보다 먼저 말한다** — 답변 맨 앞이다 (물류 수량 이상치 공시와 같은 자리)
- ⚠️ 같은 대화에서 하나 더 나왔다: 합계는 안 냈는데 표에 **`$8,322,054 (약 832만원)`**
  처럼 **달러 값에 원화 표기를 붙였다.** 환산이 아니라 **라벨이 거짓말**이다 —
  `_amount_note()` 의 통화혼재 분기가 "외화 금액에 '원'·'만원'·'억원' 을 붙이지 마라"
  를 함께 말한다 (여긴 아직 프롬프트다 — 코드 보증이 아니라는 것을 잊지 마라)
- 회귀: `tests/test_logistics_amount.py` 의 「한화 환산」 절 9문항

### ⛔ 통화가 섞인 표에서 금액을 더하지 않는다 — 코드가 막는다 (2026-09-03)

배포 직후 프로덕션 실측에서 바로 나왔다. "통화별로 나눠서" 라고 물어 **표는 맞게**
나왔는데, 코드가 만든 `합계 (조회 결과 기준)` 가 USD 90,992,472 + KRW 17,449,032,193
+ JPY 571,831,029 … 을 그냥 더해 **26,677,833,361** 을 찍었고, 요약이 그걸 받아
**"총 수출 금액 약 266.8억원"** 이라고 썼다. 달러와 엔과 원을 더한 숫자다.

- **표가 옆에 맞게 있어서 오히려 더 믿긴다** — 가장 나쁜 형태의 조용한 오답이다
- `_mixed_currency_axis()` 가 통화 축을 찾으면 `_additive_totals()` 가 **금액 컬럼만**
  뺀다. 건수·수량은 통화와 무관하게 더해도 되므로 그대로 둔다
- ⚠️ **이름만으로 판정하지 않는다** — `unit` 은 '단위' 라는 뜻으로도 쓰인다.
  값이 **통화 코드처럼 생겼고(3글자 대문자) 두 종류 이상**일 때만 통화 축으로 본다
  (`박스`/`개` 표는 건드리지 않는다. 회귀가 양방향으로 지킨다)
- ⚠️ **뺐으면 뺐다고 적는다** — 합계 블록이 그냥 사라지면 그것도 조용한 실패다
- `_amount_note()` 는 이때 **합계를 주지 않고 "더하지 마라" 고 말한다.** 코드가 안 주면
  LLM 이 스스로 더한다 — 실제로 그렇게 나갔다. ⛔ 환율 환산도 금지다 (환율 데이터가 없다)
- 💡 이 방어는 LOG 전용이 아니다 — **통화 컬럼이 있는 모든 표**에 듣는다

### ⛔ 옛 컬럼 사본이 20개 있다 — **값이 같아서 더 위험하다**

`sb_no` `order_no` `erp_no` `cnee` `buyer_name` `bl_no` `log_owner` `op_owner`
`sales_team` `transport_method` `export_declaration_no` `container_20ft`
`container_40ft` `plt` `carton` `quantity` `departure_date` `work_complete_date`
`logistics_complete_date` `unit_price` `total_amount`

- 실측으로 **전 행 일치**를 확인했다 — 잘못 골라도 결과가 나오므로 오답이 안 보인다
- 게다가 **날짜가 STRING 으로 뭉개진 사본**이 있다(`departure_date` 등) — 정렬·
  기간 비교가 문자열로 돌아 조용히 틀린다
- `unit_price` 는 이름과 달리 **단가가 아니라 통화 코드**다 (`unit` 과 동일)
- `total_amount` 는 41%만 채워졌고 `amount` 와 절반 이상 다르다
- `updated_by` `forwarder_note` `remittance_note` `sensitive_note` `etc_note` 는 **전부 빈 값**

### ⚠️ 그 밖에 조용히 틀리는 곳

- **`WHERE NOT is_deleted` 를 항상 건다** (삭제분 40건)
- **`op_manager` 는 같은 사람이 두 이름**이다 — `나영`/`안나영` · `다운`/`정다운` ·
  `주영`/`서주영` · `민재`/`정민재` · `윤아`/`정윤아`. 담당자별로 세면 한 사람이
  두 줄로 쪼개진다. 묶었으면 **묶었다고 답변에 적는다**
- **`forwarder` 는 390종에 표기 혼재** — `머스크`/`MAERSK`/`한국머스크물류서비스`,
  `DHL`/`디에이치엘글로벌포워딩코리아` 가 따로 센다. 순위표는 그 사실을 밝힌다
- **미래 날짜가 정상이다** — `eta` 가 오늘 이후인 행이 266건(운송 중). 상한을 걸지 마라
- **합적(`combined_shipment_id`)** — 371건이 101묶음으로 함께 나간다. 행 수는
  발주 건수이지 선적 건수가 아니다
- **`local_warehouse_received_date` 는 4.4%만 있다** — "현지 입고까지 며칠" 을
  이걸로 내면 대부분이 빠진다. 낼 거면 **모수를 함께 적는다**
- `status` 는 `종결`(1,700)·`진행중`(827) 둘뿐이다

### ⛔ 낱말 충돌 — 라우팅에서 실제로 물린 것들

- **`eta`·`etd` 를 맨 낱말로 키워드에 넣지 마라.** `meta`(메타 광고)·`retail`·
  `beta` **안에 그대로 들어 있고**, `_GUARDED` 낱말 경계 방어도 못 막는다 —
  `textmatch.standalone()` 은 **앞 글자가 한글일 때만** 본다. 라틴 낱말 안에서는 무력하다
- `발주`·`출고`·`수출` 은 단독으로 쓰지 않고 **물류 축 낱말과 튜플(AND)** 로 건다 —
  "발주 수량"·"수출 매출" 은 매출/제품 질문이다
- **재고는 여기가 아니다** — 창고 보유 수량은 `@@OP`(`inventory` 경로)다.
  두 도메인이 '출고'·'물류' 를 함께 써서 회귀가 **양방향**으로 지킨다
- 프롬프트의 *"재고/현재고/입출고/창고/물류/발주 데이터는 어떤 테이블에도 없다"*
  는 이제 **틀린 문장**이라 고쳤다 (현재고만 남겼다). 되살리지 마라 —
  골든셋 `log_routing_not_missing_data` 가 감시한다

### 회귀·감시

- `tests/test_logistics_source.py` — 배선 7곳 + 표기 흩어짐 + 낱말 충돌 (42문항)
- 골든셋 3문항(daily): `log_team_spelling_scatter`(789건) ·
  `log_amount_currency_mixed`(USD 1,142건) · `log_routing_not_missing_data`(해상 930건)
- System Status 카드 `LOG` + `_MONITORED_TABLES` 신선도 감시 + `schema_watch`
  (`Export_control` 데이터셋 추가)
- ⚠️ 값 목록(`LogTeam`·`LogIncoterms`·`LogTransport`)은 `value_lists_daily`(03:50)가
  채운다 — **배포 직후에는 비어 있다.** `python -c "from app.core.value_lists import
  refresh; refresh()"` 로 즉시 채울 것

## 노션 저장 — 쓰기는 다른 토큰·다른 API 버전이다 (2026-09-07)

- **파일**: `app/core/notion_export.py`(엔진) · `app/core/notion_save.py`(관문) ·
  진단 `scripts/notion_write_probe.py`
- ⛔ **쓰기 헤더는 `2025-09-03` 이다.** 옛 버전(`2022-06-28`)의 `parent: {database_id}` 는
  **단일 소스 DB 에서만** 행 생성이 된다 — 사용자가 그 DB 에 데이터 소스를 하나 더
  붙이는 순간 저장이 실패한다. 읽기 경로(학습·제품정보)는 옛 버전 그대로 둔다
- ⛔ **토큰을 학습용과 섞지 마라.** `NOTION_WRITE_TOKEN` 은 쓰기 전용 인테그레이션이다.
  크롤러 토큰이 개인 페이지 쓰기 권한을 갖는 구조를 만들지 않는다.
  ⚠️ `.env` 는 배포에서 제외된다 — WAS 에서 직접 넣고 재기동해야 한다
- ⛔ **사용자가 만든 DB 의 스키마를 고치지 마라.** `type == "title"` 인 속성만 이름과
  무관하게 찾아 채우고, 나머지는 이름·타입이 맞을 때만 채운다. 모르는 속성을 보내면
  노션은 400 을 준다. 못 채운 것은 답변에 적는다
- ⛔ **DB 를 매번 만들지 마라** — 순서는 ① 캐시(`user_notion_databases`)
  ② 부모의 자식 DB 중 제목이 `셀라` ③ 없으면 생성. ②가 있어야 사용자가 DB 를
  옮겨도 회복한다
- ⛔ **`<details>실행된 쿼리</details>` 를 반드시 걷는다** — 노션에서는 평문으로
  펼쳐져 **내부 테이블 경로가 페이지에 남는다.** 노션 페이지는 공유가 쉽다
- ⛔ **저장 요청과 문서 검색을 가른다.** `노션` 은 사내 문서 검색의 확신
  키워드이기도 하다 — 저장 **동사**가 함께 있고 검색 동사가 없을 때만 관문이 잡는다.
  회귀가 양방향으로 지킨다
- ⚠️ 관문은 **일반·스트리밍 두 경로 모두**에, 그리고 `@@` 소스 판정보다 **앞에** 있다
  (`@@물류` 를 켠 채로도 저장돼야 한다). 정적 검사가 위치를 지킨다
- ⚠️ URL 만으로는 DB 와 페이지를 구분할 수 없다 — `GET /v1/databases/{id}` 로 물어본다.
  `?v=` 뒤에도 32자 hex 가 있으니 쿼리를 먼저 버릴 것
- ⛔ `*.notion.site`(외부 공개 사이트)는 인테그레이션으로 쓸 수 없다 — 영원히 404 다
- ⛔ **저장 실패는 `_do_save` 안에서 담는다 — 관문에서 `None` 을 돌려주지 마라.** 그러면
  저장 요청이 평소 라우팅으로 새어 노션 **검색** 결과가 나간다(저장하라고 했는데 문서
  목록이 온다). `except Exception` 은 반드시 `except NotionError` **뒤에** 둔다 —
  앞에 두면 404·403·400 이 전부 일반 문구로 뭉개져 에러 분류가 통째로 무의미해진다

### 브리핑 자동 배송 (2단계, 2026-09-08)

- **파일**: `app/core/notion_briefing.py`(대상·대기열·발송) · `personal_briefing._enqueue_notion`
  (적재) · `app/api/notion_briefing_api.py` · 잡 `notion_push_halfhourly`(매시 :05·:35)
- ⛔ **대기열을 잔디와 합치지 마라.** 잔디 대기열은 DB_PC 릴레이가 꺼내 간다 —
  섞으면 릴레이가 노션 건을 웹훅으로 쏜다. 노션은 WAS 가 직접 쓰므로 릴레이가 없다
- ⛔ **절 목록·절 끄기·도착 시각은 `jandi_briefing` 것을 import 해서 쓴다.** 사본을
  만들면 한쪽만 고쳐져 조용히 갈린다 (이 저장소가 반복해서 겪은 실패다)
- ⛔ **본문 렌더러는 하나다** (`work_briefing.render_markdown`). 노션용을 따로 만들지 마라
- ⛔ `NOW()` 로 도착 시각을 비교하지 마라 — DB `time_zone` 이 `SYSTEM` 이다.
  `now_kst()` 를 파라미터로 넘긴다
- ⚠️ 입구가 둘이다 (첫 화면 `노션으로 받기` · 채팅 "브리핑 매일 노션에 넣어줘").
  **저장되는 테이블은 하나**라 상태가 갈리지 않는다
- ⚠️ 토큰이 없으면 `push_pending` 은 **대기열을 건드리지 않는다** — 실패로 태우지
  않고 그대로 둔다. 나중에 켜지면 밀린 것이 그대로 나간다
- ⚠️ 자가 점검 `notion_push` 는 **도착 시각이 지난** 대기만 센다. 18:30 을 고른
  사람의 07:30 산출물을 밀린 것으로 세면 매일 경고가 뜬다
- ⛔ **`mark_failed` 의 SET 절 순서가 의미를 바꾼다.** MySQL 은 단일 테이블 UPDATE 의
  SET 을 왼쪽부터 평가하고 **뒤 절이 앞 절의 새 값을 본다** — `attempts=attempts+1` 을
  먼저 쓰면 `status=IF(attempts+1 >= 상한)` 이 `old+2` 를 검사해 **재시도가 한 번 일찍
  끝난다.** 그래서 `status` 를 먼저 계산한다. ⚠️ 보기 좋게 정리한다고 순서를 바꾸지 마라
  (⚠️ `jandi_briefing.mark_failed` 에는 같은 오프바이원이 **아직 남아 있다**)
- ⛔ **장부 기록(`mark_sent`·`mark_failed`·`_remember_*`)까지 루프 안에서 감싼다.**
  DB 가 잠깐 흔들렸다고 예외가 밖으로 나가면 **뒤에 남은 사람들 브리핑이 전부 발이 묶인다.**
  ⚠️ 대신 그 행은 다음 회차에 다시 시도되므로 노션에 같은 브리핑이 한 번 더 생길 수 있다
- ⛔ **`send_at`·`muted` 는 `None` 이면 바꾸지 않는다** (`IF(%s, VALUES(...), ...)`).
  시각을 빼고 저장하는 요청이 사용자가 골라 둔 회차를 되돌리면 안 된다 — 채팅 등록이
  `set_target(user_id, url)` 로 부르기 때문에 실제로 그럴 뻔했다
- ⚠️ **가짜 DB 로 SQL 문자열만 보는 테스트는 SQL 의 뜻을 못 잡는다.** 이 계획에서 그
  틈으로 결함이 세 번 지나갔다 — 파라미터는 **순서까지** 고정하고, 조건부 플래그가 둘
  이상이면 서로 다른 값이 되는 호출로 **자리**를 검증하라

## OP 재고 — 시트를 표로 적재하고 조회로 답한다 (2026-08-25)

- **파일**: `app/core/inventory.py` / **테이블**: `op_inventory` (MariaDB) / **소스**: `@@OP`
- 원본은 OP팀 구글 시트 `1eU6ZHgx…` 탭 `SK/HQ_NEW` (헤더 2행, 데이터 3행부터)
- ⛔ **벡터로 넣지 마라.** 요청은 "벡터로 추가"였지만 시트가 `SKU | 품목명 | 창고별 수량`
  표라 벡터가 맞지 않는다: 숫자는 임베딩에 거의 담기지 않고(`104` 와 `1,040` 이
  구분되지 않는다), 한 청크에 SKU 수십 개가 섞이며, 매일 바뀌는 값이라 하루만 지나도
  **틀린 재고를 자신 있게** 답한다. 성분(`ingredients.py`)과 같은 사상 —
  재고는 틀리게 답하는 것이 못 답하는 것보다 나쁘다
- ⛔ **답변은 조회할 때마다 시트를 직접 읽는다** (2026-08-25 사용자 지시).
  하루 두 번 받아 둔 사본으로 답하면 그 사이 입출고를 모른 채 옛 숫자를 자신 있게
  말한다. 적재본(`op_inventory`)은 **시트를 못 읽을 때의 폴백**으로만 쓰고,
  물러섰으면 답변에 밝힌다 — 조용히 옛 숫자를 주는 것이 여기서 가장 나쁜 실패다
  - 실측(프로덕션·프록시 경유): 탭당 0.5~0.6초, 재고 답변 총 **1.5~2.3초**
  - ⛔ `with ThreadPoolExecutor` 로 감싸지 마라 — 타임아웃이 무력화된다 (`wait=False`)
- ⛔ **적재(폴백본)는 시트가 갱신된 **뒤**에 돌아야 한다.** 시트는 오전 10시경 갱신된다
  (안내문은 "1일 1회, 오후 2시 전후"). 처음에 04:10 에 걸어 **매일 전날 데이터를
  읽고 있었다** — 적재는 "성공" 하고 숫자만 하루 낡는다. 지금은 **11:20·16:20** 두 번
- **탭 세 개를 쓴다**: `SK/HQ_NEW`(주) + `ERP 코드 통합 재고현황`(+560 SKU) + `유통기한`
  - ⛔ 두 탭은 **같은 재고를 다른 창고 이름으로** 적는다. 표기를 통일하지 않으면
    겹치는 730 SKU 가 창고 둘로 갈려 **합계가 두 배**가 된다. 대응(`_ERP_LOCATIONS`)은
    눈이 아니라 **값으로** 정했다 — 창고별 수량을 전부 대조해 6개 모두 100% 일치하는
    짝만 채택했다 (`FBI` = `SK_FAST BEAUTY(인도네시아)` 는 이름만 봐선 알 수 없다).
    적재 결과가 산술로 맞는다: 19,906행 − 중복 4,380조합 = **15,526행 / 2,298 SKU**
  - ⚠️ **헤더 행을 숫자로 박지 마라.** 머리말 안내문이 한 줄만 늘어도 어긋나고,
    그때 나는 것은 에러가 아니라 **0건**이다 — `SKU.no`·`품목명칭` 이 있는 행을 찾는다
  - ⛔ **유통기한은 별도 테이블**(`op_inventory_expiry`)·별도 조회·별도 표다.
    로트 단위 잔량이라 창고 재고와 **세는 기준이 다르다** — 합산하면 같은 물건을
    두 번 센다. 답변에도 "더하면 안 된다" 를 적는다.
    질문 판정은 **재고보다 먼저** 한다 (둘 다 '재고' 를 신호로 쓴다)
- ⛔ **`SF 재고합` 컬럼은 적재하지 마라** — SF 계열만 더한 **부분합**이라 `SUM(qty)` 에
  함께 더해져 이중계상이 난다 (`Production_Cost2` 가 FOC 를 이미 포함하던 것과 같은 함정)
- ⛔ **`synced_at` 비교 전에 마이크로초를 버려라** (`microsecond=0`). MariaDB DATETIME 은
  초 단위라, 남은 마이크로초 때문에 **방금 넣은 12,166행을 그대로 지우고 '성공'** 했다.
  정리 대상이 이번 적재분 이상이면 아예 지우지 않는다(`cleanup_refused`)
- **검색은 낱말을 AND 로 건다** — 품목명이 `마다가스카르센텔라앰플100ml` 처럼 붙어 있어
  통째로 LIKE 하면 0건이다. 그래서 **품목에 없는 낱말이 하나만 껴도 통째로 0건**이 난다
  ("센텔라 앰플 재고 얼마나 남았어?" → `얼마나` 때문에 0건, 실측)
  - ⛔ 대응은 불용어 목록을 늘리는 게 아니다 (얼마나·남았어·몇 개·있나… 끝이 없다).
    **데이터에 물어본다** — 그 낱말이 든 품목이 하나도 없으면 제품 이름이 아니다
    (`usable_words`). 조사 잘림(`클레이`→`클레`)도 같은 자리에서 원형을 함께 물어 푼다
  - 뺀 낱말은 **답변에 밝힌다.** 안 쓴 말을 쓴 것처럼 보이면 안 된다 (드라이브 검색과 같은 규칙)
- ⛔ **재고 신호어에 `수량` 을 넣지 마라** — `판매수량`(BigQuery)을 가로챈다.
  수량은 무조건 `Product.Total_Qty` 다. 회귀를 **양방향**으로 걸어 둘 것
- **낡으면 표보다 먼저 말한다** (`freshness()`, 20시간). 시각을 각주로 적어 두는 것만으로는
  부족하다 — 사람은 표를 보지 각주를 안 본다
- 감시: `EXPECTED_JOBS["op_inventory_sync_daily"]` · System Status 의 `OP` 카드
- ⛔ **일반 명사는 불용어로 미리 빼라** — `usable_words` 는 데이터에 물어 판정하는데
  `제품`·`상품` 은 실제로 품목명에 들어 있다. "유통기한 임박한 제품 알려줘" 가
  품목명에 '제품' 이 든 **3건**만 찾아 온 적이 있다 (전체를 봐야 하는 질문이다)
- **제품어가 하나도 없으면 전체에서 많은 순**을 보여준다. 빈 목록을 돌려주면
  질문은 멀쩡한데 "품목을 찾지 못했습니다" 가 나간다
- ⚠️ 신선도는 **시트가 스스로 적어 둔 갱신 시각**으로 판정한다 (`parse_stamp`).
  우리 적재 시각이 아니다 — 적재가 제때 돌아도 시트가 안 갱신됐으면 숫자는 낡은 것이다

## 인증서류 찾기 (COA·MSDS) — 롯트로 찾고, 못 찾으면 못 찾았다고 한다

- **화면**: `/coa-finder` (`app/static/coa_finder.html` · `coa_finder.js`) /
  **판정**: `app/core/coa_finder.py` / **API**: `app/api/coa_finder_api.py`
- 엑셀 패킹리스트를 붙여넣으면 사용자 **본인 OAuth** 로 공유드라이브를 뒤진다
  (서비스계정을 쓰지 않는다 — 드라이브 권한을 그대로 따른다)
- ⛔ **LLM 을 부르지 않는다. 파일 선택은 규칙이 한다** — 성분 조회를 LLM 에 맡기지
  않은 것과 같은 이유다. 규제 문서에서 **틀린 파일을 주는 것은 못 찾는 것보다 나쁘다**
- ⛔ **넓혀 찾지 않는다.** 롯트가 파일명에 그대로 들고 앞뒤가 영숫자와 붙어 있지
  않은 것만 '찾음' 이다 — 경계를 안 보면 `FE161` 이 `FE1615` 에도 걸린다
- ⛔ **`COA(제품)` 열은 절대 '찾음' 이 되지 않는다** — 제품명으로만 찾은, **다른
  생산분**의 증명서다. 행 체크·전체선택으로 딸려 나가지 않고 칸마다 따로 고른다.
  서버도 이 종류는 클라이언트가 '찾음' 이라 주장해도 확정으로 받지 않는다
- ⛔ **'없음' 에는 근거를 적는다.** 화면에 '없음' 세 글자만 남으면 "정말 없다" 와
  "토큰이 죽었다"·"그 드라이브 멤버가 아니다"·"롯트를 잘못 적었다" 가 **글자
  그대로 똑같이** 보인다. 이 기능의 가치는 '없음' 을 믿을 수 있다는 것이다
- ⛔ **조회실패는 판정이 아니다.** '확인필요' 로 뭉개지 마라 — 그건 이미 다른
  뜻(ZIP 에 경고를 달고 나가는 항목)을 지고 있다

### ⛔ 제품명의 **용량**이 MSDS 를 통째로 못 찾게 하고 있었다 (2026-09-04)

사용자 제보: *"MSDS 공유파일에는 있는데 셀라는 못 찾아요"*. MSDS 는 **제형 단위**
문서라 파일명에 용량이 없는 것이 흔한데(`… FIRST AMPOULE(2026)`), 붙은 것도 있어서
(`… TONING TONER 210ml.pdf`) **어떤 제품은 되고 어떤 제품은 조용히 '없음'** 이 됐다.

    프로덕션 실측: `Hyalu-Cica First Ampoule 100ml` → 0건
                   용량을 빼면                      → 4건 (2026 판 포함)

- **용량은 요구 조건이 아니라 선호다** — 그대로 찾아보고 **0건일 때만** 용량을 빼고
  다시 찾는다. 처음부터 빼면 75ml 와 100ml 문서가 함께 걸려 여러건이 늘어난다
- ⛔ **한 낱말까지 풀지 마라** (`_MIN_TERMS_AFTER_RELAX = 2`) — `Ampoule` 하나로 찾으면
  온갖 MSDS 가 걸린다. 잡음은 답처럼 보여서 0건보다 나쁘다 (드라이브 검색과 같은 규칙).
  그래서 `Ampoule 100ml` 은 **넓히지 않는다**
- ⛔ **단위 없는 숫자를 용량으로 보지 마라** — `RETINOL 0.2` · `NIACINAMIDE 10` 의
  제품 고유어가 잘려 나간다. `_VOLUME` 은 단위(ml·l·g·kg·oz·ea)를 반드시 요구한다
- ⛔ **넓혔으면 답변에 밝힌다** — `용량(100ml)을 빼고 넓혀 찾았습니다`. 안 쓴 낱말로
  찾은 결과를 그대로 주면 사용자가 용량까지 맞는 문서라고 읽는다
- ⚠️ **제품COA 열에는 아직 같은 완화를 걸지 않았다.** 그 열은 다른 롯트의 문서라
  넓힐수록 위험이 커진다 — 필요해지면 같은 `drop_volume()` 을 쓰되 따로 판단할 것
- 💡 **이 증상은 "파일이 없다" 와 똑같이 생겼다.** 같은 제보를 받으면 먼저
  **드라이브에 직접 물어** 파일이 그 계정 눈에 보이는지부터 가른다 (추측 금지)

### 같은 LOT·품목은 최신 파일 1개 — 구분할 수 없으면 직접 선택 (2026-09-09)

- **COA는 LOT 정확 매칭, MSDS는 제품명 매칭을 먼저 통과해야 한다.** 검색된 후보 중
  구글 드라이브 최종 수정일이 가장 최근인 파일 하나를 반환한다. 파일명에 적힌 연도나
  검색 응답 순서로 최신을 추측하지 않는다. 수정일은 시간대까지 반영해 비교한다.
- **날짜가 비었거나 잘못됐거나 최신 시각이 같은 별개 문서라면 `여러건`을 유지한다.**
  선택할 수 없는 이유를 적는다. 최신 하나를 고른 경우에는 후보 수와 수정 시각을 표시한다.
- **사본 정리가 최신 파일을 먼저 버리면 안 된다.** 같은 이름·크기의 사본도 수정일이
  확인되면 최신 사본을 남긴다. 원래 후보의 누락된 날짜를 사본 정리로 숨기지 않는다.
- **짧은 LOT·접미 불일치·본문 검색·제품 COA는 수정일만으로 확정하지 않는다.**
  MSDS의 기존 용량 완화 조건과 안내도 유지한다.

직접 골라야 할 후보가 둘 이상 남은 칸에만 **파일마다** 체크박스를 단다.

- **안쪽이 진실, 바깥은 스위치다** — 파일 체크박스가 받을지를 정하고, 행·칸
  체크박스는 그것들을 한꺼번에 켜고 끈다. 반대로(둘 다 켜져야 받는 AND) 두면
  **행을 켰는데 아무것도 안 받아지는** 조용한 상태가 생긴다
- ⛔ **고를 것이 하나뿐인 칸에는 달지 않는다** — 선택지가 없는 자리의 체크박스는
  "이것도 켜야 받아지나" 하는 잘못된 질문을 만든다
- ⛔ **골라도 판정(`status`)은 그대로 실려 간다.** 사람이 파일명을 눈으로 보고
  고른 것이지 시스템이 확정한 것이 아니다. 이 값이 `_확인필요_목록.txt` 에 오를지를
  정하고, **이름에는 아무 표시도 없으므로 그 목록이 유일한 표시다**. 여기서
  `찾음` 으로 바꿔 보내면 확인되지 않은 문서가 목록에서 통째로 빠진다
- ⛔ **`.checked` 를 코드로 바꾸면 `change` 가 발생하지 않는다** — 전체선택(`#cf-all`)이
  행 체크만 켜고 파일 체크를 안 켜면 "선택했는데 안 받아진다". 에러도 콘솔 경고도 없다
- ⛔ **제품COA 후보는 행 체크·전체선택이 닿지 않는다** (`.cf-pick-file-product` 로 갈라
  둔다). 다른 롯트의 증명서라 이 열의 규칙이 그대로 유지돼야 한다 — 칸 체크박스나
  파일 체크박스로만 켜진다
- ⚠️ **체크하지 않아 빠진 건수를 화면에 적는다** — 스스로 끈 것이라도 선택보다 작아진
  ZIP 을 조용히 넘기지 않는다 (종류 필터가 걸러낸 건수를 적는 것과 같은 규칙).
  ⛔ 단 **제품COA 는 세지 않는다** — 기본이 꺼짐이라 매번 "N건 제외" 가 떠서 소음이 된다
- **후보 줄에 크기·수정일을 함께 적는다** (2026-09-03) — 열지 않고도 고르라고 붙인 값이다.
  `modifiedTime`을 `DriveFile.modified`로 전달한다. 같은 LOT·품목의 최신 선택에만 쓰며,
  **수정일만으로 불일치하거나 불확실한 문서를 확정하지 않는다.**
  ⛔ **없는 값을 지어내지 마라** — 구글 문서는 크기를 주지 않고 수정일이 비어 오는 파일도
  있다. 모르는 칸은 빼고 아는 것만 적는다 (0바이트·오늘 날짜로 채우면 조용한 거짓말이다)
- 회귀: `tests/frontend/test_coa_finder_downloads.py` 의 `MANY_ROWS` 절 11문항
  (⛔ 기존 `ROWS` 에 섞지 마라 — 다른 테스트가 그 행 수·파일 수를 세고 있다)

### ZIP 은 그대로 고객에게 가는 산출물이다

- **종류마다 폴더가 다르다**: `COA/` · `MSDS/` · `제품COA/` (2026-09-02 사용자 문의
  *"COA와 MSDS가 섞여서 다운 되는데, 따로 다운로드 가능할까요?"* — 이름으로는
  갈렸지만 한 폴더에 쏟아져서 받은 사람이 파일명을 읽어 골라내야 했다).
  `COA만 받기`·`MSDS만 받기` 버튼으로 한 종류만 받을 수도 있다
- ⛔ **파일 이름에는 판정을 적지 않는다** (2026-09-03 사용자 결정:
  *"파일명 앞에 확인필요_ 추가되는 것들.. 추가 안되게 할 수 있나용?"*).
  ZIP 이 그대로 고객에게 가는 산출물이라 한국어 접두가 파일 이름에 남는 것이
  문제였다. **경고는 `_확인필요_목록.txt` 하나로 모았다**
  - ⚠️ 그래서 **그 목록이 유일한 경고다.** 예전에는 이름·목록 두 겹이었고
    "파일 하나만 꺼내도 경고가 따라온다" 가 접두의 존재 이유였다 — 그 성질은
    이제 없다. 목록 쓰기를 건드릴 땐 이것이 마지막 방어선임을 기억할 것
  - ⚠️ 목록 머리말에 **"파일 이름에는 표시가 없습니다"** 를 적는다. 목록을 안
    열어 본 사람은 이름만 보고 전부 확정된 문서라고 읽는다 (회귀가 이 문구를 잡는다)
  - ⛔ `_is_confirmed` 는 그대로 둔다 — 이름을 정하는 판정이 아니라 **무엇을
    목록에 올릴지** 정하는 판정으로 계속 산다. 제품COA 가 클라이언트의
    `찾음` 주장으로 목록에서 빠지지 않는 것도 여전히 이 함수가 막는다
- ⛔ **자르는 것은 파일 이름만이다** (`_MAX_ZIP_NAME_BYTES`). 폴더까지 붙여 놓고
  자르면 긴 이름에서 폴더가 잘려 **그 파일만 조용히 다른 곳에 떨어진다** — 에러가
  아니라 "COA 폴더에 몇 개가 비는" 모양이라 찾기가 늦다. 255바이트 상한은 경로가
  아니라 **이름 성분**의 것이다
- ⛔ **MSDS·제품COA 이름에 그 행의 롯트를 붙이지 마라** — 롯트 없는 문서가
  롯트를 주장하면 롯트가 맞는 문서처럼 보인다
- ⛔ **제품COA 는 종류별 버튼 어디에도 안 딸려간다.** "COA 를 받겠다" 고 누른
  손에 다른 롯트의 문서가 얹히면 안 된다 — `선택 전체 받기` 로만 나간다
- ⚠️ **종류 필터가 걸러낸 건수를 화면에 적는다.** 선택보다 조용히 작아진 ZIP 이
  이 페이지가 막으려는 바로 그 실패다
- ⚠️ 아카이브 이름은 **내용물이 정한다** (`coa.zip`/`msds.zip`/`coa_msds.zip`).
  서버의 Content-Disposition 과 프론트의 저장 이름이 **같은 규칙**을 쓴다 —
  갈리면 파일 이름이 거짓말을 한다

### `COA 없는 목록` — '모른다' 를 '없다' 로 바꾸지 않는다

2026-09-02 사용자 문의: *"COA 자료 없는 품목/로트 리스트로 정리는 불가할까요?"*
`COA 없는 목록 받기`(CSV) + `없음만 보기` 토글.

- ⛔ **조회실패 행을 목록에 넣지 마라.** 조회가 끝나지 않은 것은 서류가 없다는
  뜻이 아니다 — 넣으면 사람이 그 목록을 근거로 **재발급을 요청하거나 있는 서류를
  없다고 보고한다.** 대신 건수를 **파일 끝에 적어** 경고가 파일과 함께 움직이게
  한다 (화면에만 있는 경고는 파일이 손을 떠나는 순간 사라진다)
- ⚠️ **필터는 화면만 가린다** — 받기 대상은 체크한 행 전체다. 필터가 받는 내용을
  조용히 바꾸면 그게 최악이라, 토글 옆에 그렇게 적어 뒀다
- ⚠️ **BOM 을 빼지 마라** — 엑셀이 한글을 깨서 열고, 그러면 내보내기가 고장난
  것처럼 보인다. ⛔ 회귀는 **바이트로** 읽는다: `Blob.text()` 는 규격상 BOM 을
  떼어내므로 그걸로 검사하면 **BOM 이 없어도 통과한다**
- ⚠️ 빈 파일을 답처럼 주지 않는다 — "없음이 없다" 와 "조회를 안 했다" 는 다른
  사실이라 문구도 갈린다

### 회귀

- `tests/test_coa_finder.py`(판정 규칙) · `tests/test_coa_finder_api.py`(ZIP·스트림
  + `node` 로 `planBatches`·`noneListCsv` 실제 실행) ·
  `tests/frontend/test_coa_finder_downloads.py`(Playwright 로 실제 클릭)
- ⛔ **핸들러에 `download` 를 바로 넘기지 마라** — 이벤트 객체가 `kindFilter`
  자리에 들어가 **모든 파일이 걸러져 0건**이 된다 (에러는 안 난다).
  ⚠️ 그 회귀를 설명하는 주석에 **그 호출 모양을 그대로 적지 마라** — 회귀가
  코드로 읽어 스스로 걸린다 (없는 CSS 변수를 주석에 쓰면 린트가 코드로 읽던 것과
  같은 부류. 실제로 한 번 걸렸다)

## CS/BP 제품 Q&A 캐시 — **매시 갱신한다** (2026-09-03)

- **파일**: `app/agents/cs_agent.py` (구글 시트 `[BD_BP] CS제품문의_모음집`, 13탭 ~900건)
- **소스**: `@@BP`(제품 Q&A · route `cs`) — ⚠️ `@@CS` 는 **다른 것**이다
  (CS팀 노션 문서 · route `notion` · Qdrant)
- **잡**: `cs_cache_hourly` 매시 **:40** (`EXPECTED_JOBS` 등록 · 여유 3시간)

⛔ **기동 시 한 번만 읽고 있었다** (2026-09-03 사용자 제보: *"CS DB와 제품정보에
대한 dB가 최신정보가 아닌거같은데"*). `warmup()` 호출부가 서버 기동 하나뿐이었고
스케줄 잡도 TTL 도 없었다 — **시트를 고쳐도 재기동 전까지 반영되지 않는다.**

    프로덕션 실측: 시트 최종수정 09-03 09:41 KST / 그 직전 재기동 09-02 16:18 KST
                  → 그 사이 17시간의 편집이 전부 안 보였다

⚠️ **에러가 아니다.** 옛 답을 자신 있게 내놓는 형태라 아무도 못 알아챈다 —
   이 프로젝트의 기본 결함 유형이다.

- ⛔ **`refresh()` 는 `warmup()` 을 그대로 쓰지 않는다.** 이유가 둘이다:
  1. `warmup()` 은 실패하면 `_cache_loaded=False` 로 내린다 → **다음 사용자 질문**이
     재로딩(수 초)을 떠안는다. 갱신 실패가 사용자 지연이 되면 안 된다
  2. 빈 목록으로 바꿔 끼우면 *"CS 데이터베이스가 비어있습니다"* 가 나간다.
     **옛 자료라도 있는 편이 낫다** — 그래서 비면 바꾸지 않는다
- ⚠️ **0건은 성공이 아니다** — 권한이 끊기거나 탭 이름이 바뀌면 그렇게 온다.
  `refresh()` 는 `-1` 을 주고, 잡은 그것을 **실패로 기록**한다
  (성공으로 적으면 자가 점검이 영영 못 잡는다)
- ⚠️ **기동 warmup 을 빼지 마라** — 매시 잡만 두면 기동 직후 첫 질문이 :40 까지 빈 캐시를 본다
- 자가 점검 `cs_cache` 가 **캐시 자체**를 본다 (잡이 돌았다는 것과 캐시가 채워져
  있다는 것은 다르다). ⚠️ 모듈 캐시라 **앱 프로세스 안에서** 돌 때만 뜻이 있다
- ⚠️ 프로덕션은 uvicorn **단일 프로세스**다(`--workers` 없음). 워커를 늘리면
  스케줄러를 가진 워커만 갱신되고 나머지는 낡는다 — 그때는 캐시를 프로세스 밖으로 빼야 한다

### 같이 확인한 것 — 이쪽은 정상이었다 (2026-09-03 실측)

| 무엇 | 상태 |
|---|---|
| `qdrant_pipeline_daily` (CS 노션 문서 05:00) | 3일 연속 성공 |
| `ingredient_sync_daily` (제품 전성분 04:00) | 3일 연속 성공 · 시트 84행 = 적재 84행 |
| `team_sync_daily` (01:00) | 3일 연속 성공 |

- ⚠️ `ingredients.py` 의 읽기 범위는 `AH400` 으로 **행 상한이 박혀 있다.**
  지금은 시트가 84행이라 문제가 없지만, 400행을 넘기면 **에러 없이 잘린다** —
  넘길 때 함께 고칠 것 (실측으로 7~3000행을 세어 84로 같음을 확인했다)

## 골든셋 회귀 — 2026-08-06

- **파일**: `app/core/golden_runner.py` / `data/golden_set.json` (50문항 — daily 31 · weekly 19)
- **실행**: 매일 05:30 (`golden_daily`), 일요일은 weekly 포함 전체. 화면은 Admin > `골든셋` 탭
- **데이터 규칙을 바꿨으면 골든 문항도 같이 추가한다.** 규칙은 프롬프트·후처리 여러 곳에 흩어져 있어
  "한 곳만 고치고 끝난 줄 알았던" 사고가 반복됐다 (팀 값 오기는 4곳 중 1곳만 고쳐져 있었다)
- ⛔ **기대 키워드가 실패 답변에도 들어가면 그 문항은 무용지물이다** — 2026-08-11 거짓 통과 실제 발생.
  "플랫폼" 같은 일반어는 되묻기 문장에도 나온다. **그 답변에서만 나올 값**(숫자·고유명사)을 기대로 쓸 것
- ⛔ 금지 문구(`banned`)에 정상 답변의 상투어를 넣지 마라 — "알려주세요" 는 모든 후속 질문 안내에 들어 있다
- 💡 **1초대 응답은 경로를 의심하라.** SQL 을 돌지 않고 되묻기로 빠졌을 때 그렇게 빠르다
- ⛔ **문항 질문이 되묻기를 유발하면 그 문항은 아무것도 검증하지 못한다.** 기간·범위를
  질문에 명시할 것. "우마 원가율 알려줘"는 기간을 되묻느라 규칙을 안 탔다 (run#22).
  되묻기 재발 감시는 `not_contains` 에 "언제인가요" 같은 되묻기 표현을 넣어 건다
- ⛔ **골든셋이 볼 수 없는 것을 골든 문항으로 감시하는 척하지 마라.** 골든셋은 사용자에게
  가는 **답변 문자열**만 본다. 보고서는 채팅에 요약 4줄만 돌려주므로 판정 임계값·필터
  추출·외부 절 방어는 여기서 관측되지 않는다 → `tests/test_report_rules.py` 가 맡는다
  (조회도 LLM 호출도 없는 순수 함수 검사라 빠르다). 골든은 **라우팅**만 본다:
  보고서를 만들어야 할 때 만들었는가 / 만들지 말아야 할 때 안 만들었는가
  - 판정 기준은 `/api/reports/` 와 "보고서를 만들었습니다" 두 문자열이다. **"보고서"라는
    낱말은 기대어로 쓰지 마라** — 일반 답변도 제목에 "매출 분석 보고서"라고 쓴다(실측)
  - 보고서 문항은 40~55초·조회 12회가 든다. **weekly 로 둔다**
  - 💡 이 회귀 테스트가 바로 빈틈을 하나 잡았다: `만드는\s*법` 만 있고 **"만드는 방법"이
    없어** "보고서 만드는 방법 알려줘"가 보고서를 만들고 있었다. 한 글자 차이다

## 자가 점검 (self-check) — 2026-08-04

- **파일**: `app/core/self_check.py` / **실행**: 매일 07:30 (APScheduler `self_check_daily`)
- **화면**: Admin > `자가 점검` 탭 (즉시 실행·추세 확인)
- **API**: `GET /api/admin/self-check`, `POST /api/admin/self-check/run`, `GET /api/admin/self-check/trend/{id}`
- **왜 만들었나**: AD 동기화가 **6일간 매일 밤 실패**했는데 아무도 몰랐다. 크론은 돌았고
  로그도 남았지만 읽는 사람이 없었다. `quality_monitor` 는 답변 품질만 본다 — 배치가 죽었는지,
  데이터가 썩었는지, 권한이 뚫렸는지는 아무도 감시하지 않았다.
- **검사 45종** (2026-09-02 실측 — 이 줄은 "23종 / static 6" 으로 오래 낡아 있었고,
  이틀 만에 또 낡았다. 손으로 센 숫자는 반드시 낡는다: `len(CHECKS)` 로 확인할 것):
  batch 6 / integrity 4 / permission 4 / datasource 8 / quality 5 / **static 18**
- **static 검사는 `app/core/static_checks.py` 가 판정한다** (2026-08-13 추가).
  개발 중에는 `tests/test_no_silent_failures.py` 가, 서버에서는 자가 점검이 **같은 함수**를
  부른다 — ⛔ **서버에는 pytest 도 `tests/` 도 node 도 없다.** 테스트만 만들어 두면
  매일 돌지 않는다. 같은 규칙을 두 번 구현하지도 말 것
  - node 가 필요한 검사(템플릿 스크립트 문법)만 개발 환경 전용이다
- ⛔ **`SKIN1004-Nightly-Debug` 는 2026-07-09 부터 멈춰 있다** (트리거 만료, 상태는 "Ready").
  로그·재기동·헬스체크가 전부 구서버를 향한다. 되살리려면 세 곳을 고치고 자동 적용
  유지 여부를 먼저 정할 것 — 상세는 `scripts/nightly_debug.py` 머리말.
  그 값이던 "로그 에러 읽기"는 `new_log_errors` 검사로 옮겼다 (신규 에러 **유형**만 본다)

- **원칙 — 새 검사 추가 시 지킬 것**:
  - 검사(`fn`)는 **부작용이 없어야** 한다. 고치는 것은 `repair` 로 분리
  - 자가치유는 **되돌릴 수 있는 것만**. DB 스키마 변경·삭제는 절대 자동화 금지
  - 활성 AD 계정의 `display_name` 은 치유 대상이 아니다 (다음 sync 가 덮어씀)
  - 치유 후 **재검사해서 정말 나았을 때만** 성공으로 기록
  - 알림은 **상태가 바뀐 것만** (정상→실패, 실패→정상). 매일 같은 알림은 곧 무시당한다
- ⚠️ `LIKE '%...%'` 를 파라미터 없이 쓰지 마라 — pymysql 이 `%` 를 포맷 지시자로 읽어 터진다.
  백슬래시 매칭은 `LOCATE(CONCAT(CHAR(92),'u'), col)` 처럼 회피할 것

### 배치 건강성은 부수효과가 아니라 실행 기록으로 판정한다

- **모든 스케줄 잡은 `track_job("<job_id>")` 으로 감싼다.** 실행 자체가 `job_runs` 에 남아야
  "할 일이 없어서 안 돈 것"과 "죽어서 못 돈 것"이 구분된다. 테이블에 행이 늘었는지로
  판정하면 한산한 밤을 고장으로 오탐한다 (2026-08-05 실제 발생)
- 새 잡을 추가하면 `EXPECTED_JOBS` 에 `(허용시간, 라벨)` 을 등록한다 — 그것만으로 감시 대상이 된다

### 서버별 프록시 허용 범위가 다르다 (2026-08-05 실측)

| | Jandi | Gemini/BigQuery/Claude/Notion | AD 172.16.1.13 (389·636) |
|---|---|---|---|
| **WAS** 10.1.150.5 | ❌ 403 | ✅ | ❌ **둘 다 닫힘** (2026-09-01 실측) |
| **APP** 10.1.150.105 | ❌ 403 (2026-08-18 실측) | ❌ | ✅ 둘 다 열림 (22:00 sync) |

- ⛔ **잔디는 이제 APP 에서도 막혀 있다** (2026-08-18 실측). 예전엔 여기에 "APP ✅" 라고
  적혀 있어서 "APP 경유로 알림을 우회하자"는 계획을 세웠는데, 실제로 찔러 보니
  **양쪽 다 403** 이었다. 프록시 화이트리스트에 `wh.jandi.com` 이 없다.
  → **어느 서버에서도 잔디 알림이 나가지 않는다.** 전달 경로는 Admin 화면과 로그뿐이다
  → `alert_channel` 검사가 이 상태를 매일 잡는다. 열려면 IT 요청이 필요하다
- ⚠️ `scripts/sync_ad_users.py` 의 잔디 알림은 `except: pass` 로 **완전히 조용히**
  실패하고 있었다 (로그에 흔적이 없어 오래 안 드러났다). 2026-08-18 에 원문을 남기도록 고쳤다
- ⛔ **WAS 는 AD 에 닿지 못한다** (2026-09-01 실측 — 로그인 지연을 쫓다 드러났다).
  `auth_api` 의 당일 입사자 자가 복구가 로그인 경로에서 **live AD resync 를 기다리고 있었고**,
  그것이 **136초** 걸렸다. 원인은 AD 조회가 느린 것이 아니라 **방화벽 타임아웃**이었다 —
  팀을 잘못 고른 사람이 "사용자를 찾을 수 없습니다" 한 줄을 보려고 2분 16초를 기다렸다
  (고친 뒤 **0.1초**). ⚠️ **그 자가 복구는 WAS 에서 한 번도 동작한 적이 없다** — 닿지를
  못하니 성공할 수가 없다. ⚠️ **IT 에 개방을 요청한다면 포트는 `636`(LDAPS)이다** —
  `sync_ad_users.py` 가 `use_ssl=True, port=636` 으로 붙는다. 389 만 열어 달라고 하면
  열려도 코드가 못 쓴다 (2026-09-01 실측: WAS 에서 389·636 **둘 다** 닫혀 있다).
  배선은 그대로 두되(요청 경로 밖이라 무해하고 LDAP 이 열리면
  그날부터 듣는다) **동작한다고 가정하지 마라.** 당일 입사자는 22:00 크론까지 기다린다
- 💡 **네트워크 전제는 문서를 믿지 말고 찔러 볼 것.** 이 표가 한 번 틀려서 없는 우회로를
  설계할 뻔했다. 위 AD 건도 같은 부류다 — 코드 주석은 "아마 못 닿을 것" 이라고 적혀
  있었고, 실제로 찔러 보고서야 확정됐다
- **지식맵 빌드는 Gemini 를 호출한다** (`app/knowledge_map/semantic.py`). APP 크론에 있었지만
  APP 은 Gemini 가 막혀 있어 구조적으로 돌 수 없었다(이관 후 매일 실패).
  **2026-08-05 WAS APScheduler(`knowledge_map_daily`, 03:00)로 옮겨 해결**했다 — 51초에 완주한다.
  ⚠️ APP 크론에 다시 등록하지 말 것 (이중 실행 + 어차피 실패)
- ⚠️ `deploy_new_server.py` 의 `EXCLUDE_DIRS` 는 **디렉토리 이름**으로 거른다. 이름이 겹치는
  소스 패키지가 통째로 빠지므로 최상위 산출물은 `EXCLUDE_PATHS` 에 쓸 것
  (`knowledge_map` 을 이름으로 걸러 `app/knowledge_map/` 이 배포에서 누락됐던 사고)

## 코드 규칙 — 재발 방지

- ⛔ **`ThreadPoolExecutor` 를 `with` 블록으로 감싸고 `future.result(timeout=N)` 을 쓰지 마라.**
  블록을 빠져나갈 때 `shutdown(wait=True)` 가 걸려 **타임아웃이 무의미해진다** —
  워커가 끝날 때까지 그대로 기다린다. 실제로 "차트 8초 넘으면 건너뛴다"는 로직이
  통째로 무력화돼 있었다 (2026-08-04 발견).
  ```python
  pool = concurrent.futures.ThreadPoolExecutor(max_workers=1)
  try:
      answer = pool.submit(fn).result(timeout=8.0)
  finally:
      pool.shutdown(wait=False)   # ← 반드시 wait=False
  ```
  스트리밍 경로(`run_sql_agent_stream` 등)는 원래 이 방식이다. 새 코드도 여기 맞출 것.
- **차트 축 라벨은 전치(transpose) 여부에 따라 의미가 바뀐다.** 전치하면 x축은 기간이 되므로
  원래 x축 제목(예: '국가')을 그대로 두면 축이 거짓말을 한다. 컬럼명을 눈금으로 쓸 때는
  공통 접두사뿐 아니라 **접미사도** 제거할 것 (`Q1 sales`/`Q2 sales` → `Q1`/`Q2`)
- ⛔ **구글 OAuth 콜백은 쿠키가 없는 호스트로 돌아온다** (2026-08-25 이해인 제보).
  구글이 원시 IP 리다이렉트를 거부해서 `_get_redirect_uri()` 가 `10.1.100.5` →
  `10.1.100.5.nip.io` 로 바꾼다. 그런데 **세션 쿠키는 호스트 전용**이라
  사용자가 보고 있는 `10.1.100.5` 에만 붙는다 → 콜백에 쿠키가 안 실린다.
  콜백에 로그인 요구(`get_current_user`)가 붙은 그날(커밋 `f1a3bbd`, 10:00)
  **새로 연결하려는 사람이 전부 401** 을 받았다
  - ⚠️ **이미 연결된 사람은 토큰이 갱신만 되므로 아무도 눈치채지 못한다.**
    화면엔 "연결됨" 이 그대로 뜨고, 실패는 새 사용자에게만 보인다 — 전형적인 조용한 실패
  - 대응은 **쿠키가 있는 호스트로 한 번 되돌려 보내는 것**이다 (`_browsing_host()` 가
    `_get_redirect_uri()` 의 역함수). 최상위 GET 이동이라 SameSite=Lax 쿠키가 실린다.
    ⛔ 되돌리기 단계에서 코드를 교환하지 마라 — 인증 없이 교환하면 남의 세션에
    계정을 붙일 수 있고, 그게 `f1a3bbd` 가 막으려던 것이다
  - ⚠️ 왕복은 **한 번만** (`session_hop=1`). 안 그러면 무한 왕복이다.
    되돌린 뒤에도 세션이 없으면 **읽을 수 있는 안내**를 보여준다 (원시 401 JSON 금지)
- ⛔ **`generate_json()` 에 `max_output_tokens` 를 넉넉히 줘라.** 4096 으로 하드코딩돼
  있어서 긴 응답이 잘렸는데, **`repair_json()` 이 괄호를 닫아 잘림이 정상 JSON 처럼
  보였다** — 문장이 중간에서 끊긴 채 통과했다 (2026-08-13 판단 절에서 실측).
  파싱 성공 = 응답 완결이 **아니다.** 서술을 받는 곳은 끝맺음도 함께 검사할 것
- **LLM 이 JSON 모드로 돌려준 응답도 깨질 수 있다.** Gemini 가 닫는 중괄호를 빠뜨려
  차트가 **하루 38건 조용히 사라지고 있었다** (2026-08-11 발견). `llm.repair_json()` 이
  문자열 밖 괄호를 세어 교정하고, `finish_reason != STOP` 이면 `gemini_json_incomplete` 로 남긴다.
  새로 JSON 파싱을 넣을 때 `json.loads(raw)` 를 맨살로 쓰지 말 것
- **실패를 삼키는 `except` 에는 원문을 남겨라.** 위 결함이 오래 안 잡힌 이유는
  파싱 실패 시 원문을 INFO 로만 찍었기 때문이다 — 프로덕션은 앱 INFO 를 통째로 버린다
  (structlog `filter_by_level`, `basicConfig(level=)` 미설정). **진단 정보는 WARNING/ERROR 로 남길 것**
- ⚠️ **`sql_cache` 는 0건을 내는 SQL 도 그대로 재생한다.** 캐시는 `validate_sql()` 통과 여부만 보고
  행 수는 보지 않는다. 프롬프트·리터럴 교정으로 원인을 고쳤으면 **해당 캐시 행을 지워야** 반영된다.
  "고쳤는데 그대로"인데 응답만 빨라졌다면 캐시 히트를 의심할 것 (2026-08-11 실제 발생)

- ⛔ **정규식의 `` 가 진짜 백스페이스 문자(0x08)로 들어가면 영영 매치하지 않는다**
  (2026-08-25). 에러 없이 컴파일되고 편집기에도 안 보인다. 실제로 `self_check.py` 의
  "답변 앞부분 유실 의심" 감지가 그렇게 죽어 있었다 — **붐따 #106("답변이 '니다.' 로
  시작")과 같은 증상을 잡으려던 검사가 정작 못 잡고 있었다.**
  `static_ctrl_chars` 가 매일 잡는다. 문자열을 스크립트로 생성해 파일에 쓸 때 특히 위험하다
- **차트 시리즈 라벨은 측정값을 가리켜야 한다** (2026-08-25, 붐따 #138).
  Chart.js 는 툴팁을 `<label>: <값>` 으로 그린다. 거기에 x축 이름("제품명")이 들어가
  **"제품명: 25,541,542,845"** 가 떴다. 코드가 LLM 의 `y_label` 을 그대로 믿었다 —
  라벨은 숫자가 아니라 **말**이라 틀려도 에러가 안 나고 표는 멀쩡해 보인다.
  `_series_label()` 이 측정값 낱말(매출·수량·비중·revenue·qty…)일 때만 쓰고,
  아니면 실제로 그린 컬럼명을 쓴다. y축 **제목**도 같은 함수로 막는다
- **YoY 차트는 정규화가 상한 검사보다 먼저다** (2026-08-25, 붐따 #145·#146).
  가독성 상한(행 25/36)이 먼저 돌아 정규화 전 긴 형식(대륙 2 × 월 20 = 40행)이 걸려
  `return None` 으로 **차트가 통째로 사라졌다.** `return None` 은 WARNING 만 남기고
  화면엔 아무 말도 안 한다. ⚠️ `apply_chart_intent` 에서 `group_column` 을 지우지 마라 —
  진짜 그룹 축(대륙·팀)이 사라져 어느 쪽 선인지 알 수 없게 된다
- ⚠️ **연도가 둘 보인다고 YoY 가 아니다** — "2025년 1월**부터** 2026년 6월**까지**" 같은
  기간 범위를 쪼개면 없던 비교가 생긴다. 범위 표현이 있으면 YoY 가 아니다

## 붐따(👎) 처리·회신 — 2026-08-14 (2026-08-25 보강)

- **처리함**: Admin > `붐따` 탭 / **회신**: 사이드바 알림함 (`/api/notifications`)
- ⛔ **회신은 나이와 무관하게 닿아야 한다.** `my_feedback()` 의 90일 창은 "묵은 **미처리**
  건이 목록을 채우는 것" 을 막으려던 것인데 **처리된 건의 회신까지 걸렀다.** 뒤늦게 처리
  표시를 달면 `handled_at` 만 채워지고 화면엔 아무것도 안 뜬다 — 실제로 17건 중 7건이
  그랬다. 조건은 "최근이거나, 처리됐는데 아직 안 읽은 것" 이다 (읽으면 다시 빠진다)
- ⛔ **처리함 기본 필터는 '전체' 다.** '미확인' 으로 두면 완료로 바꾼 항목이 목록에서
  **사라져** 처리됐다는 표시가 뜨지 않는다 — 사용자 제보: "처리해도 처리됐다고 안나옴".
  처리함은 처리한 결과가 보여야 처리함이다
- **뒤늦게 처리 표시를 달 때는 `scripts/backfill_feedback_status.py`** 를 쓴다.
  ⚠️ id 로만 UPDATE 하지 말 것 — 신고일을 대조해야 엉뚱한 사람에게 회신이 가지 않는다
- ⛔ **Windows PowerShell 파이프로 한글 회신을 Python에 넘기지 마라** (2026-08-26 #151).
  Windows PowerShell 5의 `$OutputEncoding` 기본값은 `us-ascii`라 한글이 DB에 닿기 전에
  리터럴 `?`로 바뀐다. 실제로 #151 회신 118자 중 82자가 `?`로 저장돼 사용자 알림이
  깨졌다. UTF-8 파일/인자를 쓰거나 ASCII 소스의 `\uXXXX` 이스케이프를 사용한다.
  `set_status()`는 물음표 비율이 비정상적인 메모를 DB 쓰기 전에 거절한다
- ⛔ **사람이 내린 판정(`ack`·메모)을 스크립트가 덮어쓰지 마라.** 처리함이 기록으로서
  값을 잃는다. 근거를 커밋·코드에서 **확인한 것만** `done` 으로 단다
- ⛔ **고쳤으면 그 자리에서 처리 표시까지 단다 — 수정과 표시는 한 작업이다**
  (2026-08-25 사용자 제보: *"붐따 계속 확인하고 있는데 했던거 계속하는 느낌이다.
  처리해도 처리됐다는 체크가 없어서 계속 그럼"*). 고치고 배포한 뒤 상태를 안 쓰면
  그 건은 **다음에 볼 때도 `new` 로 맨 앞에 온다.** 실제로 36건이 그렇게 쌓였고,
  그중 상당수는 이미 고쳐진 것이었다. 이제 수정 코드와 함께
  `app.core.feedback_inbox.DEPLOYED_FEEDBACK_RESOLUTIONS` 에 **id·신고일·회신 메모**를
  등록한다. 앱이 기동될 때 신고일까지 맞는 행만 `done` 으로 동기화하고, 이미 닫힌 행은
  건너뛴다. ⚠️ 전체 `ack` 를 자동 완료시키지 마라 — 해결을 명시한 항목만 등록한다.
  붐따 작업의 완료 조건은 **코드·회귀 테스트·해결 목록을 함께 배포하는 것**이다
- ⚠️ **코멘트 없는 👎 는 진단할 수 없다 — 처리 대기열과 섞지 마라.** 34건이 그렇고,
  그중 15건(#34~#57)은 `messages` 가 id 5332 부터만 남아 **대화 원문 자체가 없다**
  (2026-08-25 실측). 읽을 게 있는 건(코멘트 有)이 실제 대기열이다 —
  다이제스트가 `open_with_comment` 를 따로 세는 이유가 이것이다

## 만족도 설문(별점) — 접속일수 10일차부터 20일 간격으로 묻는다 (2026-09-02)

- **파일**: `app/core/satisfaction.py`(판정·저장) · `app/api/survey_api.py`(제출·되돌리기) ·
  판정 노출은 `auth_api./me` 의 `survey_prompt` · 화면은 `chat.js` 의 설문 모달 ·
  처리는 **붐따 처리함과 같은 화면**(`feedback_inbox.list_inbox`)
- **임계는 10·30·50·70…** (`MILESTONES`, 20일 간격). ⛔ 처음엔 10·50·100 이었는데
  간격이 벌어질수록 오래 쓴 사람이 몇 달을 조용히 지나간다 (2026-09-02 사용자 지시로 변경).
  ⚠️ 상한(3년치)은 **닿지 않을 만큼** 넉넉해야 한다 — 상한에서 멈추면 에러가 아니라
  "오래 쓴 사람에게만 조용히 안 뜨는" 상태가 된다
- ⚠️ **방문 횟수가 아니라 방문 "일수" 다.** 실제 문의가 있었다 — 방문 100회인 사람의
  접속일수는 60일이었다 (`user_visits` 13일 + 대화 58일의 합집합). 화면·문구에서
  "방문 N회" 라고 쓰지 마라
- **테스트**: 주소에 `?survey=test` → 답변을 기다리지 않고 바로 띄운다 ·
  `?survey=reset` → **내 기록만** 지우고 다시 띄운다 (`POST /api/survey/reset`).
  ⛔ 대상이 아니면 화면이 임계를 지어내지 않는다 — 지어낸 임계로 저장된 행은
  아무 뜻도 없는 데이터가 된다. ⛔ 되돌리기 대상은 **요청이 정하지 못한다**(본인 것만)
- **왜**: 붐따(👎)는 답변 하나가 틀렸을 때 눌린다 — "이 서비스가 쓸 만한가" 는 아무도
  답한 적이 없다. 오래 쓴 사람에게만 묻는다 (처음 온 사람은 답할 근거가 없다)
- ⛔ **방문일수를 `user_visits` 만으로 세지 마라.** 그 원장은 **2026-08-11 부터**라
  3주치뿐이고, 그것만 세면 **6개월 쓴 사람이 '10일차' 로 잡힌다** — 에러가 아니라
  조용한 거짓말이다. `conversations`(2026-03-10 부터)와 **날짜 합집합**으로 센다
  - 실측(2026-09-02, 프로덕션): 합집합 기준 10일차 **12명** · 50일차 **2명** ·
    100일차 0명(최대 60일). `user_visits` 만이면 10일차 4명 · 50일차 0명
- ⛔ **도달한 임계 중 가장 큰 것을 묻고, 그보다 작은 것은 함께 닫는다.** 작은 것을
  고르면 60일 쓴 사람에게 '10일차' 가 뜬다. 50일차에 답한 뒤 10 이 기록에 없다고
  다음 세션에 10일차를 물으면 **지나간 지점을 뒤늦게 묻는 오작동**이 된다
- ⛔ **바닥은 물어본 그때의 접속일수까지 올린다** (`asked_at_days`, 행의 `visit_days`).
  40일 쓴 사람에게 10일차를 물었다면 30일차도 이미 지난 지점이다 — 안 올리면
  방금 '나중에' 를 누른 사람에게 다음 접속에 또 뜬다 (임계를 20일 간격으로
  촘촘하게 바꾸면서 프로덕션에서 실제로 그렇게 됐다: 40일차 사용자 1명)
- ⛔ **코멘트는 별점과 무관하게 다 받는다** (사용자 지시). 다만 **처리 대기열에
  올리는 것은 3점 이하이거나 코멘트가 달린 것**뿐이다 — 읽을 게 없는 행이 대기열을
  채우면 대기열이 뜻을 잃는다 (코멘트 없는 👎 를 대기열과 섞지 않는 것과 같은 규칙)
- ⛔ **처리 동선은 하나다.** 설문을 별도 탭으로 갈라 두면 언젠가 한쪽만 읽힌다 —
  행마다 `source`(`thumbs`/`survey`)로 구분해 같은 목록·같은 상태·같은 회신을 쓴다.
  ⚠️ 상태 변경에 `source` 를 안 실으면 **설문 id 로 붐따 행을 고친다** (조용한 오작동)
- **팝업은 답변을 한 번 받은 뒤, 그 세션에 1회.** 진입하자마자 막아서면 만족도를
  묻는 팝업이 그 자체로 불만이 된다
- ⚠️ **'나중에' 도 저장한다** (`rating=NULL`). 안 남기면 다음 접속마다 다시 뜬다
- ⚠️ **팝업이 안 뜨는 것은 에러가 아니라 침묵이다.** 배선(`/me` ↔ 프론트 ↔ 처리함)을
  `static_survey_wiring` 이 매일 확인하고, 처리함 요약줄에 **대상 인원·응답 수·평균
  별점**을 함께 적어 사람이 이상을 눈치챌 수 있게 한다
- ⚠️ 모달 스타일은 `style.css` 의 `.sv-*`·`.fb-*` 다 — **JS 인라인 금지**(테마 전환에서
  조용히 빠진다). 임계는 서버가 다시 판정해 대조한다 (클라이언트가 정하지 못한다)

## 대표 제품 목록은 **실측 주입**이다 (2026-09-03)

- **파일**: `app/core/product_catalog.py` · 주입: `orchestrator._product_catalog_section()`
  → direct 프롬프트의 `## 대표 제품` 블록 · 갱신: `value_lists_daily`(03:50) 끝에 이어서

⛔ **손으로 적어 두었더니 "없는 제품" 이라고 답했다** (사용자 제보: *"왜 최신데이터가
반영이 안되어있지? 센텔라 테카같은거 말이야"*):

    질문: "센텔라 테카 앰플이 뭐야?"
    답변: "'센텔라 테카 앰플'이라는 정확한 제품명은 **공식 제품 목록에서
           확인되지 않습니다.**"

`SK_Centella_Teca_Ampoule_50ml` 은 **2026-01-16 출시 · 361,315개 판매**된 주력이다.
BigQuery(9종)·전성분(8건)·매핑(15건)·CS 자료(39건)에 **전부 있었다.**

- ⚠️ **데이터가 낡은 게 아니라 프롬프트가 낡았다.** 사용자에게는 구분이 안 된다 —
  "최신 데이터 미반영" 으로 보인다. 낡은 목록을 찾을 때 **데이터부터 의심하지 마라**
- ⛔ 목록에 붙어 있던 *"제품명 창작 금지 — 위 목록에 없는 제품명을 임의로 조합하지
  마세요"* 가 **부정을 더 강하게** 만들었다. 지금 문구는 *"여기 없다고 해서 없는
  제품이 아니다 — 단정하지 말고 데이터 조회로 확인하겠다고 안내하라"* 다
- ⛔ **캐시가 비어도 그 규칙을 남기면 안 된다** — 빈 목록 + "없으면 없는 제품" 은
  **모든 제품을 부정한다.** 조회 실패 시에는 "목록 없음 + 단정 금지" 문구가 들어간다
- ⚠️ 세트·샤쉐·부자재는 뺀다 (`_kit`·`_duo`·`sachet`·`twin_pack`·파우치·코튼패드).
  잔여 버킷(`Others`·`SET`)도 라인이 아니다 — 실측 상위에 `Others` 라는 이름의
  제품 행이 실제로 있었다
- ⚠️ 첫 판매 6개월 이내면 `⭐신제품` 을 붙인다 (프로젝트의 신제품 정의와 같다)

### 함께 고친 것 — "전성분 데이터가 없다" 는 거짓이었다

`INGREDIENT_EXCLUSION_MESSAGE` 가 *"제품별 전성분 데이터를 시스템이 보유하고 있지
않습니다"* 라고 말하고 있었다. **2026-08-06 에 적재했으므로 사실이 아니다** —
커버리지가 전 제품을 덮지 못할 뿐이다. 라인 목록도 손으로 적혀 있어 센텔라테카·
히알루테카가 빠져 있었다 → `product_lines.known_lines()` 로 바꿨다.

### ⛔ 클래스 안에 모듈 레벨 `def` 를 넣지 마라 — 거기서 클래스가 끝난다

이 작업에서 **내가 실제로 저질렀고 프로덕션에 한 번 배포됐다.** 헬퍼를
`def _build_direct_system_prompt` **앞**에 들여쓰기 없이 끼워 넣었더니, 뒤에 있던
`_BIZ_CONTEXT`·`_SEARCH_KEYWORDS` 가 통째로 클래스 밖으로 나갔다.

- ⚠️ **import 는 멀쩡히 된다.** 라우팅이 그 속성을 만질 때서야 `AttributeError` 가
  난다 — 테스트 68건이 한 번에 깨져서야 알았다
- 헬퍼는 **클래스 앞**에 둔다. `static_orch_class` 가 클래스 본문이 온전한지
  (라우팅이 실제로 쓰는 속성·메서드가 클래스 안에 있는지) 매일 확인한다
- 💡 배포 전 **전체 테스트를 돌려라.** 이 사고는 `python -m pytest tests/` 한 번이면
  잡혔다 — 부분 실행만 하고 배포한 것이 원인이다

## 제품정보 ≠ 제품 Q&A — 둘은 다른 소스다 (2026-09-04)

사용자 지시: *"제품 q&A는 따로이고, 제품정보는 cs데이터에 들어가야함"*

| | 소스 | 내용 | 규모 |
|---|---|---|---|
| **제품 Q&A** | 시트 `[BD_BP] CS제품문의_모음집` | **문의 대응 기록** | 914건 (13탭) |
| **제품정보** | 노션 `스킨1004 전제품 한 눈에 파악하기` → `product_info` | **제품 스펙** (사용법·성분·PPM·피부타입·제형) | 55종 · 8라인 |

- **파일**: `app/core/product_info.py` · 잡 `product_info_sync_daily` (04:20) ·
  답변 결합은 `cs_agent._product_info_block()`
- ⛔ **한 덩어리로 섞지 마라.** 프롬프트에 **블록을 나눠** 넣고 *"성분·사용법은 제품정보,
  문의 처리·정책은 Q&A 를 근거로"* 라고 못 박는다. 섞으면 답변이 어느 쪽을 근거로
  말하는지 사라진다
- ⛔ **함량·PPM 을 지어내지 말라**고 프롬프트에 적었다 — 가장 그럴듯하게 틀리는 값이다

### ⛔ 내용은 **4단 깊이**에 있다 — 얕게 긁으면 제품명 목록만 나온다

    스킨1004 전제품 한 눈에 파악하기        (page)
      └ [DB] New database                depth 1
          └ 히알루-테카 (New)              depth 2  ← 라인
              └ [DB] 히알루 테카            depth 3
                  └ 마다가스카르 …퍼밍 크림   depth 4  ← **진짜 내용**

실제로 노션 파이프라인이 얕게 색인해서 *"히알루테카 제품 정보"* 질문에 **경로와 링크만**
답한 사고가 있었다. ⚠️ 라인 이름은 **한 단계 위 행 제목**에서 온다 (제품 페이지 자신은 모른다).

- ⚠️ 수집에 37초 걸린다 — **질문 경로가 아니라 배치에서만** 돈다
- ⛔ **0건이면 기존 데이터를 지우지 않는다** (권한 만료·구조 변경이면 0건이 온다).
  잡은 0건을 **실패로 기록**한다 — 성공으로 적으면 자가 점검이 영영 못 잡는다
- ⚠️ `synced_at` 비교 전 `microsecond=0` (OP 재고에서 겪은 함정과 같다)

### ⛔ `lines` 는 MariaDB 예약어다

`COUNT(DISTINCT line) lines` 로 썼다가 1064 문법 오류가 났고, `except` 가 삼켜
**`count: 0`** 으로 보였다 — 적재는 55건 성공했는데 화면상 0이었다. 조용한 오작동이다.
지금은 `line_count` 로 쓰고, `except` 는 **흔적을 남긴다**.

### ⛔ 군더더기 낱말 하나가 제품정보를 통째로 0건으로 만든다 (2026-09-08 실사용 제보)

    질문: "테카 앰플 정보좀"
    답변: "'테카(TECA) 앰플'에 대한 등록 정보가 없습니다"
          "현재 조회 가능한 앰플 제품군은 센텔라 앰플, 톤 브라이트닝 캡슐 앰플…"

노션 원본 실측: `센텔라-테카 (New) | 마다가스카르 센텔라 테카 앰플` 이 **725자 본문과
함께 있다.** 데이터는 멀쩡했고 **검색이 빗나갔다.** `search()` 가 낱말을 AND 로 LIKE 거는데
`정보좀` 이 그대로 남아 `body LIKE '%정보좀%'` 이 됐다 — 어떤 행도 맞을 수 없다.

- ⚠️ **`정보` 도 `좀` 도 각각 불용어에 있었다.** 붙어 있어서 안 걸린 것이고,
  `좀` 은 조사가 아니라 `strip_particle` 도 떼지 않는다. **띄어 쓰면 통과한다** —
  실측: `"테카 앰플 정보 좀 알려줘"` 는 되고 `"테카 앰플 정보좀"` 만 0건이었다
- ⛔ **불용어 목록을 늘리는 방식으로는 끝이 없다** (정보좀·뭐임·궁금…).
  **자료에 물어본다** — `query_keywords.usable_words()` 가 그 낱말이 든 항목이
  하나도 없으면 버린다. OP 재고가 같은 사고("얼마나")를 이미 그렇게 풀었고,
  **이제 두 곳이 같은 함수를 쓴다** (규칙을 두 번 구현하면 한쪽만 고쳐진다)
- ⚠️ 반대로 넓히지는 않는다 — 자료에 있는 낱말은 살아서 결과를 좁히고,
  쓸 낱말이 하나도 없으면 **빈손이 맞다.** 잡음은 답처럼 보여서 0건보다 나쁘다

#### ⛔ 그런데 배포하고도 화면은 그대로였다 — 함수에 닿는 문자열이 달랐다 (2026-09-09)

위를 고쳐 배포하고 프로덕션에서 `search("테카 앰플 정보좀")` 을 직접 불러 **맞는 답**을
확인했다. 그런데 사용자가 화면에서 같은 문장을 던지자 여전히 *"등록되어 있지 않습니다"* 였다.

오케스트레이터는 `cs_agent` 에 원문이 아니라 **대화 맥락을 감싼 덩어리**를 넘긴다
(스트리밍·비스트리밍 양쪽 다):

    [이전 대화]
    사용자: 센텔라 앰플 사용법 …
    [현재 질문]
    테카 앰플 정보좀

`extract()` 는 **문서 순서대로 8개**만 뽑는데 현재 질문이 맨 뒤다 — 맥락이 상한을 다
먹어 `테카` 가 검색어에 **들어오지도 못했다**. 실측 검색어:
`['이전','대화','사용자','센텔라','앰플','사용법','셀라','마다가스카르']`.

- ⛔ **함수를 프로덕션에서 직접 불러 본 것은 검증이 아니었다.** 실사용 경로는 그
  문자열을 부르지 않는다. 그런데 "프로덕션에서 확인했다" 는 말이 성립해 버려서
  **고쳤다고 확신하게 만든다** — 이 저장소에서 가장 비싼 종류의 착각이다.
  검증은 **사용자가 지나는 경로**로 한다
- `cs_agent._current_question()` 이 `CURRENT_QUESTION_MARKER` 뒤만 떼어 쓴다.
  ⚠️ 맥락을 버리지는 않는다 — 현재 질문만으로 0건이면 덩어리 전체로 되돌아본다
  ("그럼 크림은?" 같은 후속 발화는 그 자체로 라인을 모른다)
- ⚠️ 표식을 양쪽이 따로 적으면 한쪽만 고쳐졌을 때 조용히 갈린다 — 회귀가
  오케스트레이터 소스에 그 표식이 실재하는지 대조한다

#### ⛔ 라인을 맞혀도 **질문한 제품이 잘려 나갔다** (같은 조사에서 드러났다)

라인 경로는 그 라인 전체를 돌려주는데 `_product_info_block` 은 앞의 **4개만** 쓴다.

| 질문 | 실렸던 것 |
|---|---|
| `"히알루시카 슬리핑 팩 알려줘"` | 슬리핑 팩은 11종 중 10번째라 **빠지고** 젤리핏 앰플 패드·워터핏 선 세럼 |
| `"센텔라 앰플 폼 정보좀"` | **포어마이징 4종** (제품명이 죄다 `마다가스카르 센텔라 …` 라 라인 필터가 거의 전부를 통과시킨다) |
| `"마다가스카르 센텔라 앰플"` | 그 제품이 상위 4건에 **없었다** (본문에 '앰플' 이 적힌 제품이 표 순서대로 먼저) |

붐따 #111 과 같은 실패 모양이다 — 질문한 것이 아닌 제품을 설명하면서 바꿔치기했다는
말은 하지 않는다. 그래서 `search()` 는 세 단계다: **라인으로 좁히고 → 낱말로 더 좁히고
→ 이름에 걸린 것을 앞에 세운다** (동점이면 이름이 짧은 쪽).

- ⚠️ **못 좁히면 라인 전체로 되돌아간다** — 표기가 어긋날 수 있다
  (`히알루시카` ↔ 자료의 `히알루-시카`). 좁히려다 0건을 만들면 안 된다
- ⚠️ 되돌아간 목록에도 **순위를 매긴다.** 실측: `"센텔라 테카 앰플 성분"` 은
  앰플 본문에만 '성분' 이 없어 AND 가 비었고, 정작 앰플이 세 번째였다
- ⛔ 순위는 **코드가 정한다.** LLM 에게 "질문한 제품을 골라라" 고 맡기면 확률이다
- 회귀: `tests/test_product_info.py` (24문항)

## ⛔ `@@` 소스 키를 개명하면 **저장된 선택이 깨진다** (2026-09-04 실사용 장애)

`LOG` → `물류` 로 바꾼 뒤, 브라우저(localStorage)에 `LOG` 를 저장해 둔 사용자가
물류 질문마다 *"허용되지 않은 테이블입니다"* 를 받았다.
프로덕션 로그 실측: **16:56 · 16:58 · 16:59 세 요청이 그렇게 죽었다.**

- ⚠️ **에러 화면이 아니라 조회 실패로 보인다** — 사용자는 데이터가 없는 줄 안다
- ⚠️ **프론트 저장분은 서버가 고칠 수 없다.** 보정은 **서버에서** 한다 —
  `sql_agent._canonical_source()` 가 `_DB_REGISTRY` 의 **키와 별칭**을 모두 보고
  현재 키로 옮긴다. 한 소스만 고치면 다음 개명에서 같은 일이 나므로 **전 소스**를 흡수한다
- ⚠️ 모르는 이름은 그대로 흘려보낸다 — 조용히 전체를 열지 않는다
- 회귀 `tests/test_source_alias_scope.py` 가 등록부의 **모든 키·별칭**을 검사한다

## 제품 라인 질문은 제품 Q&A(BP)로 간다 — 2026-09-04

⛔ **실사용 제보** (*"왜 노션정보만 가져오지?"*):

    질문: "히알루테카 제품 정보 알려줘"
    답변: "…사내 노션 문서에서 확인하실 수 있습니다. 경로: … > 히알루-테카 (New)
           **구체적인 제품 스펙이나 성분 등의 상세 텍스트는 포함되어 있지 않으나**…"

**경로와 링크만 주고 내용은 없었다.** 원인은 데이터가 아니라 **라우팅 두 겹**이었다.
데이터는 있었다 — CS 시트에 히알루-테카 Q&A 5건, 전성분 테이블에 테카 5종(767~1,713자).

### ⛔ 원인 ① `제품 정보` 가 노션 확신어였다

`_NOTION_KEYWORDS` 에 `제품 정보` 가 있고 **노션이 CS 보다 먼저** 판정된다.
그래서 `히알루` 가 `_CS_KEYWORDS` 에 있는데도 노션이 이겼다.

- 대응은 낱말 제거가 아니다 — *"제품 정보 문서 어디 있어"* 는 계속 노션이어야 한다.
  **제품 라인을 지목했으면**(`_product_line_named`) 노션을 건너뛰고 CS 판정으로 넘긴다
- ⚠️ **노션을 명시했으면**(`노션에서 …`) 그대로 노션이다 — 좁히려다 넓히면
  사내 문서 검색이 통째로 죽는다. 회귀가 **양방향**으로 지킨다
- ⛔ 라인 어휘를 라우터에 손으로 적지 마라 — `product_lines` 가 프롬프트 표에서 읽는다
  (그렇게 적어 둔 탓에 센텔라테카·히알루테카가 빠져 있던 전례가 있다)

### ⛔ 원인 ② 표지가 서로를 삼켰다 — CS 답변이 notion 으로 읽혔다

①을 고쳤는데도 **두 번째 발화**("히알루-테카 퍼밍 크림은?")가 여전히 노션으로 갔다.
후속 발화는 `_previous_route()` 로 **직전 경로를 물려받는데**, 그 판정이 틀렸다:

| | |
|---|---|
| CS 답변 꼬리 | `*출처: CS 제품 Q&A 데이터베이스*` |
| notion 표지 | `"Notion 사내 문서 검색"`, `"사내 문서에서"`, **`"출처:"`** ← |

`"출처:"` 가 CS 꼬리에 걸리고 **notion 이 cs 보다 먼저 검사**되므로 cs 항목까지 가지도
못한다. ⟹ 잘 답한 CS 턴 뒤에 후속 발화가 노션을 물려받았다.

- ⛔ **표지는 그 경로에서만 나오는 문자열이어야 한다.** 골든셋 기대어 규칙과 같은 사상 —
  *실패 답변에도 들어가는 낱말은 표지가 될 수 없다*
- ⚠️ **순서로 때우지 마라** (cs 를 앞으로 옮기는 식). 표지 자체가 고유해야 한다.
  회귀가 표지끼리 **서로를 삼키지 않는지** 검사한다
- ⚠️ 길이로 판정하지 마라 — `[메일]`(4자)은 짧아도 그 경로에서만 나온다

### 💡 첫 질문이 틀리면 대화 전체가 틀린다

후속 발화가 직전 경로를 물려받는 설계라, **라우팅 오류는 한 턴에서 끝나지 않는다.**
사용자가 본 것도 그것이다 — 두 턴 연속으로 경로만 답했다.

### ⚠️ 남은 것 — 노션 DB 는 **행 속성만** 색인된다

`스킨1004 제품 라인업` DB 의 청크는 `정렬: 9 | 한줄 설명: … | 라인업: …` 뿐이고,
9개 라인 페이지의 **본문은 들어가지 않았다** (`_extract_database_text` 가 행의 속성만
뽑는다). 그래서 노션으로 갈 경우 여전히 경로만 답할 수 있다. 제품 질문은 이제 CS 로
가므로 당장 문제는 없지만, **DB 행 본문까지 색인하려면 파이프라인 보강이 필요하다.**

## 수상/랭킹(`@@수상`) — 조건이 하나도 안 남으면 **원문 덤프가 된다** (2026-09-09)

- **파일**: `app/core/awards.py` (시트 → MariaDB `awards_rankings`, 206행) ·
  라우팅은 `orchestrator._awards_term` / `_handle_awards_query`
- 이 경로는 **LLM 을 부르지 않는다** — 판정(마케팅 활용 O/△/X)이 법적 판단이라
  문장을 만들지 않고 표와 표기 원문만 낸다 (초상권·성분과 같은 사상)

⛔ **붐따 #173** — *"@@수상으로 물어봤는데 그냥 원문만 보여줌"*. 사용자가 **세 번
다른 질문**을 던지고 **같은 표 40행**을 받았다. 에러는 없었다. 낱말이 전부
버려져 `WHERE 1=1` 이 됐고, 그 사실은 표 **아래** 각주에만 있었다.

| 질문 | 무엇이 버려졌나 | 전 | 후 |
|---|---|---|---|
| `쇼피에서 받은게 뭐있지` | `쇼피에서` — **조사째라 자료에 없다** | 206→40 | **45** |
| `2026년만 랭크되있는거` | `2026년` — 자료엔 `2026-01-15` 로만 있다 | 206→40 | **2** |
| `2026년 수상 알려줘` | 연도가 통째로 무시 (2017~2026 이 다 나왔다) | 52 | **5** |

- ⛔ **조사 규칙을 여기서 다시 구현하지 마라.** `query_keywords.usable_words` 를
  쓴다 — OP 재고("얼마나")·제품정보("정보좀")가 이미 같은 함수로 같은 사고를
  풀었다. 그래서 `_haystack()` 이 표를 한 덩어리로 읽어 넘긴다 (낱말당 SELECT 를
  하던 `_word_exists` 는 조사를 풀 자리가 없었다). 회귀가 `search()` 소스에
  `strip_particle` 이 없는지까지 검사한다
- ⛔ **텍스트로 걸릴 수 없는 축은 따로 뺀다** — `N위`(`rank_value`)와 같은 계열:
  - **연도·월** `_extract_year_filter`·`_extract_month_filter` → `_DATE_SQL`
  - **구분** `_CATEGORY_WORDS` → `category`. `랭크되있는거` 처럼 자료에 없는 말투도 산다
- ⛔ **추출 순서가 뜻을 바꾼다: `N위` → `N월` → 연도.** 연도의 맨 네 자리 규칙이
  먼저 돌면 기간 표현의 숫자를 연도로 집는다

### ⛔ 연도만 걸고 월을 흘리면 답이 조용히 뒤집힌다 (2026-09-09 제보)

    질문: "2026년 6월꺼만 보여줘"
    답변: 2026년 **전체 7건** (1·2·3월) + 각주 "6월꺼만 은 필터로 쓰지 않았습니다"

정답은 **0건**이다 — 2026년 6월엔 기록이 없다(1월 4 · 2월 1 · 3월 2뿐). 표가
옆에 맞게 있어서 *"6월에 이만큼 받았다"* 로 읽힌다. 각주는 표에 밀려 안 읽힌다.

- **연·월은 같은 날짜식(`_DATE_SQL`)을 쓴다.** 갈라 두면 언젠가 서로 다른 행을
  골라 표 안에서 숫자가 어긋나고, 그 어긋남은 티가 안 난다
- ⚠️ **날짜 표기가 두 가지다** — `2026-01-15`(205행) 와 `3/9/2026`(1행).
  앞의 것만 읽으면 그 한 행이 기간 조회에서 조용히 빠진다 (하필 2026년 행이다).
  `STR_TO_DATE` 를 두 형식으로 `COALESCE` 한다 — 실측 파싱 실패는 1행뿐이고
  (`award_start='진행중'`), 연도별 집계가 옛 문자열 방식과 **전 연도 일치**한다
- ⛔ **`_DATE_SQL` 의 `%` 는 두 번 쓴다** (`%%Y-%%m-%%d`). pymysql 은 params 가
  **빈 튜플이어도** `query % params` 를 돌려 홑 `%` 는 그 자리에서 터진다 —
  이 식은 조건 없는 기본 조회에도 실려 나가므로 터지면 조회가 통째로 죽는다.
  회귀가 `sql % ()` 를 실제로 돌려 본다
- ⛔ **기간으로 좁혀 0건이면 "안 받았다" 와 "안 적혔다" 가 똑같이 보인다.**
  `_period_hint()` 가 **기록이 있는 기간**을 함께 적는다 ("2026년에 기록이 있는
  달은 1월 4건 · 2월 1건 · 3월 2건 입니다"). 프로모션 캘린더·수출 물류의 보유
  구간과 같은 규칙이다. ⚠️ **0건일 때만** 조회한다 (조회를 늘리지 않는다)
- ⚠️ `13월` 은 달이 아니다 — 있지도 않은 조건으로 0건을 만들지 않는다

### ⛔ 같은 결함의 다른 말투 둘 (2026-09-09 스윕에서 발견)

배포 후 실제 말투로 훑어 보니 **낱말이 조건이 안 되는** 자리가 둘 더 있었다.
둘 다 증상이 같다 — 에러 없이 **전체 40행**이 나간다.

| 질문 | 왜 | 후 |
|---|---|---|
| `올해 받은 상 알려줘` | 연도를 숫자로 안 적었다 | 2026년 **7행** |
| `작년에 받은거` | 〃 | 2025년 **4행** |
| `틱톡샵에서 받은거 알려줘` | 자료엔 **`TIKTOK SHOP`**(영문)으로 있다 | **5행** |

- **상대 연도** `_REL_YEAR` (올해·금년·작년·지난해·재작년).
  ⚠️ **긴 낱말부터 본다** — `작년` 이 `재작년` 안에 들어 있어 짧은 쪽이 먼저
  맞으면 **한 해가 어긋난다** (권역명 되묻기에서 겪은 그 함정)
- ⛔ **주최사 표기가 한글·영문으로 섞여 있다** (실측 29종): `TIKTOK SHOP`·
  `Qoo10`·`PICKY`·`Daily Vanity`·`Female Daily`·`Cosmoprof` 는 영문인데
  `쇼피`·`화해`·`아마존`·`올리브영 글로벌` 은 한글이다. `_ORG_ALIASES` 가 잇는다
  - ⛔ **같은 주최사가 자료에서 두 표기로 갈려 있다** — `스타일바나`(1건)와
    `STYLEVANA`(1건). 물류 `forwarder` 표기 혼재와 같은 계열이라 **둘 다** 건다
  - ⛔ **뜻이 비슷할 뿐인 말은 넣지 마라** — 넣을 수 있는 것은 **표기 변형**뿐이다
    (드라이브 검색 씨앗과 같은 기준. 회귀가 매출·실적·성과가 들어오면 실패시킨다)
  - ⚠️ **손으로 적은 목록은 낡는다.** 그래서 `_alias_for()` 는 별칭을 쓰기 전에
    **자료에 그 표기가 실제로 있는지 확인한다** — 시트 표기가 바뀌면 별칭이
    스스로 꺼지고 원래 낱말로 되돌아간다 (낡은 목록이 조용히 0건을 만드는 그 자리)
  - ⚠️ 별칭은 자료 확인 **앞**에 끼운다. 뒤에 두면 `틱톡샵` 이 이미 버려진 뒤라
    되살릴 자리가 없다

💡 **이 부류는 회귀 테스트로는 안 드러난다** — 테스트는 내가 생각해 낸 말투만
   본다. 배포 뒤 **실제 말투로 한 번 훑어야** 보인다 (`@@수상` 스윕 9문항으로
   둘을 찾았다).
- ⛔ **`어워드`·`award` 를 구분 낱말에 넣지 마라 — 실측 17행이 사라진다.**
  `어워드` 텍스트 19행 중 `category='수상'` 은 **2행**뿐이고 나머지는 랭킹 행의
  제목에 든 것이다(`Daily Vanity Beauty Awards …`). 넣어도 되는 것은 **텍스트로
  걸던 것과 결과가 같은 낱말**뿐이다 (실측: `수상` 52=52 · `랭킹` 94=94)
- ⛔ **좁히지 못했으면 표보다 먼저 말한다** (`narrowed`). 각주로는 부족하다 —
  #173 에서 그 각주는 표에 밀려 읽히지 않았다. 문구는 "전체 목록이라 질문에 대한
  답이 아니다" + **어떻게 물으면 좁혀지는지**다
  - ⚠️ **빈 질문(`@@수상` 만 찍음)에는 띄우지 마라** — 그때는 전체 목록이 답이다.
    `dropped`·`capped` 가 있을 때만 뜬다 (매번 뜨는 안내는 곧 아무도 안 읽는다)
- ⚠️ 연도·구분으로 좁혔으면 **무엇으로 좁혔는지 밝힌다** (순위와 같은 규칙).
  조용히 좁히는 것은 조용히 안 좁히는 것과 같은 종류의 실패다
- 회귀: `tests/test_awards.py` (126문항 — #173 절 12문항)

## 제품 라인 어휘는 프롬프트 표가 단일 소스 — 2026-08-25

- 질문이 지목한 라인과 **다른 라인으로 답하지 않는다** (붐따 #111: "히알루 테카" 를
  물었는데 히알루시카를 설명했고, **바꿔치기했다는 말은 어디에도 없었다**)
- 어휘는 `prompts/sql_generator.txt` 의 `### 제품 라인` 표를 파싱한다
  (`app/core/product_lines.py`). ⛔ **손으로 다시 적지 마라** — 사본은 반드시 낡고,
  낡으면 에러가 아니라 조용한 오답이 된다
- `cs_agent.search_qa()` 는 질문이 라인을 지목하면 그 라인 자료가 아닌 행을 뺀다.
  남는 게 없으면 **0건**이 맞다 — "찾지 못했습니다 + 등록된 라인 목록" 이 나간다
- ⚠️ 짧은 영문 약어(`TB`·`LIN`)는 어휘에서 뺀다 — 정규화하면 `lin` 이 `line` 안에 걸린다

## 재무 손익(FI) 열람 권한 — 2026-08-04

- **테이블**: `skin1004-319714.Sales_Integration.FI_LLM_Flat` (월별 연결 손익, `Record_Type='PL'`)
- **보유 기간은 2026-01 ~ 2026-06 뿐이다.** 범위 밖 질문엔 빈 결과 대신 보유 기간을 먼저 안내한다.
- **승인된 사람만 조회 가능**. 권한은 `directory_users.can_view_fi` (users 가 **아니다**)
  - 미가입자에게도 미리 부여해둘 수 있고, 가입하면 자동 적용된다
  - `user_groups` 도 `ad_user_id` 를 키로 쓰므로 일관됨
  - admin 은 플래그와 무관하게 항상 허용
- **관리**: Admin > AD 사용자 탭의 `손익` 체크박스 (`PUT /api/admin/ad/users/{id}/fi`)
  - 일괄 부여/회수 스크립트: `python scripts/grant_fi_access.py [--dry-run|--revoke]`
- **권한 판정은 반드시 서버에서 DB 조회로** 한다. JWT·프론트 값은 stale 위험이 있어 신뢰하지 않는다
- **방어선을 줄이지 말 것** (하나가 뚫려도 나머지가 막는 구조):
  1. `orchestrator._requests_fi_data()` — 손익 키워드/소스 감지 시 LLM 호출 전 거절
  2. `sql_agent._load_prompt(can_view_fi=False)` — 프롬프트에서 FI 스키마 섹션 제거
  3. `_allowed_tables_from_sources()` — 테이블 화이트리스트에서 FI 제외
  4. `security.validate_sql()` — 실행 직전 FI 참조 재검사 (대소문자 무관)
  5. 프론트 — 손익 데이터소스 칩 숨김
- 컬럼은 `ensure_fi_permission_column()` 이 앱 기동 시 자동 추가 (idempotent)

## 노션 학습 범위 — 무엇을 배우고 무엇을 빼는가 (2026-09-04)

- **단일 진입점은 DB-HUB 하나다.** 새 자료는 노션에서 **팀 토글에 페이지 멘션으로**
  걸어야 학습된다. ⛔ 코드에 별도 루트 목록을 만들지 마라 — 두 곳에서 "무엇을 배우는가"
  를 정하면 반드시 갈린다
- ⚠️ **평범한 URL 링크는 수집되지 않는다.** `_collect_from_block` 이 보는 것은
  `child_page` · `child_database` · **mention** 뿐이다

### ⛔ 학습이 안 될 때 원인은 **둘**이고, 하나만 고치면 여전히 안 된다

    1. 권한 — `Skin1004_AI` 인테그레이션이 그 페이지에 연결돼 있지 않다
    2. 도달 — DB-HUB 아래에서 그 페이지를 가리키는 곳이 없다

⚠️ **둘을 구분하는 법**: 읽을 수 없는 페이지도 **크롤 목록에는 잡힌다**
   (실측 113개 중 22개가 404). 그러니 *"크롤 목록에 아예 없다"* = 권한이 아니라 **도달** 문제다.
   실제로 권한을 준 직후에도 113페이지에 없었고, CS 토글에 멘션을 넣자 114로 늘며 잡혔다.

### ⛔ `연결`은 **최상위 페이지에만** 붙는다 — 하위는 상속받는다

깊은 하위 페이지에는 `연결` 항목이 아예 안 보인다. 붙이려면 **상위 트리에 붙여야 하고,
그러면 그 아래 전부가 열린다.** 그래서 "이 페이지에 권한을 줘" 와 "이 트리를 열어줘" 는
다른 결정이다 — 임의로 넓히지 마라. 열람 전용 페이지는 메뉴에 `페이지 신고` 만 보인다.

### ⛔ 제외해도 **기존 색인 조각은 안 지워질 수 있다** (2026-09-04 실측으로 정정)

처음에 *"`EXCLUDED_PAGE_IDS` 에 적으면 기존 조각도 함께 지워진다"* 고 적었는데
**조건부다.** 삭제는 `local_page_map(로컬 JSON) − notion_page_ids` 로 계산하므로
**파이프라인이 스스로 넣은 페이지만** 지운다.

| | |
|---|---|
| 제외한 14건의 Qdrant 잔존 청크 | **45개** |
| 그중 로컬 JSON(파이프라인) 소유 | **0건** |

⟹ 다른 적재기가 넣은 옛 조각은 계산 대상 자체가 아니라 **그대로 남는다.** 정말 지우려면
Qdrant 에서 직접 지워야 한다. 💡 뒤집어 말하면 **남의 것을 실수로 지울 위험도 없다**
(링크 카드 177장을 통째로 지웠던 사고와 같은 계열의 방어다).

### ⚠️ `404스킵` 숫자만 보고 "그 자료가 없다" 고 결론짓지 마라

`skipped_404=20` 을 보고 *"PEOPLE 자료를 못 가져온다"* 고 보고했는데, **실제로는
Qdrant 에 PEOPLE 243청크가 이미 있었다** (휴가 66·채용 54·교육 41·근태 27·복리후생 13…).
사용자가 *"이미 qdrant에서 가져온 걸로 알고 있음"* 이라고 바로잡았다.
**크롤이 못 읽는 것과 색인에 없는 것은 다르다** — 결론 전에 Qdrant 를 직접 세어 볼 것.

### 외부 공개 사이트(`*.notion.site`)는 인테그레이션으로 학습할 수 없다

DB-HUB 에 멘션은 걸려 있는데 열어 보면 우리 워크스페이스가 아니라 별도 공개 사이트로
리다이렉트되는 페이지가 14건 있었다 (PEOPLE 13 · [GM]WEST 1). **영원히 404** 라
`EXCLUDED_PAGE_IDS` 에 이유와 함께 넣어 매일 나던 소음을 없앴다 (404스킵 20 → 3).

- ⚠️ **비로그인 요청으로는 판정할 수 없다** — notion.so 는 전부 로그인 페이지를 준다.
  로그인된 브라우저에서 리다이렉트 종착지를 봐야 드러난다
- ⚠️ 네트워크 실측: **WAS → `*.notion.site` 는 프록시 403**, DB_PC 는 200(0.61s).
  공개 사이트 API(`/api/v3/loadCachedPageChunk`)로 본문이 뽑히긴 한다 —
  다만 **이미 Qdrant 에 있으므로 지금은 만들 이유가 없다**

### 감시 — `notion_unshared` (자가 점검, 매일)

파이프라인이 건너뛴 페이지를 **팀·제목·링크까지** 기록한다 (`app/core/notion_watch.py`).
⛔ **총 건수로 매일 울리지 않는다** — 밀린 것 때문에 경보가 소음이 된다.
**새로 생긴 것만** 실패로 올린다: *"방금 건 자료가 안 들어왔다"* 가 진짜 신호다.
본 것은 기억해 두어 같은 건으로 두 번 울리지 않는다.

## 노션 데이터 규칙

- 사용자가 **노션을 명시적으로 언급하지 않는 한** 노션 데이터를 답변에 포함하지 않음
- 노션 데이터는 **노션 트리 기능**(채팅, System Status, @@ 데이터소스 선택)에서만 활용
- BigQuery 질문에 노션 데이터를 섞지 않을 것

## 메가와리 기간 (큐텐 Qoo10 전용)

- **2023년**: Q1(3/1~3/12), Q2(6/1~6/12), Q3(9/1~9/12), Q4(11/22~12/3)
- **2024년**: Q1(3/1~3/12), Q2(6/1~6/12), Q3(8/31~9/12), Q4(11/15~11/27)
- **2025년**: Q1(2/28~3/12), Q2(5/31~6/12), Q3(8/31~9/12), Q4(11/21~12/3)
- **2026년**: Q1(2/27~3/11), Q2(5/29~6/10)
- ⛔ **표에 없는 분기는 날짜를 추정하지 마라** — LLM 이 패턴으로 지어내면 그럴듯하게
  틀린다. 2026 Q2 가 없던 때 40.2억으로 답했고 실제는 62.2억이었다 (2026-08-18 확인).
  Q2 날짜는 Q10 일별 매출에서 실측했다 (0.16억 → 14.60억 → … → 9.49억 → 0.70억)
- ⚠️ 이 표는 `prompts/sql_generator.txt` 에도 있다. **두 곳을 함께 고칠 것**
- 메가와리 질문 시 `Mall_Classification LIKE '%Q10%'` 필터 필수

## 오케스트레이터 컨텍스트 규칙 (2026-06-01)

- **파일**: `app/agents/orchestrator.py`
- **direct 경로 시스템 프롬프트는 `_build_direct_system_prompt()` 한 벌이다** (2026-08-13 통합).
  - ⛔ 예전엔 **두 벌**이었다 — 스트리밍은 이 함수를, 비스트리밍(`_handle_direct`)은 인라인
    사본을 썼다. 한쪽만 고쳐 경로에 따라 답이 갈렸고, 실제로 *"보고서라는 별도 메뉴는
    없습니다"* 라고 답했다. **사본을 다시 만들지 마라** — 테스트가 사본 수를 세서 막는다
  - 통합할 때 두 벌의 내용이 서로 달랐다 (한쪽엔 대시보드 카탈로그·대표 제품·최신 원칙,
    다른 쪽엔 상세 회사소개·답변 형식 표준). **합집합**으로 합쳤다 — 사본이 갈리면
    "어느 쪽이 맞나"가 아니라 "양쪽 다 일부만 맞다"가 된다
- **데이터 낱말은 목록 두 곳을 본다** — `_DATA_KEYWORDS`(데이터 질문인가)와
  `_BIZ_CONTEXT`(우리 비즈니스 맥락인가). **둘은 서로 다른 질문**이라 따로 둔다
  - ⛔ 예전엔 맥락 판정이 **두 목록**으로 나뉘어 있었다 (`_SKIN1004_TERMS` + `_BIZ_CONTEXT`).
    같은 질문에 답하는데 내용이 달라서, 한쪽에만 낱말을 넣으면 다른 관문에서 걸려
    direct 로 떨어졌다 — "할인 많이 준 채널"·"신제품 몇 개"·"FOC 비용"이 전부 그렇게 샜다.
    2026-08-13 에 합집합으로 통합했다 (78 + 119 → 129)
- ⛔ **짧은 낱말을 포함 검사에 그대로 두지 마라.** `"라인"` 이 **"가이드라인" 안에서**,
  `"환율"` 이 **"전환율" 안에서** 매치됐다 (보고서 필터의 '요인도 → 인도' 와 같은 부류).
  - 낱말 경계 판정은 **`app/core/textmatch.py` 한 곳**이다. 보고서 필터와 라우팅이 같은 것을 쓴다
  - ⚠️ **모든 낱말에 경계를 강제하면 안 된다** — "월별매출"의 `매출`은 잡혀야 맞다.
    `orchestrator._GUARDED` 에 든 낱말(뜻이 뒤집히는 것들)만 앞 글자를 확인한다
  - 새 충돌은 `static_checks.keyword_collisions()` 가 알려준다. **뜰 때마다 실제 질문으로
    확인하고** `KNOWN_COLLISIONS` 에 넣어라 — 그냥 추가하면 아무것도 안 지키는 목록이 된다
- **문서를 달라는 말이 있으면 데이터 명사가 섞여도 문서다** — 단 수량·금액을 물으면 조회다
  ("시딩 가이드라인"=notion / "시딩 비용 얼마"=bigquery)
- **용어의 뜻을 묻는 질문은 direct 다** — "roas가 뭐야?" 가 `roas` 때문에 **확신을 갖고
  bigquery** 로 갔다. 뜻을 묻는데 SQL 을 만들면 답이 나올 수 없다. 기간·집계·수량 표현이
  하나라도 있으면 조회로 본다 (`_is_definition_question`)
- 💡 **라우팅 점검은 '확신을 갖고 틀린 것'만 센다.** confident=False 는 LLM 이 다시
  판정하므로 오답이 아니라 위임이다. 회귀는 `tests/test_report_rules.py::test_routing_sweep`
- **자기 기능을 묻는 질문은 direct 다** (2026-08-13). "보고서 기능은 어떤 때 쓰면 좋아?"가
  notion 으로 새서 사내 문서를 뒤졌고, "보고서 사용법 알려줘"는 '사용법' 때문에
  **확신을 갖고 cs**(제품 사용법)로 갔다 — 확신 분류라 LLM 재판정도 못 탄다
  - 대응은 키워드 추가가 아니라 **대상 판정**이다: `_is_self_feature_question()` 이
    질문 대상이 우리 자신인지 본다. 외부 플랫폼·사내 툴 이름(`_PLATFORM_TOOL_NAMES`)이
    있으면 아니다 — "틱톡샵 접속 방법"·"잔디 사용법"은 계속 notion 이어야 한다
  - 보고서 기능 질문 판정은 `registry._REPORT_META` 를 **재사용**한다. 같은 규칙을
    두 곳에서 따로 정의하면 한쪽만 고쳐진다
- **시스템 프롬프트 컨텍스트 원칙**:
  - 사용자가 "아까", "그거", "방금", "다시" 등으로 이전 답변을 참조하면 → 반드시 컨텍스트 활용
  - 질문 자체가 이전 대화와 완전히 무관한 경우 → 컨텍스트 무시해도 됨
  - "이전 맥락을 무조건 무시하라"는 지시는 금지 (참조형 질문까지 끊김)
- **`_clean_messages_for_history()`**: `generate_with_history_stream` 호출 전 assistant 메시지에서 차트 JSON·SQL `<details>` 블록 제거 (텍스트는 전체 보존, 1500자 제한 없음)
  - 직접 `messages`를 넘기지 말고 반드시 이 함수를 거칠 것
- **`_build_conversation_context()`**: BigQuery·Notion·CS 등 비-direct 라우트용 텍스트 요약
  - 최근 10개 메시지, assistant 1500자 제한, chart/SQL 노이즈 제거
  - direct 라우트에는 사용하지 않음 (full history 사용)

## LLM 모델 설정 — 2026-08-13

- **채팅(direct 경로) = `claude-opus-5`** (2026-08-13 4.8 에서 상향, 요율 동일 $5/$25)
- ⛔ **`app/config.py` 의 기본값만 고치면 아무것도 안 바뀐다.** `.env` 의
  `ANTHROPIC_OPUS_MODEL` 이 이기고, `.env` 는 **배포에서 제외**된다(`EXCLUDE_FILES`).
  서버마다 직접 고쳐야 한다 — 실제로 코드만 올리고 4.8 로 돌고 있었다
- ⛔ **답변에 노출되는 모델명을 프롬프트에 손으로 적지 마라.** `_model_display_name()`
  이 설정에서 만든다. 하드코딩돼 있어 모델을 올린 뒤에도 "Opus 4.8 기반"이라고 답했다
- **Opus 5 는 thinking 이 기본 ON 이다** (4.8 은 파라미터를 빼면 꺼졌다). 모델명만
  올리면 두 가지가 조용히 깨진다 — `llm.py` 의 `_tune()`·`_first_text()` 가 막는다:
  1. `max_tokens` 가 thinking + 답변을 **합쳐서** 제한한다 → 답변이 중간에 잘린다
  2. `content[0]` 이 text 가 아니라 thinking 블록이다 → `.text` 가 없어 터진다
- **깊이는 `ANTHROPIC_EFFORT` 로 조절한다. 현재 `medium`** (2026-08-13 사용자 결정).

  **프로덕션 실측** — direct 경로, 4문항×3회, 완료까지 걸린 시간의 중앙값:

    | | low | medium | 차이 |
    |---|---|---|---|
    | 런 중앙값 | **11.0s** | **13.3s** | +2.3s (+20%) |
    | 짧은 답(2문장) | 6.2s | 6.9s | +0.7s |
    | 긴 답(안내형) | 19.3s | 22.8s | +3.5s |

  - **첫 글자는 두 설정 모두 0.02s** — 앱이 먼저 흘리므로 체감 지연은 완료 시간에 있다
  - ⚠️ **SDK 단독 측정은 앱 지연과 다르다.** SDK 로 첫 청크만 재면 1.8s/2.4s 로 보이는데
    실제 서비스는 라우팅·후처리가 붙어 11s/13.3s 다. 반드시 프로덕션 경로로 잴 것
  - ⚠️ **캐시 히트가 섞이면 비교가 무의미하다** — 캐시된 답이 0.04초에 나왔다.
    A/B 는 매번 다른 문구(문의번호 등)를 붙여 잰다
  - ⚠️ thinking 을 끄는 것(`disabled`)으로 속도를 벌지 마라 — Opus 5 는 그때
    `<thinking>` 태그가 답변에 새는 사례가 보고돼 있다. 낮은 effort 가 정답이다

### direct 경로가 느릴 땐 effort 보다 **검색 그라운딩**을 먼저 의심하라 (2026-08-13)

`"안녕? 오늘 뭐 도와줄 수 있어?"` 가 **22.8초** 걸렸다. 원인은 모델이 아니라 두 가지였다.

- ⛔ **`_gather_search_context()` 가 Pro 를 쓰고 있었다.** multi 경로는 v6.5 에서 이미
  Pro→Flash 로 바꿨는데(60-80s → 30-40s) **direct 경로만 안 바뀌어 있었다.**
  같은 질문 실측: **Pro 7.8~8.4s vs Flash 2.0~2.1s**. 이 호출은 Claude 가 답을 쓰기
  **전에 동기로** 끼어들어 첫 토큰을 통째로 민다
  → 같은 최적화를 여러 경로에 할 땐 **호출부를 전부 grep 해서 확인**할 것
- ⛔ **`"오늘"` 한 단어가 구글 검색을 켰다.** `_SEARCH_KEYWORDS` 에 시간어가 들어 있어
  인사말이 검색을 탔다. 대응은 **키워드를 빼는 게 아니다** — "오늘 환율"은 계속 검색해야
  한다. 걸린 게 **시간어뿐이고**(`_AMBIENT_TIME`) 질문 대상이 어시스턴트 자신이면
  (`_SELF_REF`) 검색하지 않는다. 회귀 12문항으로 양방향을 함께 지킨다

  | | 첫 토큰 | 총 |
  |---|---|---|
  | 고치기 전 (medium) | 14.8s | **22.8s** |
  | 고친 뒤 (medium) | 7.0s | **12.2s** |

  - 남은 7초는 thinking 이다. **thinking 토큰은 전부 첫 텍스트 토큰보다 앞에 나오므로**
    답이 길고 열린 질문일수록 첫 토큰이 늦다 (열린 질문 654자 7.0s vs "세 가지만 짧게" 372자 3.1s)
  - 결과적으로 **medium + 이 수정(10.1s)이 예전 low + 결함(11.0s)보다 빠르다**
- 💡 **경로별 시간은 `audit_logs` 에 이미 남는다** (`route`·`first_token_ms`·`total_ms`).
  느린 질문은 추측하지 말고 여기부터 볼 것 — 같은 `direct` 라도 첫 토큰이 2.5s 와 17s 로
  갈린다는 사실이 여기서 바로 나왔다. ⚠️ 단 **비스트리밍 요청은 기록되지 않는다**

## 검색 키워드는 한 곳에서만 뽑는다 (`app/core/query_keywords.py`) — 2026-08-14

⛔ **한국어는 교착어라 조사를 떼고 나서 불용어를 봐야 한다.** 순서를 바꾸면 검색이
**에러 없이 빗나간다** — 두 곳이 그렇게 깨져 있었다 (사용자 제보로 발견):

| 경로 | 증상 |
|---|---|
| 구글 드라이브 | `"구글드라이브에서 내가 작성한 …"` 이 통째로 검색어가 돼 **항상 0건**. 불용어에 `드라이브`·`에서`·`내` 가 있었지만 붙어 있어 하나도 안 걸렸다 |
| Knowledge Wiki | `매출이`·`파이프라인의` 로 LIKE 매칭 → "매출" 이 든 문서를 못 찾았다 |

- 추출은 `query_keywords.extract(question, extra_stop=...)` 하나만 쓴다.
  경로별로 다른 것은 **도메인 불용어뿐**이다 (드라이브의 `시트`·`사진` 등)
- `textmatch.strip_particle()` 이 조사를 뗀다. ⚠️ **두 글자 이하로 줄면 떼지 않는다** —
  `교안`의 '안', `자료`의 '료' 가 잘려 나간다
- ⛔ **Drive 는 낱말을 통째로 한 조건에 넣지 마라.** `name contains '신규 입사자 교안'` 은
  그 문구가 통으로 든 파일만 찾는다. 낱말마다 조건을 만들어 AND 로 걸고, 0건이면
  **두 낱말까지만** 완화한다 (한국어는 뒤 명사가 머리말이라 `입사자 + 교안` 을 남긴다)
- ⛔ **한 낱말까지 풀지 마라.** 실측에서 관련 없는 스프레드시트 4건이 나왔고 답변은
  그걸 찾은 것처럼 보여줬다 — **0건이라고 말하는 편이 낫다.** 잡음은 답처럼 보인다
- ⛔ **동의어를 손으로 쌓지 마라** (2026-08-14 사용자 판단). 두 가지로 위험하다:
  ① 끝이 없다 ② **틀린 동의어가 조용히 오답을 만든다** — 실제로 씨앗에 {실적·성과·매출·
  결산}을 넣었었다. "실적 자료"를 찾는데 "매출 시트"가 나오고 **그게 정답처럼 보인다**
  - 0건이면 **LLM(Flash)에게 대안 검색어를 묻는다** (`query_keywords.llm_variants`).
    새 용어("뉴크루")가 나와도 등록이 필요 없다. 성공 경로는 안 느려진다 — 0건일 때만 부른다
  - 코드에 남긴 씨앗은 **표기 변형만** (매뉴얼/메뉴얼, 시트/스프레드시트). 뜻이 비슷할
    뿐인 말은 넣지 마라 — 테스트가 `매출`·`실적`·`성과` 가 씨앗에 들어가면 실패시킨다
  - 사내 은어(`센앰`→센텔라 앰플)는 기존 `term_aliases` 사전이 계속 맡는다. LLM 이 알 수
    없는 말이고 관리자 검수 체계가 이미 있다
  - 넓혀 찾았으면 **반드시 답변에 밝힌다** ("'신규 입사자 교육'으로 넓혀 찾은 결과")
- 💡 **0건일 때 검색어를 로그에 남긴다** (`search_empty` WARNING). "결과 없음" 은
  정말 없을 때와 검색어가 망가졌을 때가 **똑같이 생겼다** — 그래서 오래 안 잡혔다

## 조용한 실패 탐지 (`tests/test_no_silent_failures.py`) — 2026-08-13

**발견이 늦는 결함은 예외를 던지지 않는다.** 없는 CSS 변수는 폴백으로 넘어가고, 템플릿
스크립트가 깨지면 서버는 200 을 주며, `@@` 목록이 어긋나도 질문만 조용히 오염된다.
사람이 눈으로 볼 때까지 아무도 모른다 — 그래서 "정상처럼 보이는 상태"를 겨냥해 검사한다.

| 검사 | 잡는 사고 |
|---|---|
| 정의되지 않은 CSS 변수 | 피드백 입력창이 라이트 모드에서 안 보이던 것 |
| 템플릿 스크립트 문법(`node --check`) | 보고서가 백지로 저장되던 것 |
| `@@` 프론트/서버 목록 일치 + 파싱 후 질문 오염 | `@@Google Workspace` 가 질문에 "Workspace" 를 남기던 것 |
| 자산 캐시 지문 배선 | 마운트 경로를 두 번 이어 붙여 캐시가 조용히 안 켜지던 것 |

- ⛔ **주석에 `var(--없는변수)` 같은 예시를 쓰지 마라** — 린트가 코드로 읽는다 (실제로 걸렸다)
- 새 검사를 넣을 때도 같은 기준으로: **에러가 안 나는 고장**만 여기 넣는다.
  터지는 버그는 이미 다른 방법으로 잡힌다

## SYSTEM STATUS 소스 선택 — 빈 선택은 **결정**이다 (2026-09-03)

사용자 제보: *"SYSTEM STATUS에서 전체선택 해제가 안되는데.."* — `전체 해제` 를 눌러도
Notion 팀 소스가 **누른 그 자리에서** 다시 켜졌다. 에러는 없다. 원인은 한 줄이 아니라
**"빈 선택" 을 "아직 안 고름" 으로 읽는 자리가 셋**이었다 (`app/frontend/chat.js`).

| 자리 | 하던 일 |
|---|---|
| `pollSystemStatus()` | 발견한 팀 키를 **폴링(30초)마다 무조건** `enabledSources` 에 되집어넣음 |
| `_reconcileEnabledSources()` | *비었으면 전체* 로 되돌림 → 새로고침하면 꺼 둔 것이 부활 |
| `loadEnabledSources()` | 저장분이 비어 있으면 저장 자체를 버림 |

- ⛔ **`전체 해제` 핸들러가 스스로 `pollSystemStatus()` 를 부른다** — 그래서 30초를
  기다릴 것도 없이 누르는 즉시 되살아났다. 증상이 "안 눌린다" 로 보이는 이유다
- **판정은 개수가 아니라 `localStorage` 키의 유무로 한다** (`_hasSavedSourcePrefs`).
  저장된 `[]` 는 "전부 끄기" 라는 사용자의 결정이다
- **새 소스는 대장(ledger)으로 가른다** (`skin1004_known_sources`) — 이미 본 적 있는
  소스는 다시 켜지 않고, 처음 보는 소스만 켜서 온다 (`_adoptNewSourceKeys`)
  - ⛔ **대장이 없을 때(이 판 이전부터 쓰던 사람)는 아무것도 켜지 않는다.** 그때
    전부를 '새것' 으로 세면 배포 직후 한 번, 꺼 둔 것이 통째로 되살아난다
  - ⛔ 그 대신 **첫 방문자에게는 대장을 빈 배열로 열어 준다** (`_saveKnownSourceKeys([])`).
    이 줄이 없으면 뒤늦게 도착하는 Notion 팀이 **처음 온 사람에게 전부 꺼진 채로** 보인다
- ⛔ **아직 안 온 소스를 없어진 소스로 세지 마라.** 팀 키는 상태 응답이 도착해야
  목록에 생긴다 — `_reconcileEnabledSources()` 가 그전에 잘라내면 골라 둔 팀이
  새로고침마다 지워진다 (2026-08-14 `loadEnabledSources` 에서 이미 한 번 겪은 함정이다)
### 고른 것은 실제 질문에 닿는다 — **좁혔을 때만 보낸다** (2026-09-03 사용자 결정)

⛔ 이 체크박스는 오래 **화면 표시 전용**이었다. `_sendSources` 가 `@@`·슬래시 지정이
없으면 무조건 `null` 이라(2026-04-09 커밋에서 바뀜), 화면은 `0/N 소스 활성` 이라고
말하는데 답변은 전부 조회했다. `getEnabledRoutes()`·`getEnabledTableKeys()` 는
**부르는 곳조차 없었다.** 배지가 거짓말을 하는 상태가 가장 나쁘다.

판정은 `_sourcesForSend()` 한 곳이 한다 (`@@` > 슬래시 > 좁힌 선택 > `null`):

| 화면 상태 | 보내는 값 | 서버가 하는 일 |
|---|---|---|
| 전부 켜짐 | `null` | 기본값 (BQ+GWS+Direct) — **손대지 않은 사람의 라우팅은 그대로** |
| 일부만 켜짐 | 그 목록 | 그 소스의 경로만 허용 |
| 전체 해제 | `[]` | `직접 대화만` (`_allowed_routes` 가 `{"direct"}` 를 준다) |

- ⛔ **전부 켜졌다고 통째로 보내지 마라** — notion·cs 경로가 기본으로 열려
  아무도 손대지 않은 사람의 답변까지 바뀐다. 기본 상태는 서버가 정한다
- ⛔ **빈 배열을 `null` 로 뭉개지 마라** — 그 둘은 서버에서 정반대다
- ⚠️ 부분 선택은 라우팅을 **넓힐 수도 있다** (팀 문서를 켜 두고 매출 하나를 끄면
  notion 경로가 열린다). 화면이 말하는 대로 동작하는 것이지만, 라우팅 문제를
  쫓을 때 이 가능성을 기억할 것
- ⚠️ 목록이 아직 안 왔으면(`DATA_SOURCE_KEYS` 가 비었다) 좁힌 것인지 알 수 없다 →
  서버 기본값. 지어내지 않는다

- 회귀: `tests/test_source_selection.py` (순수 판정 함수를 **node 로 실제 실행**)

## 프론트 스타일 규칙

- ⛔ **테마를 타야 하는 스타일을 자바스크립트 안에 인라인으로 두지 마라.** 테마 전환에서
  조용히 빠진다 (2026-08-13 사용자 제보). 피드백 모달이 `chat.js` 안에서
  `var(--panel,#1e1e1e)`·`var(--input-bg,#111)` 을 썼는데 **그 두 변수는 존재하지 않아**
  폴백(어두운 색)이 테마와 무관하게 늘 먹었다. 글자색만 `--text` 로 테마를 따라가서
  라이트 모드에서 **어두운 배경에 어두운 글자**가 됐고, 사용자가 쓴 글이 안 보였다
- **없는 변수는 에러가 아니라 폴백**이다 — 오타를 아무도 못 잡는다. 실제 토큰은
  `style.css` 상단 `html.dark`/`html.light` 블록에만 있다:
  `--bg`·`--bg-surface`·`--bg-elevated`·`--bg-input`·`--bg-hover`·`--border`·
  `--border-strong`·`--text`·`--text-secondary`·`--text-muted`·`--shadow-card`
  - ⚠️ 새 색을 쓸 때 **이 목록에 있는지 먼저 확인**할 것. `--panel`·`--card-bg` 같은 이름은 없다
- 같은 결함이 `eval_review.html` 에도 3곳 있었다 (2026-08-13 함께 수정)

## 캐시 버전 — ⛔ 손으로 세지 마라 (2026-08-31 폐지)

**`?v=` 는 서빙할 때 내용 해시로 자동으로 붙는다.** `app/core/asset_version.py` +
`main.py` 의 미들웨어. HTML 소스에 숫자를 적지 마라 — 지문이 덮어쓰므로 죽은 값이고,
남아 있으면 다음 사람이 그걸 보고 또 올린다 (`static_asset_stamp` 가 막는다).

**왜 폐지했나 — 5개월간 아무 일도 안 하고 있었다** (실측):

| | |
|---|---|
| 정적 파일 헤더 | `no-store, no-cache, must-revalidate` (2026-03-17 부터) |
| `?v=` 규칙이 문서에 들어온 날 | **2026-04-08 — 그 뒤다** |
| nginx 의 `expires`·`proxy_cache` | 없음 |
| 브라우저 실측 (새로고침 1회) | 자기 자산 8개 **전부 네트워크**, 디스크 캐시 0건, **776KB** |

⟶ 번호를 올리든 말든 브라우저는 매번 다시 받았다. 막고 있던 사고는 **일어날 수 없는
사고**였고, 대신 모두가 페이지를 열 때마다 776KB 를 다시 받고 있었다.

**그래서 방향을 뒤집었다** — 번호를 자동으로 만들고 그 대가로 진짜 캐시를 켠다.
실측: 두 번째 페이지 로드에서 **776KB → 0바이트**, 파일을 고치면 그것만 다시 받는다.

- ⛔ **가장 나쁜 실패는 "영영 낡은 파일에 갇히는 것"** 이다. 그래서 `immutable` 은
  **지금 파일의 지문과 일치하는 요청에만** 준다 (`asset_version.matches`). 안 맞으면
  예전처럼 `no-store` — 낡은 URL 은 굳지 않으므로 갇히는 경로가 **구조적으로 없다**
- ⚠️ 지문을 못 구하면 **기동 토큰**을 붙인다. 버전 없이 내보내지 않는다 — 그때만
  조용히 낡을 수 있다. 기동 토큰은 배포(재기동)마다 바뀐다
- ⚠️ 배선이 끊기면 나는 것은 에러가 아니라 **캐시가 조용히 안 켜지는 것**이다.
  실제로 마운트 경로를 두 번 이어 붙여(`/frontend/frontend/chat.js`) 한 번 그랬다 —
  `static_asset_stamp` 가 배선 세 곳을 매일 확인한다
- 💡 **CDN 파일도 함께 캐시된다** (chart.umd.min.js 207KB 등) — 사내망에 두고 쓰는
  파일이라 지문이 그대로 붙는다

## 작업트리를 다른 세션과 공유한다 — 전체를 건드리는 git 금지

- ⛔ `git commit -a` 뿐 아니라 **`git add -A`·`git add .`·`git add -u`** 도 남의 미완성
  파일을 내 커밋에 담는다. **`git reset --hard`·`git checkout -- .`·`git restore .`·
  `git clean -fd`·`git stash`(경로 없이)** 는 아예 **지운다** — 다른 세션은 자기 파일이
  사라진 줄도 모르고 계속 편집한다
  - 2026-08-31 실측: `commit -a` 만 막혀 있었고 **나머지 일곱 가지가 전부 통과**했다.
    한 철자만 막는 것은 규칙이 아니다. 지금은 훅이 여덟 가지를 함께 막는다
  - 내 것만 지목하라: `git add <경로>` · `git restore <경로>` · `git stash push -- <경로>`
  - ⚠️ 범위를 지목한 형태(`git add -A -- app/frontend`)와 브랜치 전환(`git checkout master`)
    은 그대로 통과한다 — 정상 작업을 막는 훅은 그날로 꺼지고, 꺼진 훅은 아무것도 안 지킨다
- 회귀는 `tests/test_claude_hooks.py` 가 **양방향**으로 지킨다 (막을 것 17 · 통과할 것 13)

## Entra ID 로그인과 셀라 사용자 관리 (2026-09-08)

- **로그인은 Entra ID만 사용한다.** `password_login_enabled=False`가 기본이다.
  기존 비밀번호 로그인·회원가입·재설정 API는 차단하며, 아래 비밀번호 복구 설명은
  롤백용 과거 구현에 대한 기록이다.
- **기존 계정이나 Entra 연결 기록은 로그인 증명이 아니다** (2026-09-09).
  `session_auth.decode_session()`은 검증된 Entra 콜백이 발급한 세션의 provider·tenant·oid를
  확인한다. 이전 쿠키는 다시 로그인해야 하며 `/me`는 현재 요청의 인증 증명만 갱신한다.
  공통 미들웨어가 화면·API를 보호하고, 인증 실패는 `X-Cella-Auth: login-required`로 표시한다.
- **관리자 명단은 `Entra 로그인 완료 / Entra 로그인 필요`로 표시한다.** 계정 존재와
  로그인 완료를 별개의 선택지처럼 나열하지 않는다. 미가입자의 사전 권한 배정은 유지한다.
- **서버 점검 토큰은 사용자 로그인이 아니다.** 골든셋·자가 점검은 별도 purpose·scope로
  직접 loopback의 `POST /v1/chat/completions`만 호출한다. 프록시 요청·관리자 API·세션 갱신에
  사용할 수 없으며, Entra 인증으로 표시하거나 기존 JWT를 자동 승격하지 않는다.
- **미가입 직원도 첫 로그인 시 계정이 생성된다.** 회사 tenant·서명·issuer·audience·
  만료·oid·nonce를 검증하고, 게스트·신원 충돌·비활성 계정을 거절한다.
- **권한의 원본은 `directory_users`다.** 기존 `ad_users`의 ID·손익·방문자 권한과
  SK/DD 그룹을 한 번 복사하고, `users.ad_user_id`·`user_groups.ad_user_id`의 외래 키를
  새 표로 연결한다. 컬럼명은 호환성을 위해 유지한다. `users.id`도 보존한다.
- **연결은 tenant+oid가 우선이다.** 최초 연결만 기존 회사 명단의 유일한 계정 로컬파트로
  찾고, 이미 다른 oid에 연결되었거나 후보가 여럿이면 중단한다. 로그인 UPN의 도메인이
  바뀌어도 기존 업무 메일 주소를 덮어써 Google 연결을 끊지 않는다.
- **미가입자에게도 관리자에서 손익·방문자·그룹을 미리 배정할 수 있다.** 첫 로그인은
  그 직원의 기록을 재사용하며 권한을 초기화하거나 관리자 권한을 자동 부여하지 않는다.
- **새 로그인 계정은 데이터 그룹 배정 후 질문·보고서를 이용한다.** 기존 계정의 접근은
  유지한다. `requires_group_assignment`는 신규 계정에만 1로 저장하며, 실제 접근은
  현재 SK/DD 등 브랜드 필터 그룹의 존재로 판단한다. 회수 후 전체 브랜드가 열리지 않는다.
- **전체 Entra 명단 조회에는 별도 Graph 권한·통신이 필요하다.** 9/8 실측으로 WAS 프록시
  통신이 막혔고, DB_PC에서 Graph 조회도 `403 Authorization_RequestDenied`였다.
  `User.Read.All` Application 권한과 관리자 동의, WAS/APP의 Graph HTTPS 허용이 필요하다.
  첫 로그인 계정 생성은 이 일괄 조회와 독립적으로 동작한다.
- **관리자 API는 `/api/admin/directory/*`다.** 화면은 셀라 사용자·Entra 동기화로 표시한다.
  동기화 실패 시 기존 명단·권한을 유지하고 실패 원인을 표시한다.
- **`ad_users`는 롤백 보관본이다.** 마이그레이션 완료 이후 권한 판단이나 정기 동기화에
  사용하지 않는다. 재시작 시 다시 복사하지 않으므로 회수한 권한이 되살아나지 않는다.
- **주요 파일**: `app/core/user_directory.py`, `user_directory_migration.py`,
  `app/core/entra_auth.py`, `app/api/entra_routes.py`, `app/api/admin_group_api.py`.
- **회신 URL은 설정값을 사용한다.** Host 헤더로 만들지 않으며 HTTPS가 필요하다
  (localhost 예외). state·nonce·PKCE verifier는 서버에서 일회성으로 관리한다.

## 비밀번호 복구 — 경로가 **둘**이고, 둘 다 있어야 한다 (2026-09-01)

로그인하지 못하는 사람에게 길이 하나뿐이면 반드시 누군가는 갇힌다.

**프로덕션 실측 (2026-09-01, UPN 반영 후) — 가입자 66명:**

| 경로 | 누가 쓰나 | 인원 | 계정을 바꾸나 |
|---|---|---|---|
| **구글 본인 확인** (`password_reset_google.py`) | 이메일이 바로 일치 44 + AD 주소의 **로컬파트로 도달** 20 | **64 (97%)** | **바꾼다** — 구글이 서명으로 신원을 보증한다 |
| **관리자 요청** (`password_reset.py`) | 회사 이메일도 AD 연결도 없는 사람 | 2 | ⛔ **아무것도 바꾸지 않는다** — 사람이 판단한다 |

- ⛔ **커버리지 같은 수치는 프로덕션에서 재라.** 로컬 dev DB(127.0.0.1)와
  프로덕션(10.1.200.5)은 **다른 데이터**다 — dev 는 66명이 아니라 58명이었고
  폴백값도 21 vs 18 로 달랐다. dev 에서 재고 "실측" 이라고 적으면 그 숫자는
  **틀린 채로 문서에 남는다**. 프로덕션 조회는 WAS 경유다
  (`ssh 10.1.150.5` → `./venv/bin/python`)

- ⛔ **`users.email` 만 보면 안 됐다 — 절반이 영영 못 썼다.** `users.email` 은
  가입 시 `ad_users.email` 에서 한 번 박히는데, 동기화가 `mail` 만 읽어서
  **활성 AD 436명 중 238명이 이메일 없는 사람으로 적재**됐고 그들에겐
  `ad_<id>@noemail.local` 폴백이 박혔다. 즉 **앞으로 가입할 사람의 절반도 같은
  처지였다.** 지금은 `mail or userPrincipalName` 이라 프로덕션 기준 **이메일 없는
  활성 계정이 0명**이다 (cravercorp.com 110 → 341)
  - 그래서 `_find_by_linked_google_account()` 가 **앱에서 연결해 둔 구글 계정**도
    함께 본다. 그 연결은 로그인한 상태로 본인이 만든 것이라 `users.email` 과 같은
    세기의 증거다. 실측으로 폴백 18명 중 4명이 이렇게 되살아났다
  - ⛔ **두 계정이 같은 구글 계정을 연결했으면 아무것도 고르지 않는다.** 하나를
    고르면 남의 계정 비밀번호를 바꿔 준다 — 모를 때는 멈추는 쪽이다
  - ⛔ **`mail` 만 읽지 마라 — `userPrincipalName` 이 있다** (2026-09-01 APP 실측).
    AD 를 직접 찔러 보면 `mail` 이 빈 **238명 전원에게 UPN 이 있고 전부
    `@cravercorp.com`** 이다. **`@cravercorp.com` 도 회사 메일이다**(사용자 확인) —
    이메일이 없는 게 아니라 **다른 칸에** 있었다. 동기화가 `mail or UPN` 을 쓰므로
    이제 신규 가입자에게 `noemail.local` 이 박히지 않는다
  - ⛔ **그렇다고 문자열 비교만 하면 여전히 안 맞는다.** 회사 도메인이 둘이고,
    별칭이면 **구글은 대표 주소만 돌려준다**:

    | 실제로 연결된 구글 계정 | 건수 |
    |---|---|
    | `@skin1004korea.com` | 8 |
    | `@cravercorp.com` | 1 |

    AD 가 `@cravercorp.com` 인 폴백값 4명 중 **3명이 `@skin1004korea.com` 으로
    인증**한다. 그래서 `_find_by_company_local_part()` 가 **로컬파트**로 잇는다.
    근거도 실측이다: AD 활성 436명의 로컬파트가 **436개(충돌 0건)** 이고,
    `mail`·UPN 을 둘 다 가진 **198명 전원이 로컬파트 동일**(다른 경우 0건)
  - ⛔ **로컬파트 매칭은 회사 도메인일 때만.** 구글 인증은 개인 계정도 통과하므로
    이 조건이 **유일한 방어선**이다 — 허용하면 `hong@gmail.com` 하나로
    `hong@cravercorp.com` 직원의 비밀번호를 바꿀 수 있다. 회사 도메인 목록은
    `ad_users` 에서 **실측으로** 만든다(손으로 적으면 낡는다). 공용 메일
    제공자는 그 목록에 들어와도 제외한다
  - ⚠️ `users.email` 은 가입 이후 **갱신되지 않는다.** AD 에 `mail` 이 나중에
    생겨도 폴백값 그대로다 (지금은 18명 전원 AD 에도 없어 해당자가 0명이지만,
    IT 가 채우는 순간 조용한 격차가 된다). 고칠 때 **GWS 토큰 파일이
    `users.email` 로 이름 지어져 있다는 것**을 함께 봐야 한다 — 그냥 바꾸면
    연결이 조용히 끊긴다

- ⛔ **부서·이름은 신원 증거가 아니다.** 로그인 화면이 이미 목록으로 보여 준다 —
  그것만으로 재설정되면 누구나 남의 계정을 초기화할 수 있고, 셀라에는 재무 손익(FI)
  데이터가 있다. 그래서 관리자 요청 경로는 계정을 건드리지 않는다. 두 경로의 차이는
  **신원 증거의 세기**이지 편의가 아니다
- ⛔ **리다이렉트 URI 를 새로 만들 수 없다** (구글 콘솔에 등록된 것만 유효). 그래서
  재설정도 기존 `/auth/google/callback` 으로 돌아오고 **state 의 `purpose` 로 갈라진다**
  - ⚠️ 그 분기는 **로그인 요구를 건너뛴다** — 재설정하려는 사람은 로그인이 안 되는
    사람이다. 그러니 ① **서명을 검증한 state 만** 그 분기로 들어오고 ② 분기는
    로그인 검사보다 **앞**에 있어야 한다 (뒤에 두면 영영 닿지 않는다). 회귀가 둘 다 지킨다
  - ⚠️ 일반 GWS 연결 state 가 이 분기로 새면 안 된다 — `purpose` 가 다르다는 것을
    테스트가 고정한다
- ⚠️ **이 경로는 쿠키를 보지 않는다** — state 가 서버에 있어서 `session_hop` 왕복이
  필요 없다. 콜백이 쿠키 없는 호스트(`…nip.io`)로 돌아오던 그 사고를 구조적으로 비켜간다
- ⛔ **scope 는 `openid email` 뿐이다.** 비밀번호를 찾으려는 사람에게 메일·드라이브
  열람 동의를 물으면 그 화면 자체가 이상하고, 저장할 필요 없는 토큰이 생긴다.
  신원은 **`id_token` 서명 검증**으로 얻는다 (토큰 응답 본문의 이메일을 믿지 않는다)
- ⚠️ 확인 증표(grant)는 **일회용·5분**이고, 착륙 지점이 **주소창에서 HttpOnly 쿠키로
  옮긴다** — URL 에 남으면 방문기록·리퍼러·화면 공유에 그대로 실린다
- ⛔ **길이 검사는 증표를 태우기 전에** 한다. 뒤에 두면 짧게 한 번 눌렀다가 처음부터 다시다
- ⛔ **성공하면 대기 중인 관리자 요청을 함께 닫는다.** 안 닫으면 관리자가 임시 비밀번호를
  발급하고 **그 순간 본인이 방금 정한 비밀번호가 무효가 된다** (붐따 규칙과 같은 계열:
  "수정과 표시는 한 작업이다")
- ⚠️ 본인이 고른 비밀번호에는 `must_change_password` 를 **세우지 않는다** (관리자 임시
  발급과 다르다). 대신 서 있던 강제는 **내린다** — 안 내리면 로그인 후 계속 막힌다

## Entra 사용자 동기화 규칙 (2026-09-08)

- **스크립트**: `scripts/sync_entra_users.py`. APP 서버 크론에서 매일 22:00 실행한다.
  `scripts/sync_ad_users.py` 실행도 새 스크립트로 연결하는 호환 진입점이다.
- 전체 Graph 페이지를 정상 조회한 뒤에만 트랜잭션으로 명단을 반영한다. 부분 조회·권한 오류·
  통신 실패 때는 직원 비활성화나 권한 덮어쓰기를 하지 않는다.
- 이름·메일·부서·Entra 활성 상태만 갱신하며 손익·방문자·그룹 권한은 관리자의 설정을 유지한다.
- **모니터링**: `directory_sync_state`와 `entra_directory_sync` 작업 이력.
  Graph 권한·네트워크 미설정 상태를 성공이나 최신 상태로 표시하지 않는다.
- **서버 실행 환경**: 앱 `.env`와 사내 HTTP/HTTPS 프록시 환경을 함께 제공한다.
- `python scripts/sync_entra_users.py --dry-run`은 마이그레이션 상태만 읽으며 DB를 수정하지 않는다.
- 회사 AD 계정 자체를 삭제하지 않는다. 셀라의 AD 보관 표 삭제 여부는 이관 검증 후 결정한다.
